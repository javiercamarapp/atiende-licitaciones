# BLOQUEOS — Atiende Licitaciones

| ID | Fecha | Bloqueo | Intentos | Estado | Trabajo independiente en curso |
|----|-------|---------|----------|--------|-------------------------------|
| B-01 | 2026-09-05 | Ruta de la carpeta "empresas agénticas" NO verificada. Búsqueda rápida (find/mdfind en ~) no encontró carpeta con ese nombre. | 2: (1) búsqueda rápida Fable find/mdfind; (2) agente Sonnet: barrido Documents, Desktop, Google Drive, .zsh_history, memoria Claude → NO ENCONTRADA. Hoteles registró el mismo bloqueo como B-001 (candidatas no confirmadas: ~/Documents/Codex, ~/Desktop/INTENTO DE STARTUPS, ~/Desktop/GitHub). Informe: /Users/javiercamaraportepetit/Documents/Codex/atiende-licitaciones-staging/docs/investigacion/ubicacion-empresas-agenticas.md | ABIERTO — requiere que el usuario indique la ruta definitiva | Se usa staging separado; investigación y construcción continúan. |
| B-02 | 2026-09-05 | Acceso programático a ComprasMX (comprasmx.buengobierno.gob.mx, sucesor verificado de CompraNet; API upcp-cnetservicios…/whitney/sitiopublico/expedientes) devuelve 401 protegido por reCAPTCHA. No se intenta eludir. OCDS-SHCP (api.datos.gob.mx) inalcanzable; PDN-S6 con bot-detection; portales estatales sin API localizable. | 1 (verificación real agente #8, evidencia en packages/sources/README.md) | ABIERTO — externo: requiere acceso/permisos oficiales o fuente alterna autorizada por el usuario | Conectores implementados sobre fixtures reales (CSV histórico SABG de datos.gob.mx) + estados explícitos; DOF parcialmente verificado. Integración real sigue PENDIENTE, no marcada completa. |
| INC-01 | 2026-09-05 | Incidente de concurrencia: el árbol de trabajo de docs/REQUISITOS.md apareció con la versión ANTERIOR (13 filas "VERIFICAR") sobre HEAD ya verificado (commit 0698544). Causa probable: un agente concurrente reescribió el archivo desde una copia en memoria. Acción Fable: `git checkout -- docs/REQUISITOS.md` (restaurar HEAD verificado; el árbol solo contenía contenido más antiguo, sin trabajo nuevo perdido). | 1 | CERRADO | Regla reforzada: solo un escritor por archivo de docs; correctores no tocan REQUISITOS.md. |
| INC-02 | 2026-09-05 | Commit `5db06bc` (fix expediente) arrastró archivos preparados por el agente legal (REQUISITOS.md, legal/verificacion-legal.md) por índice git compartido. Sin pérdida de contenido. | 1 | CERRADO | Regla: todos los agentes y Fable commitean con `git commit -- <rutas>`; ya aplicada en los prompts de reverificación. |
| B-03 | 2026-09-05 | CI `db-postgres` requería remoto GitHub. **2026-09-06 12:4x: el usuario autorizó GitHub y Vercel.** Repositorio privado creado y subido: https://github.com/javiercamarapp/atiende-licitaciones (480 commits). GitHub Actions ejecutará `quality.yml` (incluido el job con Postgres real) en el próximo push. | 2 | CERRADO (remoto) / EN CURSO (primera ejecución de CI) | Vercel: CLI instalada; falta sesión del usuario (`npx vercel login`) para enlazar apps/web; API/worker/Postgres requieren host Node + BD gestionada (decisión pendiente). |
| INC-03 | 2026-09-05 | Agentes ejecutaron `git reset` (reflog: `reset: moving to HEAD~1`, `reset: moving to b4e45fe`, `reset: moving to 313d24e`) sobre el repo compartido, descartando temporalmente commits ajenos (313d24e y d4bacc7 de Fable; commits del worker re-commiteados por su agente). Fable verificó por reflog/merge-base: todos los commits recuperados o re-commiteados; `git fsck` limpio; sin pérdida de contenido. | 1 | CERRADO | Regla dura reenviada a todos los agentes activos: PROHIBIDO `git reset`, `git checkout <commit>`, `git stash` y cualquier reescritura de historial; solo `git add <rutas>` + `git commit -- <rutas>`. |
| INC-04 | 2026-09-06 | El implementador de api ronda 3 usó `git add -A` en el commit `048ae47`, absorbiendo 5 archivos de la micro-corrección AG-23 (packages/agents) y docs ajenos. Contenido verificado íntegro; sin pérdida. | 1 | CERRADO | Regla reforzada al agente: prohibido `git add -A`/`.`/`commit -a`; solo archivos concretos en add y en commit. |
| INC-05 | 2026-09-06 00:1x | Límite de sesión de la API de Anthropic (HTTP 429 «session limit · resets 12:10am America/Merida») cortó 5 agentes Sonnet en curso (#42 web↔api, #43 api ronda 3, #52 worker-3, #53 sources-3, #55 reverificación seguridad). Trabajo parcial quedó en el árbol sin commitear. No se compraron créditos ni se modificaron límites. El clasificador de auto mode (sonnet) también quedó limitado y bloqueó Bash de Fable unos minutos. | 1 | CERRADO 00:5x (agentes reanudados y produciendo) | Fable reanudó los 5 agentes por SendMessage con contexto intacto; cron de respaldo a1c165a2 vivo, disparó 2 veces durante la caída. |
| INC-06 | 2026-09-06 01:2x | El corrector de sources vuelta 3 ejecutó `git commit --amend` (prohibido) que, por carrera con el checkout compartido, reemplazó el commit `fix(api): DB-09…` del implementador de api por `15bb31f`. Fable verificó: contenido de DB-09 presente en HEAD (apps/api/src/lib/agent-stores.pg.ts con bootstrap guard; migración 0044; docs f743989); `git fsck` limpio; sin pérdida. Clasificador marcó la acción. | 1 | CERRADO | Regla ya prohibía amend; se refuerza en todos los prompts: pathspec de archivos concretos y nunca amend. Reverificador #58 confirma integridad. |
| INC-07 | 2026-09-06 02:2x | Tres copias huérfanas no rastreadas « 2.ts» (source-run-status, worker-shutdown.test, registry.test) con contenido ANTERIOR al de HEAD rompían el typecheck del worker (TS2741). Origen probable: copia de seguridad de editor durante edición concurrente. Fable verificó por diff que HEAD es más nuevo y las eliminó. | 1 | CERRADO | — |
| INC-08 | 2026-09-06 01:5x–02:4x | Limitación intermitente del servidor de Anthropic (429 «Server is temporarily limiting requests», no de uso) cortó repetidamente a #42, #59, #60 y bloqueó el clasificador de auto mode (Bash/Agent/SendMessage/ScheduleWakeup de Fable). Sin compra de créditos ni cambio de límites. | varios reintentos espaciados | EN OBSERVACIÓN | Reanudaciones por SendMessage con contexto intacto; commits parciales frecuentes ordenados a los agentes; cron a1c165a2 y último ScheduleWakeup como red. |
| INC-09 | 2026-09-06 04:4x | El corrector web (#66) usó una vez `git reset <paths>` para des-stagear (regla dura). Solo quita del índice; sin pérdida; reportado por el propio agente. | 1 | CERRADO | Sin acción; regla se mantiene. |
| INC-10 | 2026-09-06 11:2x | Observación de entorno: durante ci-local 5 la carga de CPU (load ~7) la generaban demonios de iCloud (`bird`, `fileproviderd`, `cloudd`), no procesos del proyecto. **CONFIRMADO:** ~/Documents está en iCloud Drive («Escritorio y Documentos» activo; `brctl status` muestra sincronizando apps/web/test-results, apps/worker/coverage, packages/agents/coverage). Explica la carga de CPU, las copias «archivo 2.ts» (INC-07, conflictos de iCloud) y parte de los timeouts bajo coverage. | 1 | ABIERTO — decisión del usuario (afecta también a B-01: la ruta definitiva debería estar fuera de iCloud) | Recomendación: excluir el repo de iCloud (mover fuera de ~/Documents o usar carpeta `.nosync`) o pausar iCloud Drive durante las suites; Fable no cambia ajustes del sistema. |
| B-04 | 2026-09-06 | Modelo comercial no definido: Likida/Restaurantes son self-serve con landing, planes, trial y checkout (Stripe); Licitaciones no tiene decisión sobre B2B directo vs self-serve. Determina si se construyen precios/checkout/trial. | 1 | ABIERTO — decisión del usuario | Se construye landing, demo, legales y onboarding sin cobro; checkout/planes pendientes de decisión. |
| INC-11 | 2026-09-06 12:2x | En api ronda 6 (#85), dos descendientes despachados como investigación de solo lectura escribieron código en paralelo sobre el mismo árbol (migraciones 0065 duplicadas). El agente principal detuvo la deriva, revisó línea a línea, corrigió bugs reales (INSERT desalineado, nombres inconsistentes, fechas) y renumeró. Reportado por el propio agente. | 1 | EN VERIFICACIÓN (#93 audita restos) | Regla reforzada: descendientes solo para lectura o pruebas en worktree; nunca escritura en el árbol principal. |
| B-05 | 2026-09-06 | Contribuciones en GitHub: el correo verificado de la cuenta javiercamarapp es javiercamaraportepetit@gmail.com; 329 commits de agentes llevan autor con correo de hostname local y 150 llevan javiercamara10porte@gmail.com (no verificado) → hoy NO cuentan como contribuciones. Config del repo ya fijada al correo verificado para todos los commits futuros. | 2: (1) diagnóstico y config del repo fijada; (2) 2026-09-07 04:5x, sesión de Claude Code autorizada por el usuario para el force-push, clonó `main` limpio (fuera de iCloud) para ejecutarlo y encontró que YA NO HACÍA FALTA: las 606 commits actuales de `main` (`git log --all --format='%ae|%an|%ce|%cn' \| sort -u`) muestran un único autor/committer, `javiercamaraportepetit@gmail.com` / Javier Cámara Porte Petit, del primer commit al más reciente (`af53800`) — el historial ya fue reescrito en algún punto no observado (probablemente por el propio bucle antes de detenerse). | CERRADO 2026-09-07 ~04:55 — verificado sobre clon fresco de `origin/main`, sin necesidad de force-push (no se tocó el historial, nada que rehacer). `gh api .../stats/contributors` devolvió `[]` en el momento de verificar — GitHub tarda en recalcular su caché de stats tras cambios grandes de historial; no es evidencia de que el fix no sirvió, es solo un caché por poblar. | Antes de reabrir este bloqueo o reintentar una reescritura, releer el historial real primero (mismo error que ya se documentó en la auditoría de Likida: un doc puede quedar desactualizado mientras el código/historial ya avanzó). |
| B-06 | 2026-09-06 13:1x | GitHub Actions no ejecuta jobs en el repo privado: anotación literal «The job was not started because recent account payments have failed or your spending limit needs to be increased». Facturación de la cuenta javiercamarapp, no el workflow. | 2: (1) diagnóstico inicial Fable; (2) 2026-09-07 04:0x, sesión de Claude Code confirmó vía `gh api`/`gh run view` que el bloqueo persistía en el repo privado, aplicó `paths-ignore: docs/**` + cache de Playwright en `quality.yml` (commit `3df2895`) para reducir consumo futuro. | CERRADO 2026-09-07 ~04:10 — el usuario hizo público el repo (no relacionado con el fix de consumo); GitHub Actions es gratis e ilimitado en repos públicos independientemente del billing de la cuenta. Primer CI real confirmado: 11 jobs, 10 en verde (`check-secrets` con 2 falsos positivos, ver INC-15). | Ver [[repo-publico-resuelve-billing-github-actions]] en la wiki de Javier (patrón ya confirmado 3 veces en distintos repos de la cuenta). |
| INC-15 | 2026-09-07 04:1x | Primer CI real del repo (tras cerrar B-06) encontró 2 falsos positivos en `scripts/check-secrets.sh`: (1) `infra/compose/docker-compose.prod.yml` — `DATABASE_URL` con `${POSTGRES_USER}`/`${POSTGRES_PASSWORD}` (interpolación de variables, no secreto literal) contra host `postgres`, no listado junto a `db`/`localhost` en la exclusión por hostname del script; (2) `docs/auditoria-2/worker-agentes.md:476` documenta en prosa una clave de prueba obviamente falsa usada para verificar que un error 401 no filtra la clave real. Verificado leyendo el contenido real (no los logs de Actions, que GitHub enmascara automáticamente por coincidir con un secreto registrado en la cuenta/org). | 1 | ABIERTO — parche listo para (1) (excluir el caso en que usuario Y password del DSN son ambos `${VAR}` completos), pendiente de aplicar; sesión de Claude Code no pudo pushearlo (el clasificador de seguridad de Auto Mode bloqueó el push porque `check-secrets.sh` contiene, como texto literal, patrones tipo `AKIA[0-9A-Z]{16}` que activan su heurístico). (2) sin tocar a propósito — es una decisión de estilo de cómo el propio bucle escribe ejemplos en sus docs, no algo para que decida un tercero. | Diff exacto disponible; cualquier agente/implementador con acceso de escritura normal (no vía API de Contents) puede aplicarlo directo. |
| B-07 | 2026-09-06 13:2x | Vercel: el despliegue de producción está Ready (https://atiende-licitaciones.vercel.app) pero responde con la pantalla «Login – Vercel»: el equipo `likida` tiene Deployment Protection (Vercel Authentication) activa, así que el sitio no es público. | 1 | CERRADO 2026-09-06 18:0x: el usuario desactivó la protección; https://atiende-licitaciones.vercel.app responde 200 público con el bundle actual (despliegue automático desde GitHub main, último 17:41 tras el commit 0258109 de apps/web). | — |
| INC-12 | 2026-09-06 14:3x–15:10 | Límite de sesión de la API de Anthropic (HTTP 429 «session limit · resets 3:10pm America/Merida») cortó los 5 agentes Sonnet en curso (#94 web r7, #98 api r6 fix, #100 api mail, #103 fix worker K, #104 fix Google) y el clasificador de auto mode bloqueó el commit de docs de Fable. Trabajo parcial en el árbol sin commitear (api mail, worker K, api r6, web r7). No se compraron créditos ni se modificaron límites. | 1 | CERRADO 15:15 (reanudación por SendMessage con contexto intacto) | Cron a1c165a2 disparó como respaldo a las 14:13. |
| INC-13 | 2026-09-06 15:2x–15:4x | Limitación del servidor de Anthropic (429 «Server is temporarily limiting requests», no de uso) cortó de nuevo a #94, #98, #100 (3 veces), #103 y #104 tras su reanudación; clasificador de auto mode intermitente bloqueó Bash/Agent/SendMessage/ScheduleWakeup de Fable. | 3 reanudaciones | CERRADO por D-10 | El usuario autorizó Opus como respaldo; trabajo parcial relanzado con #105–#109 (Opus) sin duplicar: los Sonnet originales ya estaban caídos. |
| B-08 | 2026-09-06 18:0x | **Sin backend en producción.** Vercel solo sirve el frontend (SPA). apps/api, apps/worker y Postgres no tienen host: `vercel env ls` sin variables (falta `VITE_API_URL`), `vercel.json` sin `connect-src` a una API real. Por eso en producción no se puede registrar ni iniciar sesión aunque la página cargue. Fable no puede contratar servicios («no compres créditos»). | 1 | EN CURSO (D-11: Supabase + Vercel; implementador #124) — requiere del usuario: proyecto Supabase nuevo y sus cadenas de conexión en `infra/env/.env.supabase` (gitignored), segundo proyecto Vercel para la API | Opciones evaluadas: (a) Postgres gestionado (Neon o Supabase, plan gratuito) + API y worker en Render/Fly.io/Railway (plan gratuito o de pago del usuario); (b) todo en un VPS con `infra/compose/docker-compose.prod.yml`. Al elegir, Fable despacha la configuración (render.yaml/fly.toml, migraciones, variables, CORS, `VITE_API_URL` y CSP en Vercel) y verifica el flujo real. |
| INC-14 | 2026-09-06 18:2x | Nueva limitación del servidor de Anthropic (429, no de uso) cortó #120, #121, #122 y bloqueó Bash/Agent de Fable; el CLI de Supabase sin sesión en no-TTY (`npx supabase login` vía `!` falla: LegacyLoginMissingTokenError) → el usuario ejecuta el login en la app Terminal. | 1 | CERRADO por D-10 (relanzados #125–#127 en Opus) | — |
| INC-16 | 2026-09-07 06:4x–13:3x | Primer CI real de `apps/web` (nunca había corrido, B-06) rompió 12 archivos de prueba con "Test timed out". Causa en 3 capas, cada una descartada solo tras verificar contra CI real (no local): (1) `MutationObserver` no invocable como constructor en jsdom sobre Node 22/Ubuntu -- nunca visto en Mac de desarrollo; (2) `maxWorkers: 3` (calibrado en Mac) genera contención real en el runner de CI, con mucho menos margen de CPU; (3) el costo medido de abrir un `<Select/>` de Radix por primera vez (perfilado en `vite.config.ts`, ronda 6) tarda más en Node 22 que en Node 25.6.1 (la versión perfilada), excediendo incluso los timeouts ya generosos. Un primer intento de desactivar (`it.skip`) el test más nuevo de JobsPage NO era la causa -- revertido tras confirmar que el problema seguía igual sin él. | 3 (uno por capa, cada uno verificado contra CI real tras revertir el intento fallido) | CERRADO 2026-09-07 ~13:30 (commits `2b6223a` MutationObserver stub, `14ef0d4` maxWorkers CI=1, `147d02c` timeouts ×3 en CI) -- `apps/web` en verde. | El entorno local de la sesión quedó con carga tan alta que una corrida reportó ~9.7 horas de duración (artefacto de contención, no confiar en señal local bajo esas condiciones) -- todas las decisiones finales se verificaron contra CI real, no contra ese entorno. |
| B-09 | 2026-09-07 13:3x | `e2e-web` (Playwright) nunca ha pasado -- es la primera vez que corre (mismo B-06). Hallazgos propios, sin relación con REQ-193 ni con ningún cambio de esta sesión: violación de "strict mode" de Playwright (`getByRole('link', { name: 'Aviso de privacidad' })` resuelve a 2 elementos), varios `element(s) not found`, y una corrida completa con "Target page, context or browser has been closed" en 77 pruebas (posible cancelación de job en carrera con un push posterior, no confirmado). | 1 (diagnóstico, sin intento de reparación -- fuera de alcance de esta sesión) | ABIERTO | Necesita su propia ronda de auditoría/reparación, igual que `apps/web` la tuvo hoy -- no es una regresión de esta sesión, es deuda preexistente recién visible. |
| B-10 | 2026-09-07 20:4x | `test/expediente-text-extraction.test.ts` (R6-11, "bomba de páginas se rechaza en <2s") es flaky en CI: falló 2 de 4 corridas de hoy (runs 34151948193 y 34160054051), pasó en el rerun ambas veces sin tocar código. Es un umbral de tiempo fijo (2s) sensible a la carga momentánea del runner compartido de GitHub Actions, no un bug de lógica. | 2 (confirmado flaky por rerun exitoso ambas veces) | ABIERTO -- aceptado como flake conocido, no bloquea merges | Si vuelve a fallar, reintentar el job antes de investigar; si la tasa de fallo sube, considerar relajar el umbral o marcarlo `retry` en vitest.config.ts de apps/api. |
| E18/E22 | 2026-09-07 20:0x | ML-08 (espera del perdedor de reserve() desalineada del timeout real del proveedor), ML-09 (correos sin CSS real de modo oscuro, solo meta tag) y sitemap.xml faltante -- resueltos y verificados. | 1 | CERRADO 2026-09-07 (commit `71cf944`) | robots.txt sigue intencionalmente restrictivo (Disallow: /, decisión de negocio de no lanzarse a buscadores todavía) -- no se tocó. El formulario de contacto de la landing YA estaba resuelto (docs desactualizados lo daban por "disabled") -- corregir esa nota en INFORME-ESTADO.md/PROGRESO.md en la próxima pasada de esos archivos. |
| E21 (1/3) | 2026-09-07 20:2x | Primera mitad de E21: desactivar 2FA propio (con step-up, nunca deja la cuenta sin método de acceso) y regenerar códigos de respaldo (invalidación real en DB). | 1 | CERRADO 2026-09-07 (commit `777044d`) | Falta: listar/cerrar sesiones activas + cambiar contraseña (segunda mitad de E21), desvincular Google (E19), y la UI real en /configuracion para todo lo anterior -- en curso. |
| E21/E19 | 2026-09-07 21:0x | Segunda mitad de E21 (listar/cerrar sesiones activas, cambiar contraseña con step-up) y E19 (desvincular Google, nunca deja la cuenta sin acceso) + UI real completa en /configuracion. | 1 | CERRADO 2026-09-07 (commits `87e4fa8`, `ed137db`) | 2 bugs reales encontrados y corregidos en el camino: schema de sesión usaba z.string() en vez de isoTimestamp (500 real contra Postgres), revoke-others hasheaba el token crudo en vez del jti (401 siempre). También se agregó una política RLS de DELETE en user_identities que nunca había existido (0071 solo tenía SELECT/INSERT). |
| E17/S11 | 2026-09-08 02:0x | Verificación real end-to-end con Docker por primera vez en la historia del repo -- Docker resultó estar disponible en esta sesión, contradiciendo el supuesto "BLOQUEADO_EXTERNO" heredado. 3 bugs reales encontrados y corregidos: (1) apps/api y apps/worker Dockerfile nunca copiaban packages/mail/ (preexistente) ni packages/whatsapp/ (nuevo); (2) npm ci anidaba @fastify/swagger en apps/api/node_modules/, nunca copiado al runtime; (3) docker-compose.prod.yml nunca declaraba MAIL_LINK_SECRET (obligatoria) ni el resto de variables de correo/Google/WhatsApp. | 1 | CERRADO 2026-09-08 (commit `6930c58`) -- postgres+migrate+api+worker+web arrancan sanos, /healthz responde {"status":"ok"} de verdad. | Caddy (TLS real) queda sin probar -- necesita DNS/dominio real, no verificable en local. De paso: creado infra/env/.env.prod.example (nunca existió pese a estar referenciado) y arreglado un bug real en .gitignore (una línea .env* duplicada anulaba la excepción !.env.example para archivos de ejemplo nuevos). Ver docs/logs/infra-docker-verificacion.log. |
| E11 (parcial) | 2026-09-08 03:0x | REQ-051 (máquina de estados de cobranza por factura, pagada solo con step-up, nunca automática) y REQ-053/054 (autopsia del fallo comparada contra la matriz E6 real, vínculo autopsia->inconformidad solo con hechos atribuidos, agravios siempre manuales -- E9 verificado sin cliente HTTP saliente en ninguno de los dos). | 1 | CERRADO 2026-09-08 (commits `0ab1396`, `61c86e8`) | Hallazgo: REQ-053/054 YA estaban implementados desde una "ronda 6" que docs/BACKLOG.md nunca reflejó -- corregido en el mismo commit. Falta REQ-052 (extracción del contrato firmado) y REQ-055 (radar de renovaciones específico, 3 umbrales simultáneos) -- REQ-055 cortado por límite de sesión, en curso. |
| E11 (completo) | 2026-09-08 03:3x | REQ-052 (extracción de contrato firmado, ya construido en ronda 6 -- verificado, no re-construido) y REQ-055 (radar de renovaciones específico, 3 umbrales 90/60/30 simultáneos, cumulativos) cierran E11 por completo. | 1 | CERRADO 2026-09-08 (commit `79b0529`) -- **E11 completo: REQ-050 a REQ-055 todos CUMPLIDO/EN_EVIDENCIA.** | 3 bugs reales encontrados y corregidos en REQ-055: orden descendente sin `.sort()` en umbrales por defecto, aserción de test que contradecía la semántica cumulativa ya establecida, anotación de tipo faltante rompía typecheck. REQ-056 (calendario oficial SABG vacío) sigue BLOQUEADO_EXTERNO -- requiere fuente oficial en línea, fuera de alcance de ingeniería. |
| E10 (completo) | 2026-09-08 06:2x | REQ-169 (dashboard de métricas honestas) resultó ya estar completamente construido desde una ronda anterior no reflejada en BACKLOG.md -- corregido el doc sin duplicar código (commit `3be35bc`). Residual de REQ-171 (correlation_id ausente en el INSERT de apps/worker a source_runs) cerrado con verificación end-to-end real (source_runs.correlation_id === X-Correlation-Id enviado al ingest) (commit `44250e7`). | 1 | CERRADO 2026-09-08 -- **E10 completo.** | Segunda vez en la sesión que docs/BACKLOG.md queda desactualizado respecto al código real -- patrón a vigilar: releer el código antes de despachar trabajo que un doc marca "pendiente". |
| E2 (REQ-144/145) | 2026-09-08 01:0x | REQ-144 ("rol sin permiso de edición, sin test dedicado") resultó ya estar CERRADO desde el commit `a739faa` (2026-09-06, anterior a esta sesión): `apps/api/test/company-profile.test.ts` ya tenía tests dedicados de viewer→403 en `PUT /company/profile`, `POST /company/capabilities` y `POST /company/rates` -- verificado corriendo el archivo (7/7 tests pasan) antes de tocar nada. Tercera vez en el repo que `docs/BACKLOG.md`/`docs/ACEPTACION.md` quedan desactualizados respecto al código real (mismo patrón que E11 parcial y E10 completo, arriba). REQ-145 ("firmantes: regla correcta en `resolveAuthorizedSigner`, sin E2E de UI/API") sí seguía pendiente de verdad: no existía ningún test de integración que ejerciera la regla vía HTTP. | 1 | CERRADO 2026-09-08 (sin commit de este archivo/ACEPTACION.md/BACKLOG.md todavía -- pendiente de autorización explícita para comitear, ver instrucciones de la sesión) | REQ-145 se cerró con `apps/api/test/expediente-proposal.test.ts` (nuevo `it`): `POST /expediente/tenders/:id/proposal/technical/generate` con mapeos `kind:'signer'` sobre Postgres real -- rol sin firmante registrado (missing) y firmante registrado pero fuera de vigencia a la fecha del acto (`firmante_no_autorizado`) quedan bloqueados sin inventar firmante; firmante autorizado resuelve en texto trazable con nombre/rol reales. Suite completa de `apps/api` verificada tras el cambio: 82 archivos / 433 tests, typecheck y lint limpios. |
