# PROGRESO — Atiende Licitaciones

Formato por entrada: requisito · acción · archivos/commit · prueba · resultado · siguiente paso.

## Ronda 0 — arranque (2026-09-05)
- **Requisito:** continuidad real del bucle. **Acción:** `/loop` dinámico invocado; `CronCreate` respaldo `a1c165a2` (`13 */2 * * *`) verificado con `CronList`. **Archivos:** docs/operacion-bucle.md. **Prueba:** salida CronList. **Resultado:** OK. **Siguiente:** primer `ScheduleWakeup` al cierre del turno.
- **Requisito:** staging separado de Hoteles. **Acción:** creado `/Users/javiercamaraportepetit/Documents/Codex/atiende-licitaciones-staging` con `git init`. **Resultado:** OK (ruta definitiva de empresas agénticas pendiente de verificación, ver BLOQUEOS).
- **Requisito:** investigación delegada a Sonnet. **Acción:** 4 agentes model=sonnet despachados (PDF, frontend Restaurantes, Likida, ubicación). **Resultado:** en curso. **Siguiente:** al recibir informes, Fable define arquitectura y despacha ronda 1 de implementación.

## Ronda 0 — resultados parciales (2026-09-05, mismo turno)
- **Requisito:** ubicación definitiva. **Acción:** agente Sonnet #4 completó barrido. **Archivos:** docs/investigacion/ubicacion-empresas-agenticas.md. **Resultado:** NO ENCONTRADA; bloqueo B-01 documentado (2 intentos). **Siguiente:** seguir en staging; usuario debe indicar ruta.
- **Requisito:** paridad visual con Restaurantes. **Acción:** agente Sonnet #2 completó inventario. **Archivos:** docs/investigacion/frontend-restaurantes.md. **Resultado:** stack Vite 8 + React 18 + TS + Tailwind 3.4 + shadcn/Radix + Supabase; logo como componente React (SVG copiado al informe); sidebar por grupos; admin sin móvil real (a corregir en Licitaciones); mapa sidebar y tabla origen→destino listos. **Siguiente:** Fable decide stack final al llegar informe Likida (#3) y REQUISITOS (#1); luego ronda 1 de implementación.
- **Pendientes en curso:** agentes Sonnet #1 (PDF → REQUISITOS/ACEPTACION) y #3 (Likida arquitectura).

## Corrección del usuario (2026-09-05 18:3x) y ronda 1
- Investigación: se considera COMPLETA (04-Licitaciones-investigacion, DECISIONLLMLICITACIONES.pdf, 01-Blueprints). Agentes #1 y #3 recibieron orden de cerrar con solo lo implementable; no se abren nuevas líneas de investigación. Rentas no se inicia desde esta sesión.
- **Decisión de arquitectura (Fable, por evidencia de entorno: solo node 25 + npm; sin docker/postgres/supabase/deno):** monorepo npm workspaces; `apps/web` Vite+React 18+TS+Tailwind 3.4+shadcn (paridad con Restaurantes); `packages/db` migraciones SQL Postgres + RLS ejecutadas en pruebas sobre PGlite (Postgres embebido) y en producción con `pg`; `apps/api` Fastify+TS+zod+JWT, idempotencia, rate limit, audit_log, jobs con reintentos. Ubicación definitiva (B-01) NO es impedimento técnico: se construye en staging.
- **Ronda 1 despachada:** agente #5 frontend base; agente #6 núcleo db+api. Commit raíz `c8834bd` (workspace). **Siguiente:** al completar, Fable despacha auditoría adversarial (contexto independiente, docs/auditoria-1/) y ronda 2 de módulos de dominio con REQUISITOS.md.
- **Ronda 0 cierre #3 (Likida):** docs/investigacion/likida-arquitectura.md (301 líneas). Copia más reciente: /Users/javiercamaraportepetit/Documents/Codex/2026-08-30/haz-x20/work/repo (auditoría 22, 30-ago) → referencia definitiva de patrones; audit-likida (auditoría 18) queda como secundaria. Patrones fijados para auditoría: auditores solo encuentran, verificación adversarial, corrección en serie con revert si falla, tope 3 vueltas, estado en docs/auditoria-N/. Esquema SQL y 9 reglas de diseño disponibles para ronda 2.
- **Ronda 0 cierre #1 (requisitos):** docs/REQUISITOS.md (140 REQ, 28 módulos), docs/ACEPTACION.md (140 criterios PENDIENTE), docs/investigacion/pdf-resumen.md. Jurisdicción: México (federal + estatales). Fuentes: ComprasMX, DOF, OCDS-SHCP, PDN-S6, portales estatales. Decisión LLM de los PDF: OpenAI Agents SDK/Responses API, 3 niveles de modelo, componentes "tolerancia cero" en proveedor EE.UU., gates para modelos alternativos. Legal: varias citas marcadas VERIFICAR (vigencias LAASSP contradictorias, plazo de pago en disputa) → no se afirma cumplimiento legal sin fuente. L11–L21: cubiertos por descendiente pero no transcritos fila a fila (duplicaban Blueprint); anotado como cobertura parcial a revisar en auditoría de requisitos.
- **Ronda 1 ampliada:** con REQUISITOS.md disponible, despachados #7 (packages/agents, REQ secc. 13/14/16/17/18/24/26) y #8 (packages/sources, REQ secc. 1/2/27/15/16/17), ámbitos disjuntos de #5 y #6. **Pendiente diferido:** verificación de fuentes legales (secc. 23–25, marcadas VERIFICAR) se hará como tarea de verificación con fuente DOF, no como investigación nueva; integraciones reales (OpenAI, ComprasMX) quedan PENDIENTES hasta credenciales/red verificada.

## Ampliación prioritaria back office (2026-09-05, ~18:5x)
- **Acción Fable:** copiada a docs/AMPLIACION-BACKOFFICE.md; decisión D-05/D-06 en docs/DECISIONES.md; estado de reanudación del bucle actualizado en docs/operacion-bucle.md. Despachado agente #9 (Sonnet) para extender REQUISITOS.md/ACEPTACION.md (secc. 29–33, REQ-141+) y crear docs/BACKLOG.md. Enviadas instrucciones delta a implementadores en curso #5 (web: Empresa/perfil, Fuentes y frescura, Expedientes, Aprobaciones), #6 (db: perfil empresa con procedencia por campo, tender_versions, source_runs, price_catalog aprobado, approvals con invalidación, package_manifests), #7 (agents: políticas duras nunca enviar/firmar/inventar; ausencia de dato = pendiente), #8 (sources: estados explícitos de fuente, versionado/cambios con TZ America/Mexico_City, verificación puntual del portal oficial vigente).
- **Siguiente:** al completar ronda 1, auditoría adversarial docs/auditoria-1/ y ronda 2 = módulos de dominio del ciclo ampliado (expediente, checklist, paquete, back office).
- **Ampliación → requisitos (#9 completado, commit ad1573f):** REQUISITOS.md ahora 171 REQ / 33 módulos (secc. 29–33 = REQ-141..171); ACEPTACION.md con 31 criterios nuevos + pruebas mínimas A1–A15; BACKLOG.md con épicas E0–E12 (E7 propone packages/expediente; E12 legal parcialmente bloqueado hasta verificación DOF). **Siguiente:** ronda 2 según BACKLOG al cerrar ronda 1 (#5–#8 en curso).

## Ronda 1 — apps/web completado (2026-09-05 19:0x)
- **REQ:** secc. 20–22 (a11y, móvil), 33 (estados honestos), paridad visual. **Acción:** agente #5. **Archivos/commit:** apps/web (89 archivos), `a43efd7`. **Prueba:** `npm run -w apps/web typecheck lint test build` → exit 0; Vitest 8 archivos / 29 pruebas OK; axe sin violaciones en AppShell y Login (docs/logs/web-ronda1.log). **Resultado:** shell, sidebar completo (incl. Empresa, Fuentes y frescura, Expediente, Aprobaciones, Paquete con badge Borrador), drawer móvil accesible, tema oscuro, EmptyState/ErrorState/LoadingState, login zod, api tipado. Sin backend aún. Notas técnicas: vitest 4 en web vs 2 en packages; Node 25 webstorage desactivado en tests. **Siguiente:** auditoría adversarial #11 (render real + capturas) y, tras apps/api, conectar datos reales.

## Ronda 1 — packages/agents completado (2026-09-05 19:1x)
- **REQ:** secc. 13/14/16/17/18/24/26 y REQ-164..171. **Acción:** agente #7. **Commit:** `c56e20f`. **Prueba:** typecheck/lint/build OK; Vitest 13 archivos / 127 pruebas OK (docs/logs/agents-ronda1.log). **Resultado:** ToolRegistry (rechaza esquemas con org_id), AuthorizationPolicy con prohibiciones duras (enviar/firmar/portales/terceros → denied para todos incl. superadmin), guardrail anticorrupción (regex+hooks, suite 240/100), NoFabricationPolicy, DependencyInvalidation, AgentRunner (idempotencia, rate limit, presupuesto, reintentos, AbortSignal, trazas sha256 + correlationId), ProviderRouter (tolerancia cero + 5 gates), OpenAIResponsesProvider real SIN ejercitar contra red (PENDIENTE credenciales). Incidente: borrado accidental de test/llm restaurado y reverificado antes del commit (a verificar por auditor). **Siguiente:** auditoría #12; persistencia real de stores en apps/api.

## Ronda 1 — packages/db + apps/api completado (2026-09-05 19:2x)
- **REQ:** secc. 10–18, 22 y esquema de 29–32. **Acción:** agente #6. **Commit:** `8052ff4` (59 archivos). **Prueba:** db 7 archivos / 90 tests OK; api 3 archivos / 12 tests OK; typecheck/lint OK (docs/logs/api-ronda1.log). **Resultado:** 16 migraciones idempotentes (tenancy, audit_log, idempotency_keys, jobs, rate_limits, agent_runs/tool_calls, dominio tenders→post_award, perfil de empresa + field_provenance, tender_versions/change_events, source_runs, approved_rates con trigger anti precio no aprobado, proposal_approvals, package_manifests con constraint ready⇒checklist); RLS en todas las tablas org_id; runtime con SET LOCAL ROLE app_role; API auth (jose+scrypt), orgs, health, idempotencia, rate limit, problem+json. **Limitaciones honestas:** PGlite una conexión (concurrencia de jobs solo invariante atómico); sin endpoints de ampliación aún; sin aceptar invitación ni revocar refresh. **Siguiente:** auditoría #13; ronda 2 API (#14) y worker (#15) despachados.

## Ronda 1 — packages/expediente completado (2026-09-05 19:3x)
- **REQ:** secc. 4–8 y REQ-156..171 (E6/E7/E8/E9). **Acción:** agente #10. **Commit:** `467be32` (30 archivos). **Prueba:** 11 archivos / 84 tests OK; typecheck/lint/build OK (docs/logs/expediente-ronda1.log). Pruebas A6–A15 cubiertas con tests nombrados (flujo integrado `expediente-flow.test.ts`, ZIP real releído; `api-surface.test.ts` prueba que no existe envío/firma). **Resultado:** matriz de requisitos con extractor regex + hook LLM (fake), conflictos escalados, CompanyDataService (missing/blocked, nunca inventado), propuesta técnica con source_ref, económica en centavos bigint + cantidad en letra, versiones con hash, checklist 7 dimensiones, aprobaciones con invalidación jerárquica, paquete ZIP draft/ready. **Pendientes honestos:** resolver real sobre packages/db, cliente LLM real, endpoints/descarga en apps/api, correlation_id desde API. **Siguiente:** auditoría #16; integración en apps/api ronda 3.

## Auditoría 1 — packages/agents (2026-09-05 19:3x)
- **Acción:** auditor #12 (contexto independiente, worktree). **Archivos:** docs/auditoria-1/agents.md, docs/logs/audit-agents-ronda1.log, commit `60588ec`. **Resultado:** 16 hallazgos: 2 CRÍTICOS (prohibiciones duras y tolerancia cero configurables/desactivables por constructor, contradice REQ-125/164–167), 4 ALTOS (bypass por alias/homoglifos, NoFabrication opt-in, BudgetLedger sin validar signo/NaN), 6 medios, 4 bajos. Comprobado correcto: idempotencia bajo carrera, liberación de presupuesto, AbortSignal, aislamiento por org, provider sin fuga de secretos, mutación 5/5, incidente test/llm sin anomalías. Cobertura real ~90% líneas / 84.5% ramas. **Siguiente:** corrector #17 despachado (invariantes de código, actionKind semántico, NoFabrication por defecto recursivo); después reverificación independiente.

## Auditoría 1 — apps/web (2026-09-05 19:4x)
- **Acción:** auditor #11 (render real Playwright, capturas en docs/auditoria-1/capturas/). **Archivos:** docs/auditoria-1/web.md, docs/logs/audit-web-ronda1.log, commit `0382ea6`. **Resultado:** 16 hallazgos: 2 críticos (W-01/W-02: typecheck/build no reproducibles en clon limpio por `@types/node` no declarado; la parte "paquetes no en git" quedó superada por commits posteriores, verificado con `git ls-files`), 5 altos (contraste axe serious en wordmark oscuro y badge warning; skip-link sin mover foco; login sin paridad con el login real de Restaurantes; sin suite Playwright pese a REQ-049/065), 6 medios, 3 bajos. Comprobado correcto: 29 tests en clon limpio, mutaciones 3/3 detectadas, sin datos ficticios ni secretos, paquete nunca "Listo", 5 estados de fuente, drawer y modo oscuro. **Siguiente:** corrector #18 despachado; luego reverificación independiente.

## Ronda 1 — packages/sources completado (2026-09-05 19:4x)
- **REQ:** secc. 1, 2, 15–17, 27, REQ-146..155. **Acción:** agente #8. **Commit:** `3dc9ce7`. **Prueba:** 64/64 tests; typecheck/lint/build OK (docs/logs/sources-ronda1.log). **Resultado:** TenderRecord, HttpClient (reintentos, Retry-After, concurrencia, tasa por host), 5 conectores, dedupe, DiscoveryPipeline idempotente con checkpoints, MatchingEngine determinista, SourceHealth con 6 estados explícitos, TenderVersion/ChangeDetectedEvent con TZ America/Mexico_City. **Verificación real por fuente (honesta):** ComprasMX = portal y API reales localizados pero 401/reCAPTCHA (bloqueo externo B-02); DOF parcial; OCDS-SHCP, PDN-S6, estatales PENDIENTES. Fixture real: CSV histórico SABG de datos.gob.mx. **Siguiente:** auditoría #19; persistencia de repositorios en apps/api/worker.

## Auditoría 1 — packages/expediente (2026-09-05 19:5x)
- **Acción:** auditor #16. **Archivos:** docs/auditoria-1/expediente.md, docs/logs/audit-expediente-ronda1.log, commit `fcc249e`. **Resultado:** 10 hallazgos: 4 CRÍTICOS reproducidos (invalidación de aprobación nunca se dispara automáticamente ante cambio de insumos → `ready` con total no aprobado; `ready` acepta aprobación de alcance documento; requisitos obligatorios sin evidencia desaparecen; veredicto de vencimiento depende del TZ del proceso), 2 altos (apócope "UN PESO"; extractor no reconoce DD/MM/AAAA), 2 medios, 2 bajos. Comprobado correcto: missing/blocked nunca fabricado, half-up determinista, jerarquía de invalidación, sin superficie de envío/firma; mutación 5/5; cobertura ~90%. **Siguiente:** corrector #20 despachado; luego reverificación independiente.

## Auditoría 1 — packages/db + apps/api (2026-09-05 19:5x)
- **Acción:** auditor #13 (worktree 8052ff4). **Archivos:** docs/auditoria-1/db-api.md, docs/logs/audit-dbapi-ronda1.log, commit `550cc6a`. **Resultado:** RLS adversarial dinámico: 34 tablas × 6 roles × operaciones = 622 ataques, 620 bloqueados, 2 fugas (DB-01: funciones SECURITY DEFINER `app.membership_role` y `app.find_user_by_email` invocables con parámetros arbitrarios → rol ajeno y hash de contraseña). 14 hallazgos: 6 ALTA (DB-01, DB-02 vigencia de tarifa no validada, DB-03 edición post-aprobación sin re-aprobación, DB-05 sin invalidación automática por change_events, API-01 refresh sin rotación, API-02 admin se autopromueve/degrada al único owner), 5 media, 4 baja, 0 crítica. Comprobado correcto: JWT 7/7 ataques bloqueados, migraciones idempotentes con checksum sin sintaxis PGlite, mutación 5/5, sin secretos, 0 vulns de producción, pendientes honestos. **Corrección:** asignada al agente #14 (único escritor de apps/api y migraciones ≥0017) con prioridad sobre módulos nuevos; evita escritores simultáneos.

## Ronda 2 — apps/worker completado (2026-09-05 20:0x)
- **REQ:** secc. 15–17, 19, 30–31 (E3/E4). **Acción:** agente #15. **Commit:** `069f690`. **Prueba:** 6 archivos / 36 tests sobre PGlite; typecheck/lint OK (docs/logs/worker-ronda2.log); cubre A1, A2, A4. **Resultado:** JobQueue (SKIP LOCKED, lease+heartbeat, backoff+jitter, dead letter, jobKey, cancelación, reloj inyectable), Worker con cierre ordenado y métricas, Scheduler sin duplicados por (tipo, fuente, ventana), handler discover_tenders con los 5 conectores reales → hoy todos `verified:false` ⇒ `not_configured` explícito (nunca "0 nuevas"), envío a `POST /internal/tenders/ingest` (alineado al código real de apps/api), run_agent esqueleto con FakeProvider. **Pendientes honestos (contrato con packages/db):** enum source_run_status más angosto que los 7 estados (proyección a failed + evidence.fineState), sin unique jobKey ni estado cancelled, worker sin rol propio (usa conexión de migraciones), OpenAI no ejercitado. **Siguiente:** auditoría #22; ajustes de esquema (migraciones ≥0017) a coordinar con el escritor de packages/db.

## Auditoría 1 — packages/sources (2026-09-05 20:0x)
- **Acción:** auditor #19 (worktree 3dc9ce7; repitió peticiones reales por fuente). **Archivos:** docs/auditoria-1/sources.md, docs/logs/audit-sources-ronda1.log, commit `1d23b5f`. **Resultado:** verificación del README confirmada de forma independiente (DOF 200 con tamaño exacto, timeout api.datos.gob.mx, PDN-S6 bot-detection, CSV histórico SABG 951 MB Last-Modified 2025-07-03), salvo ComprasMX: 403 con cuerpo distinto en lugar de 401 (ambos = protegido; B-02 sigue). 11 hallazgos: 3 ALTA (reordenar arrays dispara falsa versión; fechas ComprasMX dependen del TZ del proceso; DOF sin noteCodes reporta ok sin red), 5 media (redirecciones cross-host evaden throttle, sin test 401/403, CSV histórico no conectado al pipeline, colisión de fingerprint, MatchingEngine no separa relevancia/elegibilidad REQ-168), 3 baja. Mutación 4/5. Corrección de trazabilidad SR-10: REQ-146 (cadencia configurable) tiene evidencia en apps/worker Scheduler (commit 069f690), no en packages/sources. **Siguiente:** corrector #23 despachado; reverificación después.

## E12 — verificación legal puntual completada (2026-09-05 20:1x)
- **REQ:** REQ-100..123 (secc. 23–25). **Acción:** agente #21, solo fuentes oficiales. **Archivos:** docs/legal/verificacion-legal.md, filas 23–25 de REQUISITOS.md (contenido en commit `0698544`). **Resultado:** 6 VERIFICADO, 11 VERIFICADO-CON-MATIZ, 4 NO-VERIFICABLE-EN-LÍNEA, 3 NO-APLICA. Cambios normativos con fecha DOF registrados en DECISIONES D-07 (LAASSP nueva 16-abr-2025; pago 17 días hábiles; LOPSRM reformada; Plataforma Digital de Contrataciones Públicas / SABG; nueva LFPDPPP 20-mar-2025). Corregida cita REQ-102. **Pendiente:** 4 citas no verificables en línea quedan marcadas; validación por abogado requerida antes de afirmar cumplimiento a clientes. **Siguiente:** propagar nomenclatura legal vigente a packages/sources (README/etiquetas) y apps/web (etiquetas de fuente).

## Auditoría 1 — apps/worker (2026-09-05 20:2x)
- **Acción:** auditor #22 (worktree 069f690). **Archivos:** docs/auditoria-1/worker.md, docs/logs/audit-worker-ronda2.log, commit `edaae1b`. **Resultado:** 12 hallazgos: 4 ALTA reproducidos (max_attempts no se respeta tras crash → job `running` perpetuo con attempts=6; sin fencing token → doble ejecución no detectada; discover_tenders no escribe source_runs si falla la ingesta; dedupe del scheduler falla con dos procesos concurrentes 5/5), 4 media (coverage engañoso, enum source_run_status colapsa estados, conexión del worker bypassa RLS en todo el esquema y run_agent sin filtro de tenant), 4 baja. Comprobado correcto: SKIP LOCKED atómico, backoff acotado, dead letter conserva error, cierre ordenado incl. doble señal, cliente de ingesta, sin fuga de secretos, A4 honesto. **Siguiente:** corrector #24 despachado; cambios de esquema irán como propuestas SQL a asignar al escritor de packages/db (agente #14) al terminar su ronda.

## Corrección 1 — packages/agents (2026-09-05 20:2x)
- **Acción:** corrector #17. **Commits:** 14 (AG-03 `114a2fb`, AG-06 `f8ef82f`, AG-01 en `df9f6dc` por índice compartido, AG-02 `6d15186`, AG-08 `4731139`, AG-10 `5dff06e`, AG-04 `01550ad`, AG-05 `480d183`, AG-07 `f41d3ce`, AG-09 `f6a89f1`, AG-11 `b300521`, AG-12 `1acf087`, AG-13 `b5c8a38`, AG-14 `6e0be34`). **Prueba:** 13 archivos / 182 tests; typecheck/lint/build OK; cobertura 94.4% líneas / 91.3% ramas con gate (docs/logs/fix-agents-ronda1.log). **Resultado:** prohibiciones duras y tolerancia cero ahora invariantes de código (constructor solo añade); actionKind obligatorio; NFKC; NoFabrication recursivo por defecto; BudgetLedger/RateLimiter validados; GuardrailEvent redactado. Suite anticorrupción independiente: detección real ~3% → el guardrail regex es una capa mínima, se registra como límite (capa LLM/juez pendiente). **No corregidos:** AG-15 (anotar por paquete en ACEPTACION.md, docs), AG-16 (informativo). **Siguiente:** reverificación independiente #25.
- **Nota operativa (Fable):** el índice git es compartido; a partir de ahora los commits de Fable usan `git commit -- <rutas>` para no arrastrar cambios preparados por agentes.

## Corrección 1 — packages/expediente (2026-09-05 20:3x)
- **Acción:** corrector #20. **Commits:** 11 (EX-01 `5db06bc`, EX-02 `42b6f1a`, EX-03 `aa77898`, EX-04 `b57eb25`, EX-05 `e9a001c`, EX-06 `98b401a`, EX-07 `b29fd25`, EX-08 `c327594`, EX-09 `4357efb`, EX-10 `421317f`, cierre `49141fb`). Cada test verificado fallando contra el código previo. **Prueba:** 14 archivos / 118 tests; typecheck/lint/build OK; cobertura 91% líneas / 88% ramas (docs/logs/fix-expediente-ronda1.log). **Resultado:** invalidación automática por hash de insumos, `ready` exige aprobación de alcance expediente sobre la versión actual, obligatorios sin evidencia = PENDIENTE + bloqueo, TZ America/Mexico_City determinista (test bajo 3 TZ reales), apócope "UN PESO", fechas DD/MM/AAAA. **Incidente INC-02:** el commit `5db06bc` arrastró docs/REQUISITOS.md y docs/legal/verificacion-legal.md del agente legal (índice compartido); contenido preservado íntegro; a partir de ahí commits con pathspec. **Siguiente:** reverificación independiente #26.

## Reverificación 1 — packages/agents (2026-09-05 20:4x)
- **Acción:** reverificador #25 (worktree, 30 pruebas adversariales propias). **Archivos:** docs/auditoria-1/agents-reverificacion.md, docs/logs/reverify-agents-ronda1.log, commit `3c0820c`. **Resultado:** 11 CERRADO, 3 PARCIAL (AG-05 límite de diseño reconocido; AG-10/AG-11 huecos de recursividad), 0 NO CERRADO; 14 commits verificados; 182/182; gate de cobertura probado. **Nuevos:** AG-17 ALTA (`roleCeiling` permite subir techo de consultor_externo a irreversible, viola REQ-062), AG-18/19/20 MEDIA. **Regresión:** AG-05 (`declaredEffects` obligatorio) rompe apps/worker (`run-agent.ts`): typecheck/build y 3/36 tests → asignada al corrector del worker (#24) como WK-13. **Siguiente:** corrector #27 despachado; tras él, reverificación 2 de agents.

## Corrección 1 — apps/web (2026-09-05 20:5x)
- **Acción:** corrector #18. **Commits:** 12 (W-02 `a9e45c2`, W-05/06 `435ecbc`, W-07 `9b5f43a`, W-08/09 `0151404`, W-11 `3ab333c`, W-04 `2f85346`, W-12 `1adb386`, W-10 `533fbf3`, W-13 `ac6b632`, W-15 `345ca9f`, W-16 `7c37e23`, W-14 `508c37d`); cierre de columna/README en `3bc21f6`/`bb41c6c` (carrera de índice, contenido correcto). **Prueba:** 38/38 unitarias + 44/44 E2E Playwright+axe en Chromium real sobre las 24 rutas; clon limpio `git clone --no-local` → typecheck/lint/test/build/test:e2e exit 0 (docs/logs/fix-web-ronda1.log). **Resultado:** deps explícitas, contraste corregido, skip-link con foco, login alineado a la anatomía real de Restaurantes, targets ≥44px, AiDisclosureNote (REQ-115), estado de paquete derivado. **No corregidos:** W-01 (resuelto por commits de otros paquetes), W-03 (package-lock del monorepo compartido; se revisará al cerrar la ronda de todos los paquetes). **Siguiente:** reverificación independiente #30; conexión de datos reales a apps/api en ronda 3.

## Reverificación 1 — packages/expediente (2026-09-05 21:0x)
- **Acción:** reverificador #26. **Archivos:** docs/auditoria-1/expediente-reverificacion.md, docs/logs/reverify-expediente-ronda1.log, commit `0c308a8`. **Resultado:** reproducibilidad exacta (118 tests, 91%); 5 CERRADO, 3 PARCIAL (EX-01: el hash de insumos lo define el consumidor → cambiar documento de empresa o versión de bases sin incluirlo sigue dando ready; `inputChanged()` código muerto; EX-04: offset `+99:00` pasa validación y evalúa "nunca vencido" (fail-open); EX-06), 2 NO CERRADO (EX-03: condicionales que aplican siguen desapareciendo; EX-05: "VEINTIUNO" en 121, 221, 1121…). 6 nuevos EX-EXP-11..16 (3 ALTA). A6–A15 siguen PENDIENTE hasta cierre. **Siguiente:** corrector #31 despachado (segunda vuelta; tope Likida: 3 vueltas por hallazgo); después reverificación 2.

## Ronda 3 — CI, README raíz y runbook (2026-09-05 21:0x)
- **REQ:** secc. 19 y 28 (E0/E10). **Acción:** agente #29. **Commit:** `bb54a69`. **Archivos:** .github/workflows/quality.yml (matrix 7 workspaces; job `db-postgres` con Postgres 16 real: migraciones una a una + ataques RLS dinámicos vía API pública de @atiende/db con `pg`; `e2e-web` Playwright+axe; `npm-audit`; `check-secrets`), scripts/ci-local.sh, scripts/check-secrets.sh, scripts/db-postgres-*.mjs, README.md, docs/OPERACION.md. **Prueba real (docs/logs/ci-local.log):** 27/29 OK; **fallos honestos:** apps/api:test 2 timeouts de 5 s (tests de aislamiento/rol bajo carga concurrente de otros agentes; a reverificar en aislamiento tras cerrar ronda 2 de API) y apps/worker test:coverage ENOENT por carrera de filesystem (repetir en aislamiento). `npm audit` 0 vulnerabilidades. **Pendiente:** el job Postgres real solo puede verificarse en GitHub Actions (sin Docker local); no se marca cumplido hasta una ejecución real en CI (requiere remoto, no autorizado a publicar aún).

## Corrección 1 — packages/sources (2026-09-05 21:1x)
- **Acción:** corrector #23. **Commits:** 11 (SR-01 `b133a67`, SR-02 `3738f9a`, SR-03/05 `a90888c`/`34134f1`, SR-04 `f9b25ba`, SR-06 `fa68ad8`, SR-07 `67f6ed9`, SR-11 `e5dba74`, SR-08 `2bfdac8`, SR-09 `3bc21f6`, SR-10 `b9e4476`, cierre `dbfa671`). **Prueba:** 95/95 tests (13 archivos); typecheck/lint/build OK; cobertura 91.5% líneas / 82.3% ramas; consumidor apps/worker typecheck+test OK (docs/logs/fix-sources-ronda1.log). **Resultado:** hash canónico, fechas por fromMexicoCityNaive con test en 3 TZ, estado not_configured, redirecciones con throttle al host destino y https-only, conector CSV histórico SABG, fingerprint con fecha+número de procedimiento, eligibility separado de score (REQ-168), nomenclatura legal vigente. Trazabilidad SR-10: REQ-146 evidencia en apps/worker Scheduler. **Incidente INC-03:** resets de git por agentes; historial verificado y recuperado por Fable. **Siguiente:** reverificación independiente #32.

## Corrección 2 — packages/agents (2026-09-05 21:2x)
- **Acción:** corrector #27. **Commits:** `971feea` (AG-17 roleCeiling solo baja; incluye AG-19/AG-20 por pathspec de directorio, declarado en nota de transparencia), `861aea4` (AG-18 Sets por instancia congelados + Object.freeze(this)). Ambos ancestros de HEAD (verificado por Fable). **Prueba:** 215/215 tests (13 archivos); typecheck/lint/build OK; cobertura 95.1% líneas / 91.4% ramas; apps/api typecheck OK (docs/logs/fix-agents-ronda2.log). 21 tests nuevos verificados fallando antes del arreglo. **Resultado:** cierra AG-17..20 y los PARCIAL de AG-10/AG-11. **Siguiente:** reverificación 2 (#33); es la tercera vuelta del paquete → si quedan abiertos tras ella, se registran como límite con causa e intentos (regla Likida).

## Corrección 1 — apps/worker (2026-09-05 21:3x)
- **Acción:** corrector #24. **Commits:** WK-13 `3bc21f6` (mixto por carrera de índice), WK-01/02/04/10/12 `8f03444`+`e2ac765`, WK-03/05/06 `d7ac83a`, WK-09 `a1437cc`, WK-08 `a545298`, WK-11 `1b656a3`, docs `21aadef` `cc30b4c` `56ce542` `852615a`; ancestría verificada por el agente tras cada commit. **Prueba:** 55/55 activos + 5 skipped (pendientes de esquema); typecheck/lint OK; cobertura 92.8% líneas / 80.5% ramas (docs/logs/fix-worker-ronda2.log). **Resultado:** max_attempts en recuperación de lease, fencing, dedupe con advisory lock, errores permanentes → dead, source_runs siempre (ingest_failed), coverage honesto, Idempotency-Key, org_id en run_agent. **PENDIENTE esquema:** 3 propuestas SQL en apps/worker/db-proposals (enum source_run_status, unique (kind, job_key) + cancelled, worker_role) → asignadas al agente #14 (único escritor de packages/db). **Incidente:** commit `5a3073b` huérfano por carrera de refs; re-commiteado sin pérdida (INC-03 ampliado). **Siguiente:** reverificación #34.

## Reverificación 1 — apps/web (2026-09-05 21:4x)
- **Acción:** reverificador #30. **Archivos:** docs/auditoria-1/web-reverificacion.md, capturas-reverificacion/, docs/logs/reverify-web-ronda1.log, commit `38d2e4b`. **Resultado:** 14/16 CERRADO; W-04 PARCIAL (vitest-axe resuelve peer a vitest@2 compartido, sin impacto runtime, doc incompleta); W-03 bloqueado por lockfile compartido. Clon limpio real 44/44 E2E. Mutación W-14 confirma que la E2E detecta quitar el aviso de firma/presentación y forzar "Listo". Paridad W-11 confirmada con render real de Restaurantes (Fraunces, layout, kicker). **Nuevos:** W-17 ALTA (badge destructive 4.31:1 en oscuro), W-18 ALTA (foco del skip-link invisible), W-19/W-20 BAJA. **Siguiente:** corrector #35 (vuelta 2), luego reverificación 2.

## Tablero de aceptación (2026-09-05 21:5x)
- **Acción:** agente #28. **Archivos:** docs/ACEPTACION.md (columnas paquete + evidencia, escala estricta), docs/TABLERO.md, commit `d8e6d29`. **Estado real:** 186 criterios → 92 PENDIENTE, 82 EN_EVIDENCIA, 7 CUMPLIDO, 4 NO_APLICA, 1 BLOQUEADO_EXTERNO; ~939 tests verdes en 7 workspaces. **Brechas estructurales señaladas:** apps/web sin conexión a apps/api (ronda 3); packages/db+apps/api sin reverificación independiente (se hará al cerrar #14); W-03 package-lock raíz no commiteado → **corregido por Fable** (commit del lockfile); B-02/B-03 externos. **Regla del tablero:** ningún criterio pasa a CUMPLIDO sin reverificación CERRADO; mocks no cuentan.

## Reverificación 2 — packages/agents (2026-09-05 22:0x)
- **Acción:** reverificador #33 (30 ataques propios). **Archivos:** docs/auditoria-1/agents-reverificacion-2.md, docs/logs/reverify2-agents.log, commits `587d4a2` (arrastró 5 archivos ya coherentes de la corrección concurrente de expediente; sin daño, documentado) y `d50d8ed`. **Resultado:** 215/215; cobertura 95%; gate exit 0; regresión declaredEffects del worker CERRADA; apps/api ya importa @atiende/agents con typecheck OK. AG-17 y AG-18 CERRADO; AG-19 y AG-20 PARCIAL con residuos acotados (arrays de nivel superior, Buffer truncado a 8192, base64-JSON; ZodPipeline/Branded, keyType, sin guarda de profundidad → RangeError). Nuevos AG-21/AG-22 MEDIA. Balance del paquete: 14 cerrados, 4 abiertos (ninguno ALTA/CRÍTICA), AG-05 límite arquitectónico, AG-12 alcance acotado. **Siguiente:** corrector #36 (vuelta 3, última). Si tras la reverificación 3 quedan residuos ≤ MEDIA, se registran como límites aceptados con causa e intentos (regla de 3 vueltas) y el paquete se considera cerrado para aceptación con esa nota.

## Reverificación 1 — packages/sources (2026-09-05 22:1x)
- **Acción:** reverificador #32. **Archivos:** docs/auditoria-1/sources-reverificacion.md, docs/logs/reverify-sources-ronda1.log, commit `54ccf09`. **Resultado:** 95/95 reproducido; gate de cobertura y regla SR-05 fallan correctamente al mutar. 7 CERRADO, 3 PARCIAL: SR-02 (el conector CSV histórico nuevo reintrodujo el bug de TZ), SR-03 (HTTP 200 con HTML de captcha sigue dando `ok`, antipatrón REQ-148), SR-06 (CSV real de 951 MB sin robustez: encoding latin1, fila malformada pierde el lote, comas sin escapar). **Nuevos:** SR-14 ALTA (200+captcha → ok silencioso), SR-15 ALTA (mojibake latin1), SR-13/SR-16 MEDIA-ALTA, SR-12/SR-17 MEDIA, SR-18 BAJA. Worker sin regresión. **Siguiente:** corrector #37 (vuelta 2) con clasificador de respuesta común e invariante de TZ para todos los conectores.

## Corrección 2 — packages/expediente (2026-09-05 22:2x)
- **Acción:** corrector #31. **Commits:** EX-01/11 `f5991f5` (computeInputsHash cerrado en 5 categorías, stableStringify, inputChanged conectado), EX-03/12 `1dc9026` (condicional fail-closed, test de propiedad 200 casos), EX-05/12 `284db62` (apócope en "veintiuno" fusionado), EX-04/13 `5dfae17`+`9845ecf` (offsets -12:00..+14:00, calendario, isPast fail-closed), EX-02/14 `fdf17e4` (scopeRef exigido), EX-06/15 arrastrado en `587d4a2` (contenido verificado), EX-16 `b19ced8`, log `b5a8846`. **Prueba:** 16 archivos / 364 tests; typecheck/lint/build OK; cobertura 93.2% líneas / 90.1% ramas; proposal-version.ts 95.7% (docs/logs/fix-expediente-ronda2.log). **Siguiente:** reverificación 2 (#38); tercera vuelta si hace falta, luego cierre con límites documentados.

## Corrección 2 — apps/web (2026-09-05 22:3x)
- **Acción:** corrector #35. **Commits:** W-17 `946914a` (destructive oscuro 4.59:1 + test de contraste WCAG + E2E axe oscuro), W-18 `ad26360` (anillo de foco en main/login, E2E 25 pantallas), W-19 `81e64b1` (min-h-11 acordeón, E2E 390x844), W-04/W-20 `c043f7a` (doc vitest-axe; sin overrides raíz por afectar otros workspaces). **Prueba:** 49/49 unit, 71/71 E2E (44 + 27 nuevas), typecheck/lint/build OK (docs/logs/fix-web-ronda2.log); cada hallazgo reproducido en rojo en worktree antes del arreglo. **Siguiente:** reverificación 2 (#39). W-03: lockfile raíz ya commiteado por Fable (`d739f13`) → a cerrar en reverificación.

## Reverificación 1 — apps/worker (2026-09-05 22:4x)
- **Acción:** reverificador #34. **Archivos:** docs/auditoria-1/worker-reverificacion.md, docs/logs/reverify-worker.log, commit `401f2c3`. **Resultado:** 7 CERRADO, 4 PARCIAL, WK-07 PENDIENTE_ESQUEMA. **Nuevos:** WK-14 ALTA (complete/fail/dead no verifican el token de lease → un zombie de generación anterior puede finalizar un job ajeno si se reutiliza WORKER_ID), WK-16 ALTA (`organizationId: null` desactiva el guard de tenant en run_agent), WK-15 MEDIA (PGlite serializa transacciones: la "concurrencia" del scheduler no está probada realmente; SQL correcto para Postgres), WK-17 MEDIA (408 clasificado permanente), WK-18 BAJA. Propuestas SQL correctas; PROPOSAL-02 con 2 riesgos de despliegue (duplicados preexistentes, índice sin CONCURRENTLY) → comunicados al escritor de packages/db (#14). Sin regresión cruzada con sources. **Siguiente:** corrector #40 (vuelta 2 del worker); prueba real de concurrencia queda ligada a B-03 (CI Postgres).

## Ronda 2 — apps/api + packages/db completado (2026-09-05 23:0x)
- **REQ:** secc. 1–3, 8, 10–12, 18, 29–31, 33 (E2/E3/E4/E5/E10) + correcciones DB-01..07/API-01..07 + migraciones del worker. **Acción:** agente #14. **Commits:** ~20 (`0ef79f4`…`9bf7a93`; DB-01 `c1c0f1c`, DB-02 `62a77e0`, DB-03 `7922171`, DB-04 `8a17818`, DB-05 `0698544`, API-01 `ff4ec6d`, API-02 `0ab4dfd`, matching `cb2bc3f`, agents `eaee816`, admin `b00efeb`, worker proposals `2a7b809`, 0026b `da36f65`). **Prueba:** apps/api 51/51, packages/db 128/128, typecheck/lint OK (docs/logs/api-ronda2.log); cubre A1, A2, A3, A5, A7, A8. **Resultado:** invitaciones, rotación de refresh, último owner, helmet/CORS; Empresa completa con field_provenance y approved_rates con enforcement en DB; ingest idempotente con versiones/eventos/invalidación por trigger; matching relevancia+elegibilidad con no_evaluable; PgRunStore/PgToolCallStore; /admin superadmin; migraciones 0017–0028 (+0026b) incl. enum source_run_status ampliado, índice único jobs + cancelled, worker_role. **Limitaciones honestas:** sin 2FA en aprobaciones económicas; AgentRunner sin LLM real; roles de agents vs OrgRole con cast; DB-07 parcial; CONCURRENTLY no soportado por el runner; sin lease_token en jobs (el worker usa locked_by compuesto). **Siguiente:** reverificación independiente #41; ronda 3 despachada: #42 (web↔api real) y #43 (api E6–E9/E11).

## Corrección 3 — packages/agents (2026-09-05 23:1x)
- **Acción:** corrector #36. **Commit:** `9cb38be` (AG-19/21: arrays de nivel superior, Buffer completo por ventanas con solape y límite 5 MiB → no_evaluable, heurística base64-JSON; AG-20/22: ZodPipeline/Branded, keyType de record/map, guarda de profundidad 256 con error controlado, verificación runtime de claves prohibidas en validateInput). **Prueba:** 241/241 tests (26 nuevos); cobertura 94.1% líneas / 90.8% ramas; apps/api y apps/worker typecheck OK (docs/logs/fix-agents-ronda3.log). **Siguiente:** reverificación 3 final (#44) → cierre del paquete con límites aceptados documentados.

## Reverificación 2 — packages/expediente (2026-09-05 23:2x)
- **Acción:** reverificador #38. **Archivos:** docs/auditoria-1/expediente-reverificacion-2.md, docs/logs/reverify2-expediente.log, commit `8d67af9`. **Resultado:** 364/364; cobertura 93.2/90.2; 14 CERRADO, 2 PARCIAL (EX-01/11: `approve()`/`buildManifest()` aceptan `inputsHash` string sin ligarlo a `computeInputsHash` → hash artesanal produce ready; EX-EXP-17 ALTA). Nuevos menores: EX-18 (Date → "{}" en stableStringify), EX-19 (condicional no aplica desaparece), EX-20 (24:00 rueda al día siguiente), EX-21 (sin gate de cobertura), EX-22 (ACEPTACION desfasado → lo actualiza Fable al cierre). Familias extendidas (apócope, offsets, scopeRef) limpias. **Siguiente:** corrector #45 (vuelta 3, última; tipo branded + verificación runtime), coordinado con el implementador de apps/api ronda 3 que integra el paquete. Tras la reverificación 3 el paquete se cierra con límites documentados.

## Reverificación 3 (cierre) — packages/agents (2026-09-05 23:3x)
- **Acción:** reverificador #44. **Archivos:** docs/auditoria-1/agents-cierre.md, docs/logs/reverify3-agents.log, commit `f3e6a3b`. **Resultado:** 241/241; cobertura 94.1/90.8; consumidores OK. AG-19, AG-20, AG-21 CERRADO; AG-22 PARCIAL por residuo nuevo AG-23 MEDIA (`FORBIDDEN_INPUT_FIELDS` sin normalización: `ORG_ID`, `org-id` eluden). **Balance del paquete:** 19/22 cerrados, 2 PARCIAL con causa (AG-05 arquitectónico; AG-22→AG-23), 0 NO CERRADO. **Límites aceptados documentados:** AG-05, AG-12 (regex ~3%, capa LLM pendiente en apps/api), compresión/cifrado en no-fabricación, z.record clave genérica (mitigado en runtime), URL-safe y doble base64 no decodificados. **ACEPTACION (al cierre):** REQ-062 → CUMPLIDO; REQ-165 EN_EVIDENCIA con nota AG-05. **Siguiente:** micro-corrección AG-23 (#46, un commit) y verificación puntual; después el paquete queda cerrado.

## Reverificación 2 — apps/web (2026-09-05 23:4x)
- **Acción:** reverificador #39. **Archivos:** docs/auditoria-1/web-reverificacion-2.md, capturas-reverificacion-2/, docs/logs/reverify2-web.log, commit `eeeeaac`. **Resultado:** W-01..W-20 CERRADOS (20/20): axe 0 violaciones en 48 combinaciones ruta×tema; skip-link con teclado real en 96 combinaciones; targets ≥44px a 320 y 390; W-03 lockfile confirmado con `npm ci` en clon; 49 unit + 71 E2E en clon `--no-local`. **Nuevos:** W-21 ALTA (ThemeSelector invisible en móvil <466px por switcher de ancho fijo; enmascarado por overflow clip), W-22 BAJA (switcher y cerrar menú <44px), W-23 infra (colisión de puerto 4173 entre worktrees). **Corrección:** asignada al implementador #42 (único escritor de apps/web, ya rehaciendo el header con datos reales).

## Corrección 2 — apps/worker (2026-09-05 23:5x)
- **Acción:** corrector #40. **Commits:** WK-14 `ab5fa95` (lease token UUID en locked_by exigido en heartbeat/complete/fail/dead → StaleLeaseError), WK-16 `95f3a03` (org requerida fail-closed en run_agent), WK-17 `e9879f3`+`521b321` (408/425 transitorios, tabla de verdad contra servidor real), WK-15 `b6b879f` (afirmación honesta; concurrencia real PENDIENTE ref. B-03), PROPOSAL-02 `7ab874c` (paso 0 duplicados; CONCURRENTLY no viable con el runner), WK-18 `d88d334`. **Prueba:** 79 tests + 6 skip; typecheck/lint OK; cobertura 90.4/82.2 (docs/logs/fix-worker-ronda3.log). **Siguiente:** reverificación de cierre (#47); posible activación de los `.pending.test.ts` ahora que 0026–0028 están aplicadas.

## Corrección 2 — packages/sources (2026-09-06 00:0x)
- **Acción:** corrector #37. **Commits:** SR-14 `0a0b9df` (ResponseClassifier común: 200+HTML de captcha/challenge → captcha_detected/interface_changed), SR-12/15/16/17 `bf6f5c8` (CSV RFC 4180, detección de encoding BOM/UTF-8/latin1, filas malformadas a errors[] sin perder lote, TZ en conector histórico), SR-13 `5c72192` (null⇄ausente), SR-18 `bef652e` (clave de anexo NFD+casefold; URL/hash sí versionan), invariante TZ `d8569d4` (subproceso ×6 conectores + grep estático), docs `81c8aa6`. **Prueba:** 18 archivos / 145 tests; cobertura 91.5/84.5 con gate; build OK (docs/logs/fix-sources-ronda2.log). El typecheck del worker reportado como fallido era transitorio por trabajo concurrente; Fable lo verificó en verde en HEAD. **Siguiente:** reverificación de cierre (#48).

## Micro-corrección AG-23 — packages/agents (2026-09-06 00:1x)
- **Acción:** corrector #46. **Contenido:** `isForbiddenFieldKey()` con normalización NFKC + minúsculas + alfanumérico, igualdad exacta contra {organizationid, tenantid, orgid} y prefijo/sufijo para orgid/tenantid; 22 tests nuevos (14/68 fallaban antes del arreglo en worktree). **Prueba:** 263/263; cobertura 94.1/90.9; apps/worker typecheck OK; apps/api typecheck falla por adaptación en curso a `InputsHash` branded (esperado, asignado a #43). **Incidente INC-04:** el contenido quedó dentro del commit `048ae47` de otro agente por `git add -A`; verificado íntegro. **Estado del paquete agents:** 22/23 hallazgos cerrados o con límite aceptado; AG-23 CERRADO por su corrector, pendiente de confirmación independiente en el cierre global.

## Reverificación 1 — packages/db + apps/api (2026-09-06 00:2x)
- **Acción:** reverificador #41 (3 descendientes Sonnet en worktrees). **Archivos:** docs/auditoria-1/db-api-reverificacion.md, docs/logs/reverify-dbapi.log, commit `b92570a`. **Resultado:** 10/14 CONFIRMADO_CERRADO; reabiertos DB-02 (vigencia de tarifa contra hoy, no contra fecha de presentación) y API-02 (admin puede invitar como owner vía POST invitations); PARCIAL API-01 (TOCTOU), API-03 (oráculo de timing 24×), DB-07. **CRÍTICA nueva DB-08:** funciones SECURITY DEFINER de refresh_tokens (0017) aceptan user_id externo → cualquier usuario autenticado puede acuñar refresh token de otro (account takeover) o revocar sus sesiones; mismo defecto que DB-01 no revisado en su cierre. Nuevos: API-08 ALTA, DB-09/10/11, API-09..12. RLS: 176 ataques, 173 bloqueados (3 fugas = DB-08/09); `app.apply_org_rls` 167/167 sin fugas incl. tablas 0011–0028 y worker_role. Migraciones CUMPLE. Regresión: db 130/130, api 51/51 ×3 corridas, worker 55/55, web typecheck OK; los 2 timeouts de ci-local no reproducen en aislamiento. Trazabilidad: A3/A7/A8 con más evidencia de la reconocida (a actualizar en ACEPTACION al cierre). **Siguiente:** corrector de seguridad #49 (prioridad máxima, rango de migraciones 0040–0049 reservado; coordinado con api ronda 3).

## Backlog refrescado (2026-09-06 00:3x)
- **Acción:** agente #50, commit `976b6bd`. **Estado por épica:** E1 en curso (DB-08/API-08 abiertos → #49); E2–E5 implementado-en-evidencia (brechas: REQ-145 sin test, panel de frescura no conectado, REQ-061 pgvector sin implementar); E6–E8 en curso (#43, #45); E9 bloqueada por DB-08; E10 en curso (#42 conecta back office); E11 pendiente de lógica (en #43); E12 verificado con matices (validación por abogado externa). Sección "Orden de cierre restante" añadida.

## Corrección 3 — packages/expediente (2026-09-06 00:4x)
- **Acción:** corrector #45. **Commits:** `14a35bb` (EX-17: `InputsHash` branded + `HashedInputs` sellado con símbolo privado, exigido por approve/revalidate/buildManifest con `requireValidHashedInputs`; EX-18 stableStringify Date/Map/Set/BigInt; EX-19 secciones "NO APLICA" visibles + `notApplicableRequirements` en manifiesto; EX-20 validación de hora), `b5acc45` (EX-21 gate de cobertura 85/80), `5e03e90` (EX-22 anotado → ACEPTACION lo actualiza Fable). **Prueba:** 391/391 (16 archivos, ~35 nuevos); cobertura 93.0/90.6 gate OK; build/typecheck/lint OK (docs/logs/fix-expediente-ronda3.log). apps/api typecheck falla por 19 errores de WIP de la ronda 3 de api (no por expediente; api ya consume sealInputs/HashedInputs). **Siguiente:** reverificación de cierre (#51); tras ella el paquete se cierra con límites documentados.

## Reverificación 2 (cierre) — apps/worker (2026-09-06 00:5x)
- **Acción:** reverificador #47. **Archivos:** docs/auditoria-1/worker-cierre.md, docs/logs/reverify2-worker.log, commit `7621fd2`. **Resultado:** 79 tests reproducidos; 18 ataques: fencing WK-14 resiste 4/4 (zombie, mismo workerId, requeue, `::` en id; sin fuga de token en logs); 408/425 y errores de red correctos; índice único 0027 real (23505). 16 CERRADO, 2 PARCIAL (WK-04 concurrencia real → B-03; WK-08). **Nuevos ALTA:** WK-22 (el código del worker no adoptó el esquema ya migrado: sigue proyectando estados finos a 'failed' y cancel escribe 'dead'; los `.pending.test.ts` con aserciones reales FALLAN → no activar a ciegas), WK-23 (con `worker_role`, `updateAgentRunRow` no fija current_user_id → RLS bloquearía updates legítimos y el código lo leería como cross-tenant). Menores WK-19/20/21. REQ-146/150 candidatos a CUMPLIDO; REQ-147–149 solo parcialmente hasta WK-22. **Siguiente:** corrector #52 (vuelta 3, última) → reverificación puntual en el cierre global.

## Reverificación 2 (cierre) — packages/sources (2026-09-06 01:0x)
- **Acción:** reverificador #48. **Archivos:** docs/auditoria-1/sources-cierre.md, docs/logs/reverify2-sources.log, commit `29a120d`. **Resultado:** 145 tests reproducidos; gate real; invariante TZ detecta inyección de `new Date(...)`. CERRADO SR-12/16/17/18 (+ ronda 1 sin regresión); PARCIAL SR-13/14/15. **Nuevos:** SR-19 ALTA (`{"error":"captcha"}` o JSON sin clave de datos pasa por `.default([])` → ok: antipatrón REQ-148 reproducido), SR-20 MEDIA-ALTA (conectores de texto sin chequeo de challenge/login), SR-21 ALTA (registro con title null se descarta en silencio sin errors[]), SR-22 MEDIA (UTF-16LE). Medición: CSV histórico ~951 MB → ~44 GB heap extrapolado → streaming obligatorio. REQ-148 NO candidato a CUMPLIDO; REQ-151 sí. Sin regresión en worker/api. **Siguiente:** corrector #53 (vuelta 3, última) → verificación puntual en cierre global.

## Reverificación 3 (cierre) — packages/expediente (2026-09-06 01:1x)
- **Acción:** reverificador #51. **Archivos:** docs/auditoria-1/expediente-cierre.md, docs/logs/reverify3-expediente.log, commit `92e577f`. **Resultado:** 391/391; gate de cobertura real; 11 vectores contra el sellado (Object.create, copia de símbolo, structuredClone, Proxy, mutación profunda, JSON, Symbol vs Symbol.for) sin bypass; EX-18/19/20 confirmados. **Balance:** 20/22 CERRADO, EX-08/EX-10 límites de diseño documentados, 1 nuevo BAJA (lectura del marcador por cadena de prototipos; no explotable hoy), residual -0/NaN BAJA. 0 críticos/altos abiertos. Nota de integración para apps/api: `conditionEvaluations` no forma parte del hash de insumos → cambio de aplicabilidad no invalida aprobación por hash (responsabilidad de la API; pasar a #43). apps/api typecheck limpio con la integración real de expediente (`8fdeb68`, `72cc525`, `c4a6165`). A6–A15 siguen PENDIENTE hasta E2E integrado en API. **Siguiente:** micro-corrección #54 (dos BAJA) y cierre del paquete.

## Corrección de seguridad — packages/db + apps/api (2026-09-06 01:2x)
- **Acción:** corrector #49. **Commits:** DB-08 `2b6b2f7` (create/revoke refresh exigen current_user_id o superadmin; REVOKE EXECUTE PUBLIC; test que enumera todas las SECURITY DEFINER de pg_proc contra lista blanca justificada), DB-12 `38b157e` (nuevo: `app.my_organizations(p_user_id)` mismo patrón → sin parámetro), API-08/02 `7eb4ce7` (solo owner invita como owner), DB-02/10 `3f25760` (vigencia de tarifa contra submission_deadline), API-01/09 `31780e9` (rotación atómica + revocación de familia por reutilización; bug real de RAISE que deshacía la revocación corregido), API-03 `3fd1093` (scrypt siempre; ratio 1.03–1.14× vs 24×), DB-09 `21b6a8c` parcial, DB-11 `616f15e`/`13c044f` docs, `a8d5442`. **Prueba:** db 19 archivos / 144 tests; api 24 archivos / 79 tests; typecheck/lint OK (docs/logs/fix-dbapi-seguridad.log). **Fuera de ámbito → #43:** API-09 (tool_calls), API-10, API-11, API-12, DB-09 residual. **Siguiente:** reverificación de seguridad #55.

## Micro-corrección — packages/expediente (2026-09-06 01:3x)
- **Acción:** corrector #54. Sellado: `Object.hasOwn` + `WeakSet` por identidad de instancia (bypass por prototipo cerrado, 5 tests fallaban antes en worktree); `stableStringify`: NaN/±Infinity → error fail-closed, `-0` con marcador. **Prueba:** 398/398; cobertura 93.1%; apps/api typecheck OK (docs/logs/fix-expediente-micro.log). **Estado del paquete expediente:** 22/22 hallazgos cerrados o con límite documentado (EX-08/EX-10); pendiente solo confirmación puntual en el cierre global y E2E integrado en API (A6–A15).

## Incidente INC-05 y corrección 3 — apps/worker (2026-09-06 00:4x–01:0x)
- **INC-05:** límite de sesión 429 cortó 5 agentes (#42, #43, #52, #53, #55) a las 00:1x; reanudados por SendMessage a las 00:43 con contexto intacto; cron a1c165a2 dio 2 latidos durante la caída. Sin compra de créditos ni cambio de límites.
- **Worker vuelta 3 (#52):** WK-22 `4f7a933` (estados finos 1:1 con enum 0026; cancel → 'cancelled'; los 3 pending tests activados con aserciones reales y en verde), WK-19/23 `7cdafc5` (UUID estricto; `updateAgentRunRow` con `SET LOCAL ROLE worker_role` y `app.current_user_id` = actor real según las políticas de 0028; RLS real probada: legítimo OK, cross-tenant y sin actor bloqueados), WK-20/21 `a3e42a3` (tabla 400–599 exhaustiva; 407 de undici → error permanente de proxy), docs `f06f9df`. **Prueba:** 298/298; cobertura 91.2/82.6; packages/db 144/144 sin regresión (docs/logs/fix-worker-ronda4.log). **Estado del paquete worker:** 23 hallazgos cerrados o acotados; concurrencia real de motor ligada a B-03. Confirmación puntual en cierre global.

## Ronda 3 — apps/api E6–E11 completado (2026-09-06 01:1x)
- **REQ:** secc. 4–9, 32–33 (E6/E7/E8/E9/E11) + API-09..12/DB-09. **Acción:** agente #43 (reanudado tras INC-05). **Commits:** ~20 con pathspec (incidente 048ae47 previo documentado). **Migraciones:** 0029–0035 (matriz, checklist, aprobaciones+eventos, paquete, post-adjudicación, audit_log.org_id nullable). **Endpoints:** 24 rutas `/expediente/tenders/:tenderId/…` (documents con extracción PDF/texto, matrix+conflicts, proposal technical/economic, checklist/run, approval, package assemble/download, submission/declare, post-award). **Prueba:** apps/api 29 archivos / 95 tests; packages/db 19 / 144; typecheck/lint OK (docs/logs/api-ronda3.log); tests nombrados para A8, A11, A12, A13, A14, A15 y E2E completo; seguridad API-09 (atómico), API-10, API-11 (magic bytes), API-12 (timingSafeEqual), DB-09 mitigado con caché (cierre total requiere cambio de interfaz en packages/agents). **Pendientes honestos:** sin OCR (requires_ocr explícito), sin firma real (declaración del usuario), sin envío a portal (verificado sin cliente HTTP saliente), extracción sin páginas reales, files/formatLimits declarados por el llamador, calendario de inhábiles solo sáb/dom, EX-EXP-08. **Siguiente:** auditoría adversarial #57 (docs/auditoria-2/); tras web↔api (#42), ci-local en aislamiento.

## Verificación puntual final — worker / agents / expediente (2026-09-06 01:2x)
- **Acción:** verificador #56, commit `1270ea6`. **Resultado:** worker WK-19..23 CERRADO (298/298; 13 ataques, 0 bypass; estados finos persistidos; cancelled no reclamable; worker_role bloquea cross-tenant/actor inexistente/sin membresía); agents AG-23 CERRADO (263/263; 14 ataques incl. fullwidth y zero-width); expediente micro CERRADO (398/398; 12 ataques al sellado incl. copia del símbolo privado vía Reflect.ownKeys → WeakSet por identidad no falsificable). Sin hallazgos nuevos. **Paquetes cerrados con reverificación independiente:** packages/agents (límites AG-05/AG-12 documentados), packages/expediente (EX-08/EX-10), apps/worker (concurrencia real → B-03). **Para ACEPTACION al cierre:** REQ-147/148/149 candidatos (estados finos), REQ-165 nota AG-05, REQ-159/161/162/163 sin la salvedad de falsificación; REQ-148 sigue condicionado a sources vuelta 3 (SR-19/20/21).

## Corrección 3 — packages/sources (2026-09-06 01:3x)
- **Acción:** corrector #53 (reanudado tras INC-05). **Commits:** SR-19/20 `0e9dc4f` (sin `.default([])` en colecciones requeridas → interface_changed; marcadores Akamai/Imperva/Turnstile/login; `minimalContentMarkers` para texto), SR-21 `15bb31f` (`{records, dropped}`, `reportDropped`, `dropRateThreshold` 20% → interface_changed, `coverage.emptyResult`), SR-22 `877b60f` (UTF-16LE/BE; CSV en streaming con memoria acotada: heap <5× un lote de 2 MiB vs 46× antes), docs `2dff88f`. **Prueba:** 186 tests; cobertura 89.4/84.2 con gate; worker 298 OK; api typecheck OK (docs/logs/fix-sources-ronda3.log). **INC-06:** amend prohibido reemplazó un commit ajeno; contenido verificado íntegro por Fable. **Siguiente:** reverificación de cierre final #58.

## Reverificación de seguridad — packages/db + apps/api (2026-09-06 01:4x)
- **Acción:** reverificador #55 (reanudado tras INC-05). **Archivos:** docs/auditoria-1/db-api-seguridad-reverificacion.md, docs/logs/reverify-dbapi-seguridad.log, commit `8edacdb`. **Resultado:** 10 commits verificados; db 144/144, api 95/95; 22 ataques nuevos: GUC current_user_id (apps/api único guardián, confirmado por grep exhaustivo; defensa en profundidad pendiente = DB-07), SET ROLE (identidad de conexión física), superadmin revocando sesiones ajenas (por diseño, sin audit_log, inalcanzable vía HTTP hoy), 15/15 SECURITY DEFINER de 0029–0039 en lista blanca, DB-02 ambas direcciones + NULL + TZ, API-01 3 refresh concurrentes + token robado tras logout, API-08 variantes, API-03 ratio 1.07× (100 muestras). CERRADOS: DB-08, DB-12, API-08/02, DB-02/10, API-01, API-03, API-09, API-10, API-11, API-12. PARCIAL acotados: DB-09 (mitigación reduce riesgo práctico), DB-07 (alcance documentado). Colateral: migración 0034 estuvo sin commitear un lapso (autocorregido en `5bcaa41`). **Nuevos medios:** DB-13 (vigencia de tarifas evaluada en el TimeZone por defecto de Postgres, no fijado), API-13 (eventos de autenticación no llegan a audit_log). **Siguiente:** DB-13/API-13 se corrigen junto con los hallazgos de la auditoría 2 de expediente en API (#57) para no duplicar escritores en apps/api.

## Reverificación 3 (cierre) — packages/sources (2026-09-06 01:5x)
- **Acción:** reverificador #58. **Archivos:** docs/auditoria-1/sources-cierre-final.md, docs/logs/reverify3-sources.log, commit `784f7c4`. **Resultado:** 186/186; gate real; INC-06 reproducido con hash de árbol idéntico: `15bb31f` contiene en realidad el commit DB-09 y el diff de SR-21 vive en `d8a3da7` (mensaje "TEMP-CHECK-SR21"); sin pérdida. SR-19 y SR-22 CERRADO (streaming con campos partidos entre chunks OK). **Nuevos:** SR-24 ALTA (`reportDropped` solo conectado en ComprasMX: OCDS-SHCP/PDN/estatales siguen descartando en silencio), SR-23 MEDIA (marcadores de plantilla DOF con falso positivo y falso negativo ante interstitial JS). REQ-148 sigue EN_EVIDENCIA. Sin regresión. **Siguiente:** micro-corrección #59 (capa común de descartes + invariante; validación semántica), verificación puntual en cierre global.

## Auditoría 2 — apps/api expediente (2026-09-06 02:0x)
- **Acción:** auditor #57 (4 worktrees). **Archivos:** docs/auditoria-2/api-expediente.md, docs/logs/audit-api-ronda3.log, commit `810178e`. **Resultado:** 95/95 + 144/144; aislamiento 46/46 ataques bloqueados (0 fugas, incl. ZIP y documentos entre orgs). **ALTA:** AE-01 (`asOfIso` controlado por el cliente para vigencia de tarifas/documentos; el trigger 0042 no participa en este flujo), AE-02 (editar sección tras aprobación NO invalida: `proposal_sections` fuera del hash de insumos → paquete "ready" con contenido no revisado). **MEDIA:** magic bytes solo en offset 0, HTML/script sin sanitizar en texto extraído, sin anti zip-bomb, plazo de pago sin advertencia de calendario incompleto ni régimen legal por fecha (REQ-050). **BAJA:** sha256 de JSON en manifiesto, Zip Slip latente, caché DB-09 sin TTL, README 24 vs 26 rutas, ACEPTACION desactualizado. 0 críticos. **Siguiente:** corrector #60 (incluye DB-13/API-13), luego reverificación.

## Micro-corrección SR-23/SR-24 — packages/sources (2026-09-06 02:2x)
- **Acción:** corrector #59 (reanudado tras 429 del servidor). **Commits:** SR-24 `305e2fb` ({records, dropped} en OCDS/DOF/estatales; `reportDropped` en los 5 conectores; invariante `drop-invariant.test.ts`), SR-23 `a86a6eb` (`semanticContentMarkers` + detección de interstitial), docs `19a2c5c`. **Prueba:** 204 tests; cobertura 89.4/83.7; worker 298 OK (docs/logs/fix-sources-micro.log). **INC-07:** copias « 2.ts» obsoletas eliminadas por Fable (worker typecheck en verde). **INC-08:** limitación intermitente del servidor; #42 y #60 con reanudaciones repetidas. **Siguiente:** verificación #61; cierre definitivo de sources.

## Cierre definitivo — packages/sources (2026-09-06 03:0x)
- **Acción:** verificador #61, commit `0bf72d3`. **Resultado:** 204/204; gate real; SR-24 CERRADO (5 conectores + histórico: dropped/errors/interface_changed confirmados con fixtures propios; zod rechaza fecha/importe corruptos); SR-23 CERRADO con residual **SR-25** (nota DOF real de 87 bytes útiles rechazada solo por longitud; baja-media). **Límite documentado:** el reporte de descartes es cooperativo por convención (un conector de terceros que filtre sin `reportDropped` no es detectado por el pipeline). **Decisión Fable (regla de 3 vueltas):** SR-25 y el límite cooperativo quedan como límites aceptados con causa en el balance del paquete; no se abre cuarta vuelta. REQ-148: cumplido para los conectores conocidos con dos salvedades (se reflejará así en ACEPTACION). **Paquetes cerrados con reverificación independiente:** agents, expediente, worker, sources. **Pendientes de cierre:** apps/web (ronda 3 #42 en curso), apps/api (corrector #60 en curso → reverificación).

## Corrección — apps/api tras auditoría 2 (2026-09-06 03:1x)
- **Acción:** corrector #60 (reanudado 3 veces por 429 del servidor). **Commits:** AE-01 `f2fef8a` (asOf derivado en servidor de submission_deadline; bloqueo si falta), AE-02 `6c43174` (edición de sección invalida aprobación; conditionEvaluations), DB-13 `f8ffaa6` (TZ explícita America/Mexico_City; test bajo UTC/Tokyo), API-13 `e097c27`+`668a082` (eventos de auth en audit_log vía `app.record_auth_event`, migración 0051, lista blanca justificada), AE-09 `3532308` (calendarNote + legalRegime con fecha DOF), AE-03/05 `2170bf3` (magic bytes en todo el buffer, anti zip-bomb), AE-04 `622dbe2` (sanitización de texto extraído), AE-10 `6534f5f` (caché con TTL/tamaño), AE-12 `d4788fe`, columnas `f7654e2`, log `682f9a4`. Todo rojo→verde verificado. **Prueba:** apps/api 36 archivos / 121 tests; packages/db 20 / 147; typecheck/lint OK (docs/logs/fix-api-auditoria2.log). **Fuera de ámbito → #63:** AE-06/07/08/11 (packages/expediente). AE-13 (ACEPTACION) → Fable al cierre. **Siguiente:** reverificación #62; micro-corrección #63.

## Ronda 3 — apps/web conectado a apps/api real (2026-09-06 03:2x)
- **REQ:** secc. 11–12, 20–22, 29–33 (E2/E3/E5/E10 desde UI). **Acción:** agente #42 (reanudado 2 veces por 429). **Commits:** 11 (auth/sesión con refresh rotation y guard W-12, Empresa, Convocatorias, Go/No-Go, Back office, W-21, W-22, W-23, robustez E2E, fix Content-Type, docs). **Prueba:** 63/63 unit (MSW: 401→refresh→reintento, 403, 500+request_id, red caída); `test:e2e:full` contra apps/api real: 89 OK + 2 flaky recuperados + 3 fallos en skip-link correlacionados con el rate limit real de la API en ráfaga (no funcionales); flujo real login→org→perfil→documento→tarifa→convocatorias vacías→403 pasa completo (docs/logs/web-ronda3.log). **Honesto:** Usuarios y roles y Auditoría sin conectar por falta de endpoints. **Bugs reales hallados:** CORS de apps/api sin PUT/PATCH/DELETE (mitigado en E2E con proxy same-origin; corrección en API asignada), POST sin cuerpo con Content-Type JSON → 400 (corregido en cliente), drawer sin overflow-y (corregido). **Endpoints faltantes:** memberships, audit-log, aprobación cross-org de tool_calls. **Siguiente:** #64 (api ronda 4: CORS, endpoints, rate limit E2E) y #65 (auditoría del frontend integrado).

## Micro-corrección AE — packages/expediente (2026-09-06 03:3x)
- **Acción:** corrector #63. **Commits:** AE-06/07 `bfbd54e` (sha256 de bytes reales por documento y del ZIP + `verifyManifest(zip)`; nombres de entrada saneados con `filename` aditivo en manifiesto), AE-11 `50fb7ea` (`recordEdit`/`authorsOf`: el aprobador no puede ser autor de contenido del alcance), docs `dbd87e2`. **Prueba:** 413 tests; cobertura 93.0/89.7; apps/api typecheck OK (docs/logs/fix-expediente-ae.log). **AE-08:** el defecto real está en apps/api (`inputs.ts` omite capabilities/experience/signatories del hash) → asignado a #64 junto con el cableado de `recordEdit`. **Estado del paquete expediente:** 413 tests, cerrado; solo integración pendiente en API.

## Reverificación — apps/api tras auditoría 2 (2026-09-06 03:4x)
- **Acción:** reverificador #62 (3 descendientes). **Archivos:** docs/auditoria-2/api-expediente-reverificacion.md, docs/logs/reverify-api-auditoria2.log, commit `2b0b028`. **Resultado:** 121/121 + 147/147; 46 ataques; AE-01/03/04/05/09/10 y DB-13 RESUELTOS sin huecos (políglotas reales, zip-bomb 1004:1, fronteras TZ/régimen/fin de semana); AE-02 resuelto con laguna **AE-14** MEDIA (`/package/latest` y `/download` sirven el ZIP ready antiguo tras invalidar hasta reensamblar); API-13 resuelto con **API-14** (record_auth_event acepta actor arbitrario; mitigado por el único llamador); **AE-15** BAJA (bodyLimit 1 MB hace inalcanzable el límite de storage). 0 críticos, sin fugas. Sin regresión en web/worker. **Siguiente:** AE-14/API-14/AE-15 asignados a #64 (único escritor de apps/api); reverificación final de api al cerrar #64.

## Auditoría 2 — apps/web integrado (2026-09-06 03:5x)
- **Acción:** auditor #65. **Archivos:** docs/auditoria-2/web-integrado.md, capturas-web/, docs/logs/audit-web-ronda3.log (commit en rama `45304d6` importado a main por Fable con `git archive`, sin tocar el WIP ajeno de apps/api). **Resultado:** 63/63 unit y E2E reproducidos (3 fallos por 429 real del limitador 100 req/min; `RATE_LIMIT_PROFILE` aún no existía). **Hallazgos:** WI-01 MEDIA (sin CSP: helmet con contentSecurityPolicy:false y build web sin cabeceras; agrava refresh en localStorage), WI-02 MEDIA (subida sin accept ni validación cliente, REQ-098), WI-03 BAJA/MEDIA (logout sin limpiar caché global), WI-04 BAJA (aprobar/rechazar sin guard y API sin transición condicional), WI-05 BAJA (E2E no determinista por rate limit). Comprobado: 29 rutas a 320 px sin overflow con datos reales; A4 UI real confirmada (`useSourceFreshness` → `app.source_freshness()` sobre source_runs). **Siguiente:** #66 corrige lado web; WI-01/WI-04 lado API asignados a #64.

## Corrección — apps/web integrado (2026-09-06 04:4x)
- **Acción:** corrector #66 (reanudado 2 veces). **Commits:** WI-03 `74c2358` (clear de caché en logout/switchOrg), WI-02 `2ae7af8` (validación cliente tipo/tamaño ≤22 MB, rechazo de extensiones e.firma REQ-098), WI-04 `a1f70bb` (guard por fila + 409), WI-01 `33869c2` (CSP como meta en build y cabeceras en preview; ejemplos nginx/Caddy; E2E prueba que un script inline no ejecuta; TODO refresh→cookie httpOnly), WI-05 `5cbb538` (RATE_LIMIT_PROFILE=e2e en orquestador; bug real de puerto por worker corregido), docs `a7215a6`. **Prueba:** 79/79 unit; `test:e2e:full` 97 OK / 1 flaky recuperado / 0 fallos (antes 89/2/3); typecheck/lint/build OK (docs/logs/fix-web-ronda3.log). **Nota:** usó una vez `git reset <paths>` para des-stagear (regla dura; sin pérdida) → INC-09 leve. **Siguiente:** al cerrar #64, reverificación final integrada (api ronda 4 + web WI) y tablero.

## Ronda 4 — apps/api (2026-09-06 04:5x)
- **Acción:** agente #64 (reanudado 2 veces). **12 ítems, un commit cada uno:** CORS explícito `0e127a1`; memberships (`app.org_members` SECURITY DEFINER, migración 0052) + audit-log org/admin + tool_calls cross-org atómico `88b28bb`/`270bbd9`; rate limit por niveles con `RATE_LIMIT_PROFILE=e2e` solo opt-in `7bbd81a`; POST sin cuerpo en rutas de acción `270bbd9`; AE-08 `9064469` (hash de perfil completo), AE-11 `e30e41c` (recordEdit cableado), AE-14 `1b73639` (package re-derivado), API-14 `a47c287` (actor validado), AE-15 `12189a3` (bodyLimit), WI-01 `75a1000` (CSP real), WI-04 `72771ea` (transiciones atómicas); docs `9fc30a3`, `c61fb02`. **Prueba:** apps/api 48 archivos / 180 tests; packages/db 22 / 156; typecheck/lint OK (docs/logs/api-ronda4.log). **Siguiente:** reverificación final integrada (#67, en curso) y ci-local 2 (en curso); después tablero de aceptación e informe.

## ci-local segunda corrida (2026-09-06 05:0x)
- **Acción:** Fable ejecutó `scripts/ci-local.sh --skip-install` con el árbol limpio y sin agentes escribiendo código (docs/logs/ci-local-2.log). **Resultado real:** 28/28 combinaciones de workspaces OK (typecheck/lint/test+cobertura/build de apps/api, apps/web, apps/worker, packages/agents, packages/db, packages/expediente, packages/sources); `npm audit` OK; **`check-secrets` FALLÓ** por 2 falsos positivos: fixtures de prueba con PEM falsa en `apps/api/test/security-api11-file-magic-bytes.test.ts:50` y `apps/web/e2e/ronda3-flujo-real.spec.ts:157`. Los 2 timeouts de la primera corrida no reaparecen. **Siguiente:** micro-corrección #68 (marcador de fixture solo en tests + prueba negativa) y ci-local 3.

## ci-local tercera corrida (2026-09-06 05:1x)
- **Acción:** agente #68, commit `c210db4`: marcador `check-secrets:allow-fixture` por línea y solo en archivos de test/e2e; prueba negativa (PEM fuera de tests → falla) ejecutada y eliminada; `*.log` excluidos del escaneo (autorreferencia). **Resultado real (docs/logs/ci-local-3.log):** 28/28 workspaces OK, `npm audit` OK, `check-secrets` OK. **Pipeline local completo en verde.** Pendiente externo: job Postgres real solo en GitHub Actions (B-03).

## Reverificación final integrada — apps/api + apps/web (2026-09-06 05:2x)
- **Acción:** reverificador #67 (2 descendientes). **Archivos:** docs/auditoria-2/reverificacion-final-integrada.md, docs/logs/reverify-final-integrada.log, commit `1ff99fb`. **Resultado:** 20 commits de ronda 4 y WI verificados; suites 180 (api) + 156 (db) + 79 (web unit) + 298 (worker) = 713 verdes; `test:e2e:full` con `RATE_LIMIT_PROFILE=e2e` 98/0/0 en dos corridas (determinista, WI-05 cerrado); 21 ataques dinámicos (13 API por HTTP real, 8 web en Chromium real) sin bypass; 12/12 hallazgos CERRADOS (WI-04 con matiz visual sin impacto: el servidor sigue siendo la barrera, 1×200 + 1×409, una sola auditoría). **Nuevos BAJA no explotables:** API-15 (approve/reject de tarifas rechazan cuerpo vacío), WI-06 (doble clic físico dispara 2 peticiones; servidor idempotente). Tabla A1–A15 con test integrado real por criterio. **Siguiente:** tablero final (#69) y micro-corrección API-15/WI-06 (#70); después informe y decisión del bucle.

## Micro-corrección API-15/WI-06 (2026-09-06 05:4x)
- **Acción:** corrector #70. **Commits:** API-15 `fbf5771` (approve/reject de tarifas aceptan cuerpo vacío; 3 tests), WI-06 `9bd8b55` (guard síncrono con useRef antes de mutate; test de componente + E2E con doble clic físico real → 1 petición), log `5adeadf`. **Prueba:** apps/api 183 tests; apps/web 80 unit; E2E dirigido 11/11 (docs/logs/fix-api15-wi06.log). Reproducción pre-fix en worktree. **Estado:** REPARADO por el corrector; sin reverificación independiente adicional (BAJA no explotables; la barrera real es el servidor, ya reverificado). En tablero: EN_EVIDENCIA para esos dos ítems.

## Tablero final e informe de estado (2026-09-06 05:5x)
- **Tablero (#69, commit `e61156e`):** 186 criterios → 47 CUMPLIDO, 53 EN_EVIDENCIA, 77 PENDIENTE, 5 LÍMITE_ACEPTADO, 3 NO_APLICA, 1 BLOQUEADO_EXTERNO. Épicas cerradas con reverificación: E1, E3 (mecanismo), E4, E7, E9. **Inexactitud detectada por Fable:** E11 marcada "0% construido" pese a `apps/api/src/modules/expediente/post-award.routes.ts`, migración 0032 y `expediente-post-award.test.ts` → a reconciliar en la próxima pasada.
- **Informe:** docs/INFORME-ESTADO.md (implementado/probado, pendiente ejecutable, bloqueos externos, no construido).
- **Decisión del bucle:** NO se detiene: queda trabajo ejecutable (conexión web del expediente y back office, REQ-142, REQ-171, calendario oficial, 2FA, aviso de privacidad, guard 404). Ronda 5 despachada (#71 web, #72 api).

## Ronda 5 — apps/api (2026-09-06 06:5x)
- **Acción:** agente #72. **Commits:** E11 estructurado + `calendar_holidays` (vacío por defecto, sin fuente verificable en línea; carga admin) + alertas `480d691`; REQ-142 procedencia vinculante `1025978`; REQ-171 correlation_id extremo a extremo + `GET /audit-log?correlationId=` `67394d8`; REQ-044/064 2FA TOTP + step_up_sessions `53427ff`; REQ-119/131 aviso de privacidad versionado (borrador) `234eb54`; log `25c53bb`. **Migraciones:** 0055–0057. **Prueba:** api 53 archivos / 200 tests; db 158; expediente 413 (docs/logs/api-ronda5.log). **E11 reconciliado:** apps/api/docs/e11-cobertura.md (post-award existía desde ronda 3; el tablero estaba mal). **Pendientes honestos:** REQ-051..055 (máquina de estados de contrato, extracción de contrato firmado, redactor de inconformidades, autopsia del fallo, radar de renovaciones) no construidos; solo TOTP (sin passkey); tool_calls sin 2FA; validación jurídica del aviso. **Siguiente:** auditoría #73; web (#71) conecta privacidad, 2FA y alertas.

## Auditoría — apps/api ronda 5 (2026-09-06 07:1x)
- **Acción:** auditor #73 (informe commiteado por Fable tras corte 429). **Archivos:** docs/auditoria-2/api-ronda5.md, docs/logs/audit-api-ronda5.log. **Resultado:** reproducibilidad limpia (200/158/413/298); **2 CRÍTICOS reproducidos:** R5-01 (`calendar_holidays` cargado nunca excluye el día en el cómputo del plazo por bug de conversión de fecha, y `calendarNote` afirma falsamente que se incluyó), R5-02 (endpoints TOTP sin rate limit específico: 40 códigos incorrectos seguidos sin 429); R5-03 MEDIA (fallos 2FA sin audit_log), R5-04 MEDIA (traza REQ-171 no incluye ingesta/descubrimiento), R5-05 BAJA-MEDIA (step_up_sessions no ligado a acción/org), 3 BAJA (esquema de URL, fecha inválida → 500, docs de épicas). Aislamiento y roles intactos. **Siguiente:** corrector #74 (prioridad críticos), luego reverificación.

## Corrección — apps/api ronda 5 (2026-09-06 07:4x)
- **Acción:** corrector #74. **Commits:** R5-01 `fccab53` (feriados serializados en fecha civil; calendarNote veraz), R5-02/03/05 `7242298` (tier `twoFactor` 5/5 min con mínimo no relajable por perfil e2e; `twofa_lockouts` 0058 con bloqueo progresivo; fallos 2FA a audit_log 0059; step_up_sessions con org/propósito 0061), R5-04 `4db65e9` (correlation_id desde ingest, 0060; mitad en worker documentada fuera de ámbito), R5-06/07 `c2deca5`, docs `efea5b3`. **Prueba:** api 227 tests; db 158; worker 298 (docs/logs/fix-api-ronda5.log). **Siguiente:** reverificación #75; R5-08 se cierra en la reconciliación del tablero.

## Reverificación — apps/api ronda 5 (2026-09-06 08:0x)
- **Acción:** reverificador #75, commit `3e02eca`. **Resultado:** 227/158/298 reproducidos; R5-01 CERRADO (feriado el mismo día, bisiesto, TZ), R5-02 sólido (IP rotada, usuario distinto, desbloqueo, ventana progresiva), R5-04 CERRADO (inyección y cabecera de 10k saneadas), R5-06/07 CERRADOS. **PARCIAL:** R5-03 → **R5-10** (fallos 2FA sin IP), R5-05 → **R5-09** (apps/web nunca envía orgId/purpose → sesiones genéricas válidas para cualquier acción/org en producción). Candidatos a CUMPLIDO: REQ-044/064, 050, 142, 171 (residual worker), 119. **Siguiente:** corrector #76 (API exige org+propósito; IP en auditoría) y el agente web (#71) añade orgId/purpose en el cliente.

## Corrección R5-09/R5-10 — apps/api (2026-09-06 08:5x)
- **Acción:** corrector #76. **Commits:** R5-09 `a7026ec` (X-Org-Id + purpose obligatorios con enum cerrado; migración 0062 borra sesiones genéricas y NOT NULL + consumed_at; 0063 CHECK; `requireStepUp` exige coincidencia exacta y consume la sesión una sola vez), R5-10 `586e3cf` (ip/user-agent en fallos 2FA). **Prueba:** api 58 archivos / 230 tests; db 24 / 161 (docs/logs/fix-api-r5-09-10.log). El cliente web ya declara los mismos valores (commits 4f44a26, 41e6d75 de #71). **Siguiente:** al cerrar #71, auditoría final de ronda 5 integrada (web + step-up end-to-end) y reconciliación del tablero.

## Reverificación R5-09/R5-10 — apps/api (2026-09-06 09:0x)
- **Acción:** reverificador #77, commit `f6f0170`. **Resultado:** R5-09 CERRADO (8 ataques: sin org/purpose 400, enum 400, cross-org/cross-purpose/reutilización/expirada 403; migración 0062 sobre sesiones genéricas reales; concurrencia: exactamente una de dos usos simultáneos del mismo token), R5-10 CERRADO (ip/UA en los 4 caminos; sin secretos en logs; X-Forwarded-For no confiable por trustProxy=false). Tests adaptados sin debilitar aserciones. **Nuevos:** R5-11 (approve/deny de tool_calls no exigen step-up pese a tener propósito en el enum) → corrector #78; R5-12 (comentario obsoleto en apps/web) → auditoría web.

## Corrección R5-11 — apps/api (2026-09-06 09:2x)
- **Acción:** corrector #78, commit `428797f`: `requireStepUp` en approve/deny de tool_calls (org: `tool_call.approval`; admin cross-org: `admin.action`, resolviendo org por la fila); superadmin sin 2FA → 403; tests nuevos y existentes adaptados sin debilitar aserciones. **Prueba:** api 59 archivos / 238 tests (docs/logs/fix-api-r5-11.log). **Siguiente:** verificación puntual junto con la auditoría del frontend de ronda 5 cuando #71 cierre.

## Ronda 5 — apps/web cerrado (2026-09-06 09:3x)
- **Acción:** agente #71 (reanudado 3 veces). **Resultado:** conectados a la API real Análisis de bases, Cumplimiento, Redacción, Expediente/Aprobaciones, Entregas/Paquete, Seguimiento post-adjudicación con alertas, Usuarios/roles, Auditoría con traza por correlación, Aprobaciones cross-org, enrolamiento y step-up 2FA con orgId/purpose, aviso de privacidad servido por la API, guard 404 cross-tenant. **Prueba (corrida limpia, docs/logs/web-ronda5.log):** typecheck/lint/build OK; unit 105/105; `test:e2e:full` 116/116 contra apps/api real. **Bugs reales corregidos en el cierre:** orden de enrolamiento 2FA, colisión de time-step TOTP, carrera en A14, frase de bases sin evidencia, falsos positivos de axe por animaciones, contraste del toast. Sin endpoints faltantes. **Siguiente:** auditoría final #79 en curso; reconciliación del tablero.

## Auditoría final — ronda 5 (2026-09-06 09:5x)
- **Acción:** auditor #79 (worktree en 38a6a3b; UI real en Chrome contra API real). **Archivos:** docs/auditoria-2/ronda5-final.md, capturas-r5/, docs/logs/audit-ronda5-final.log, commit `6099a04`. **Resultado:** api 238/238; web unit 105/105; `test:e2e:full` FALLÓ 2/2 en el HEAD auditado por una violación axe "serious" determinista en el toast (RF-04; 6 commits posteriores de #71 cambian el diagnóstico → reverificar). **Hallazgos:** RF-01 MEDIA-ALTA (páginas de tool_calls nunca envían X-Step-Up → 403 siempre tras R5-11, sin test que lo detecte), RF-02 MEDIA (carrera real: bootstrap llama refresh fuera del mutex → refresh concurrente 200+401, reproducido en dev y preview; puede cerrar sesión válida), RF-03 BAJA (sin QR visual), RF-04 MEDIA. **Comprobado correcto:** step-up con org/propósito, TOTP y códigos de respaldo, roles en UI, A11/A12/A14/A15 con tests nombrados, back office con traza, post-adjudicación con régimen derivado, CSP, bundle sin secretos, REQ-142/171 aplicados. **Siguiente:** corrector #80; luego reverificación y tablero.

## Corrección RF-01..04 — apps/web (2026-09-06 10:2x)
- **Acción:** corrector #80 (reanudado). **Commits:** RF-01 `0d4f5cf` (StepUpDialog en tool_calls org y cross-org con orgId del dueño; MSW verifica cabecera/body/403), RF-02 `6282988` (`refreshSessionOnce()` único mutex; en preview real 20 recargas → 0×401), RF-03 `9dc6000` (QR real en canvas accesible), RF-04 no reproduce (cerrado por `40b3dfe`; 2/2 corridas 116/116), docs `ec16003`, README `9be55d9`. **Prueba:** unit 114; build; `test:e2e:full` ×2 116/116 (docs/logs/fix-web-ronda5.log). **Siguiente:** reverificación #81; después tablero e informe finales y decisión del bucle.

## ci-local cuarta corrida (2026-09-06 10:3x)
- **Resultado real (docs/logs/ci-local-4.log):** 29 combinaciones OK; **`apps/web:test:coverage` FALLÓ**: 6 tests de páginas por timeout (~200–232 s, retry ×1) con errores `HTMLCanvasElement.prototype.getContext` no implementado en jsdom (canvas del QR de RF-03). La misma suite pasó en docs/logs/fix-web-ronda5.log (114) y la corrida coincidió con la reverificación #81 (Playwright + API en paralelo). **No se marca en verde:** corrector #82 despachado para demostrar la causa (canvas en jsdom vs contención) y corregir; se repetirá la corrida.

## Diagnóstico ci-local-4 — apps/web (2026-09-06 10:5x)
- **Acción:** corrector #82, commit `0a29c10`. **Causa demostrada:** los 6 timeouts fueron contención de CPU real (load 10–12 en 10 núcleos con Playwright/Chrome del reverificador en paralelo): `npm run -w apps/web test` 115/115 dos veces sin coverage; con `--coverage` caían pruebas distintas cada vez, siempre por su `it(…, 20000)` propio, nunca un hang real. El error de canvas del QR era real y separado: mock de `qrcode` en setup de vitest (sin tocar getContext global, que usa axe) + try/catch con fallback textual accesible en `TotpQrCode` y prueba nueva. **Regla:** `test:coverage` de apps/web se ejecuta sin otras cargas pesadas; ci-local 5 se repetirá al cerrar #81.

## Reverificación final — ronda 5 (2026-09-06 11:0x)
- **Acción:** reverificador #81, commit `b99a72f`. **Resultado:** api 238/238; web unit 114/114 (aislado); build; `test:e2e:full` 2/2 116/116. RF-01 CERRADO (UI real: admin sin 2FA bloqueo honesto, con 2FA 200 con purpose/orgId correctos, superadmin cross-org con orgId del dueño, token de otro purpose 403; R5-11 concurrencia 200+409), RF-02 CERRADO (30/30 recargas de build de producción sin 200/401; refresh corrupto → logout limpio), RF-03 CERRADO (QR decodificado con jsQR coincide con otpauth; canvas accesible), RF-04 CERRADO (contraste 14.88:1 oscuro / 17.13:1 claro). R5-12 BAJA abierta (comentario). Nota: la suite automatizada no corre en tema oscuro (cobertura, no hallazgo). **Ronda 5 cerrada con reverificación independiente.** Worktrees huérfanos eliminados por Fable; ci-local 5 lanzada sin agentes de código en paralelo; tablero #83 despachado.

## ci-local quinta corrida (2026-09-06 11:1x)
- **Resultado real (docs/logs/ci-local-5.log, sin agentes de código en paralelo, load ~7):** 29 OK, npm audit y check-secrets OK, **`apps/web:test:coverage` FALLÓ de nuevo** (10 tests de páginas por timeout 44–80 s; error de canvas persiste pese al mock de 0a29c10). Total de tests contados en la corrida: 1681. **Conclusión de Fable:** el diagnóstico "contención" de #82 no explica un fallo consistente en 3 corridas con coverage mientras `test` sin coverage pasa 115/115 → corrector #84 con obligación de demostrar la causa real y dejar 2 corridas de coverage en verde. No se marca en verde hasta entonces.

## Tablero tras ronda 5 (2026-09-06 11:3x)
- **Acción:** agente #83, commit `a644803`. **Conteos:** CUMPLIDO 47→54, EN_EVIDENCIA 53, PENDIENTE 77→69, LÍMITE_ACEPTADO 5, NO_APLICA 3, BLOQUEADO_EXTERNO 1→2 (REQ-056 calendario SABG sin fuente en línea). Cierres: REQ-044/064, 050, 142, 170, 171, A13. E11 corregida a EN CURSO real. Único defecto abierto: R5-12 (BAJA). Tests aislados: api 238, db 161, web 114 unit + 116 E2E ×2. **Pendiente de cierre en este ciclo:** `apps/web:test:coverage` (corrector #84 en curso) y ci-local 6.

## Decisión del bucle tras ronda 5 (2026-09-06 11:4x)
- **Informe actualizado:** docs/INFORME-ESTADO.md. **Decisión:** el bucle NO se detiene: quedan 69 criterios PENDIENTE con trabajo ejecutable (REQ-051..055 post-adjudicación avanzada, REQ-169 dashboard, sala de guerra, aviso de privacidad validable, etc.) además de bloqueos externos (B-01/B-02/B-03, INC-10 iCloud, OpenAI, abogado). **Ronda 6:** #85 API (REQ-051..055) despachado; web queda reservado para #84 (coverage) y después REQ-169/sala de guerra.

## Ampliación 2 — salida a promoción (2026-09-06 11:5x)
- **Instrucción del usuario:** todo de punta a punta a nivel Likida/Restaurantes: auth de Google lista, correos listos y editados con plantilla como Likida, listo para salir a promoción. **Registro:** docs/AMPLIACION-2-SALIDA.md, D-08, estado persistente del bucle actualizado. **Despachos:** #86 investigación de referencias (solo lectura), #87 requisitos/aceptación/backlog (REQ-172+, S1–S12, E13–E17), #88 packages/mail (nuevo; no choca con #84 web ni #85 api). **Siguiente:** Google OIDC en apps/api cuando cierre #85; login con Google, onboarding y landing en apps/web cuando cierre #84; integración de correos en api/worker; infra/ despliegue. **Bloqueos externos previstos:** credenciales OAuth de Google, proveedor de correo y dominio remitente, hosting/dominio/BD, textos legales validados.

- **Ampliación 2 → requisitos (#87, commit `d107164`):** REQUISITOS.md sección 34 «Salida a promoción» REQ-172..210 (39); ACEPTACION.md con esas filas (REQ-178 BLOQUEADO_EXTERNO por credenciales de Google) y pruebas mínimas S1–S12 (S11 BLOQUEADO_EXTERNO: sin docker en esta máquina); BACKLOG.md E13–E17. Total ahora 210 REQ + A1–A15 + S1–S12.

## Paridad de producto — inventario y matriz (2026-09-06 12:0x)
- **Acción:** agente #89 (3 descendientes), commit `a76bcf4`, docs/investigacion/paridad-producto.md. **Hallazgo:** Restaurantes y Likida son SaaS de consumo con landing/precios/checkout; Licitaciones tiene módulos de dominio conectados con mejor calidad de tests/a11y pero le faltan capas transversales. **Rondas reproducibles sin credenciales (A–M):** dashboard real (REQ-169), exportaciones CSV/PDF e importación de tarifas, notificaciones in-app, actividad reciente, paginación/filtros completos, perfil de usuario y sesiones, recuperación de contraseña, feature flags/kill-switch, OpenAPI público, backups/drill, `run_agent` con lógica real, demo de solo lectura, términos con FaltaDato. **Decisiones/credenciales del usuario:** modelo comercial (B2B directo vs self-serve con Stripe/planes/trial), hosting, correo, Sentry, LLM, Google OAuth, fuentes oficiales (B-02). **Plan Fable:** al liberarse apps/api (#85) y apps/web (#84) se ejecutan E13–E16 (Google, correos, onboarding, landing/legales) y las rondas A–M por lotes con auditoría y reverificación.

- **Referencias salida a promoción (#86, `489d7c0`):** Google en ambos vía Supabase Auth OAuth (no crea tenant; Likida compuerta SIN_ROL → /sin-acceso: aplicar en REQ-179/180). Correos: Resend por fetch, HTML tablas/CSS inline, logo cid; outbox con reintento/backoff (Restaurantes); webhooks bounce/queja + supresión (Likida). Onboarding: entrevista + checklist por hechos reales (Likida). Landing fuera del código en ambos; Likida modela calculadora/demo/blog/seguridad/legales. Despliegue: Vercel+Supabase, healthcheck, promoción/rollback manual. 10 plantillas propuestas (REQ-181). Instrucciones pasadas a #88 (packages/mail).

## Infra de despliegue (E17) (2026-09-06 12:1x)
- **Acción:** agente #90, commit `e6b9c80`. Dockerfiles multi-etapa no-root (api/worker node:22-alpine con tsx; web nginx con CSP/cabeceras coherentes), compose prod (postgres, migrate one-shot, api, worker, web, caddy TLS) y dev, `.env.prod.example` completo, scripts backup/restore/healthcheck, rotate-secrets.md, infra/README.md, sección 11 de docs/OPERACION.md. **Hallazgo real:** `node dist/index.js` documentado no arranca (paquetes internos exportan TS sin compilar) → corregido usando tsx en imágenes (deuda: build real de paquetes, pendiente). **Validado aquí:** dockerfile-utils, YAML, shellcheck. **BLOQUEADO_EXTERNO:** build/run de Docker, compose end-to-end, TLS, backup/restore reales (sin Docker en esta máquina; S11).

## Ronda 6 — apps/api REQ-051..055 (2026-09-06 12:2x)
- **Acción:** agente #85. **Commits:** 6 (uno por REQ + tests/docs); migraciones 0065–0070. **Resultado:** contratos con máquina de estados (409 en transición inválida, historial inmutable por RLS, step-up para rescindir/penalizar/modificar/inconformidad, alertas por jobs); extracción declarativa del contrato firmado (campos `sugerido` hasta confirmación; requires_ocr); borrador de inconformidad versionado e inmutable con fundamentos LAASSP art. 49/95 (DOF 16-abr-2025), plazo 6/10 días hábiles, disclaimer y guardrail anti-frivolidad; autopsia del fallo con "no disponible" explícito y lecciones por org; radar de renovaciones con dedupe y jobs sin envío. **Prueba:** api 262/263 (1 fallo atribuido a iCloud → a verificar por #93), db 168/168 (docs/logs/api-ronda6.log). **INC-11** registrado. **Siguiente:** auditoría #93; Google OIDC #92 en apps/api (auth) en paralelo.

## Cobertura de apps/web — causa real (2026-09-06 12:4x)
- **Acción:** corrector #84, commit `c27e905`. **Causa demostrada (cpu-prof):** abrir un `<Select/>` de Radix con user-event en jsdom produce una pausa real del bucle de eventos de 10–30 s (95% inactivo), no causada por coverage ni qrcode ni MSW, agravada por varios workers abriendo Select a la vez. **Corrección:** `maxWorkers: 3` solo bajo coverage y timeouts propios medidos en las 11 pruebas afectadas; 2 corridas consecutivas de `test:coverage` en verde bajo carga real (115/115); `test` sin cambios. **Siguiente:** ronda 7 web (#94) despachada: landing, términos, onboarding, dashboard real, demo.

## packages/mail (E14) (2026-09-06 12:5x)
- **Acción:** agente #88. **Resultado:** motor React Email + zod; 16 plantillas en español de México con marca Atiende y estructura de Likida (wordmark de texto porque Gmail descarta SVG; plumbing cid: listo para PNG oficial); MailService con preferencias → supresión fail-closed → idempotencia (outbox) → render → rate limit → reintentos; adaptadores Resend/Postmark/SMTP por fetch directo + Capture; webhooks con firma Svix. **Prueba:** 220 tests / 24 archivos; cobertura 96.4/83.4; preview generado (docs/logs/mail-ronda6.log). **Pendiente externo:** credenciales del proveedor y `MAIL_LINK_SECRET`. **Siguiente:** auditoría #95; integración en apps/api (verificación, invitación, restablecimiento, 2FA, aprobaciones, paquete) al cerrar #92, y en apps/worker (alertas, digesto) al cerrar #91.

## Vercel — frontend en producción (2026-09-06 13:1x)
- **Acción:** Fable (autorizado por el usuario; sesión Vercel del usuario, cuenta likidaai-8016). Proyecto `likida/atiende-licitaciones` enlazado desde la raíz del monorepo con `vercel.json` (npm ci, build de apps/web, salida apps/web/dist, rewrites SPA, cabeceras de seguridad). Primeros dos intentos fallaron (deploy desde apps/web sin el monorepo; luego configuración); tercer y cuarto despliegue **Ready** en producción: https://atiende-licitaciones.vercel.app (alias) . **Pendiente:** `VITE_API_URL` y CSP `connect-src` apuntando a un host real de API/worker/Postgres (decisión del usuario: Supabase/Neon + Render/Fly, o adaptar); la landing y la demo funcionan sin API; las pantallas con datos reales muestran estado de error honesto. Integración Git de Vercel: cada push a main despliega.

## Auditoría — apps/api ronda 6 (2026-09-06 13:3x)
- **Acción:** auditor #93 (reanudado 5 veces), commit `ef6d3fd`. **Resultado:** db 168/168, api 262/263 reproducidos fuera de iCloud: el test fallido `expediente-documents-and-matrix.test.ts` NO era un flake de iCloud sino **R6-01/02 ALTA**: `pdf-parse@1.1.1` no lee PDFs con xref-stream (formato de pdf-lib y de muchos PDF reales) → REQ-052 nunca probó con éxito la extracción real; INC-10 se mantiene como observación de entorno pero deja de explicar ese fallo. **R6-03 ALTA:** radar de renovaciones 17–28 s con 5.000 contratos (N+1 en una transacción HTTP). R6-04 MEDIA (posible carrera en transiciones), R6-05..08 BAJA. **Comprobado correcto:** 78/78 transiciones inválidas → 409, historial inmutable incluso para superadmin, step-up correcto, aislamiento en 8 tablas nuevas, citas LAASSP 49/95 exactas, sin envío externo. **Siguiente:** corrector #98 (extractor PDF real por páginas, radar por lotes, transición condicional).

## Worker ronda K — agentes de negocio reales (2026-09-06 13:4x)
- **Acción:** agente #91, commits `44ab74a`, `6b1035f`. 8 herramientas reales (leen/escriben Postgres vía worker_role con verificación de políticas), 5 agentes nombrados con plan fijo (analista_convocatorias, analista_bases, redactor_borrador, vigilante_cambios, recordatorios) ejecutados por `run_agent` con AgentRunner real (autorización, guardrail, no fabricación, presupuesto, idempotencia, kill-switch); encolado por evento con dedupe; 26 evals deterministas con FakeProvider. **Prueba:** 370 tests; cobertura 93.0/84.7 (docs/logs/worker-ronda6.log). **PENDIENTE esquema:** PROPOSAL-06 (grants de tools) → escritor de packages/db. OpenAI real sigue sin credenciales. **Siguiente:** auditoría adversarial del worker ronda K; grants en migración.

## Google OIDC — apps/api (E13) (2026-09-06 13:5x)
- **Acción:** agente #92 (reanudado 6 veces). PKCE S256 + state firmado + nonce; issuer configurable con discovery; migraciones 0071 (`oauth_states` de un solo uso con TTL) y 0072 (`user_identities`, password_hash nullable, funciones SECURITY DEFINER justificadas en la lista blanca; acciones de auditoría google_login/linked/rejected); vinculación por email verificado con detección de conflicto; compuerta `sin_acceso` salvo invitación pendiente; 2FA respetado con `pendingToken` de 5 min y `POST /auth/google/verify-2fa`. **Prueba:** proveedor OIDC falso real (HTTP local, RSA, discovery/JWKS/token, PKCE verificado); 11 tests de integración (S1–S3 + state/aud/2FA/sin_acceso/conflicto) + 17 de esquema; api 278 tests; db 185 (docs/logs/api-google.log). **BLOQUEADO_EXTERNO:** flujo real hasta credenciales de Google (REQ-178). **Siguiente:** #100 integración de correos, recuperación de contraseña y contacto en la API; botón de Google en web cuando cierre #94.

## Corrección packages/mail (2026-09-06 14:0x)
- **Acción:** corrector #96, 6 commits. ML-01 reserva atómica por messageKey (10 concurrentes → 1 envío), ML-02 List-Unsubscribe/RFC 8058 en los 3 adaptadores + endpoint de baja documentado, ML-03 safeUrl con lista blanca, ML-04 contraste faint AA, ML-05 anti-replay Svix; ML-06 devDeps compartidas (documentado); ML-07 → ACEPTACION al cierre. **Prueba:** 252 tests; cobertura 96.3/85.2 (docs/logs/fix-mail.log). Instrucciones pasadas a #100 (integración en API). **Siguiente:** reverificación puntual de mail junto con la auditoría de la integración en API.

## Auditoría — agentes del worker (2026-09-06 14:1x)
- **Acción:** auditor #99, commit `eb109f5`. **ALTA:** WK6-01 (las evals de aislamiento por org solo comprueban `status==='ok'`: quitar el filtro org_id no rompe nada pese a una fuga real demostrada con PoC; el código de producción es correcto, la red de seguridad no), WK6-02 (correlationId de negocio no se persiste en agent_runs y el log usa job.id). BAJA: WK6-03 (test de grants flaky bajo carga). **Comprobado con evidencia:** homoglifos y org_id anidado rechazados; prohibiciones duras para todos los roles; proveedor malicioso que inventa precio → bloqueado; presupuesto excedido sin efectos parciales; kill-switch; tormenta de 1.000 eventos dedupe correcto; sin red sin OPENAI_API_KEY y clave falsa sin fuga. **Siguiente:** corrector #103.

## Reverificación — packages/mail (2026-09-06 14:0x)
- **Acción:** reverificador #102, commit `1722a50`. ML-01..ML-06 confirmados corregidos con ataques nuevos sobre código real (reserva concurrente 50→1 envío, List-Unsubscribe en las 16 plantillas y ausente en las 8 obligatorias, 12 vectores SSRF sin bypass, contraste WCAG ≥4.5:1, 6 escenarios de replay Svix sin bypass, `npm audit --omit=dev` = 0). 252 pruebas verdes, cobertura 96/85/98/96. **Abiertos:** ML-07 (ACEPTACION.md: S4–S7/S12 y REQ-207 se actualizan en el pase de documentación al cerrar la integración #100), ML-08 y ML-09 BAJA (ventana de espera del perdedor de reserva 200 ms corta; sin CSS de modo oscuro) → backlog.

## Auditoría — Google OIDC en la API (2026-09-06 14:2x)
- **Acción:** auditor #101, commit `3847748`. Sin bypass en 8 ataques adversariales; 503 limpio sin credenciales; 429 en el tier de auth; función SECURITY DEFINER de invitaciones resistió. **GO-01** (REQ-174 exigía organización automática; la implementación aplica compuerta `sin_acceso`) → **D-09**: se adopta la compuerta, REQ-174 reformulado en REQUISITOS/ACEPTACION, web ronda 8 añade `/sin-acceso` + onboarding «Crear organización» sobre `POST /orgs`. **GO-07** (carrera 23505) y **GO-03** (issuer https) → corrector #104. GO-09 (desvincular Google) → backlog.

## Corrección — worker K (2026-09-06 16:0x)
- **Acción:** corrector #106 (Opus, D-10) cerró WK6-01/02/03 en `c778526`, `caee2fe`, `93ea69f`. Hallazgo propio: el script de mutación que dejó el Sonnet cortado probaba HEAD sin los arreglos (falso «no detectada»); corregido y verificado en vivo (bajo mutación la suite falla con 2 fallos). 383/383 tests en 4 corridas completas, 0 timeouts. **Límite documentado:** la consulta por correlation_id (REQ-171) va contra JSONB sin índice; columna indexada `agent_runs.correlation_id` → backlog E20 (packages/db). **Siguiente:** reverificación independiente del worker K junto con las demás correcciones en curso.

## Corrección — Google OIDC (2026-09-06 16:1x)
- **Acción:** corrector #107 (Opus, D-10) cerró GO-07 (`da18559`) y GO-03 (`c4a0cf9`); las 3 pruebas nuevas de GO-07 fallan con 500 sin el arreglo (no falso verde). **Hallazgo nuevo GO-10 (ALTA):** en el login repetido con Google no se fija `app.current_user_id` antes de `app.my_organizations()` y `sel_user_totp_secrets` → el usuario que vuelve recibe `sin_acceso` aunque tenga organización y NO se le exige el 2FA (bypass de REQ-176). Comprobado en vivo por el corrector. D-09 no lo congela (solo aplica a cuenta nueva sin invitación). **Siguiente:** corrector #110 con test primero.

## Web ronda 7 cerrada (2026-09-06 16:2x)
- **Acción:** #94 (Sonnet) implementó y commiteó landing pública con SEO por ruta y sin analítica, `/legal/terminos` como borrador con `FaltaDato` explícito, `/onboarding` de 5 pasos con `RequireOrganization`, panel REQ-169 (KPIs, actividad con 403 honesto, checklist de activación) y `/demo` de solo lectura aislada; #105 (Opus) cerró: E2E 133/133 ×2, build OK, corrigió la carrera intermitente de WI-06 (clic sobre botón aún `disabled`) y el arranque del E2E (variables MAIL_LINK_SECRET/PUBLIC_URL que la API ya exige). **Pendiente:** reejecutar `test:e2e:full` desde el árbol principal cuando #109 commitee el módulo de correo. **Siguiente:** ronda 8a (#111) con Google, `/sin-acceso`, crear organización y 2FA; 8b (recuperación, contacto, preferencias, verificación de correo) al cerrar #109.

## Corrección — GO-10 (2026-09-06 16:2x)
- **Acción:** corrector #110 (Sonnet), commit `0e130ae`. Confirmado en rojo (login repetido devolvía `sin_acceso` en vez de `ok`/`requires_2fa`); arreglo mínimo: fijar `app.current_user_id` en la rama de identidad ya vinculada; las otras dos ramas ya lo hacían. Google 16/16 y auth 9/9 en dos corridas. **Siguiente:** reverificación independiente de Google (GO-07/03/10) tras cerrar #109 para evitar interferencia con la compuerta de verificación de correo.

## Corrección — api ronda 6 cerrada (2026-09-06 16:3x)
- **Acción:** #98 (Sonnet) commiteó R6-01..08 (pdfjs-dist sustituye a pdf-parse para xref-stream; radar por lotes; transiciones condicionadas; anidamiento por contrato); #108 (Opus) verificó cada commit y detectó R6-09: el test de rendimiento del radar medía reloj de pared y fallaba en suite completa (2.9 s > 2 s; un techo de 10 s también falló bajo carga); reemplazado por criterio estructural (conteo de sentencias SQL: 22 frente a ~60.000 del N+1). Gate 288/288 ×2 aislado y 326/326 ×2 en el árbol compartido. **Siguiente:** reverificación independiente de api ronda 6 (R6-01..09).

## Integración de correos en la API (2026-09-06 16:5x)
- **Acción:** #109 (Opus, D-10) cerró la integración de `@atiende/mail` en apps/api: verificación de correo (registro, `/auth/email/verify`, reenvío limitado, compuerta configurable en login), recuperación de contraseña de punta a punta, invitación por correo con enlace firmado, contacto público anti-abuso, preferencias aplicadas al enviar, webhook Svix con anti-replay y supresión. **Bug real hallado por las pruebas:** el job `mail_retry` lo rechazaba la política RLS `ins_jobs` para correos sin organización (justo verificación y reset) y el error se tragaba; corregido con migración 0086 (`app.enqueue_mail_retry`, SECURITY DEFINER acotada a ese kind), sin relajar la política. 350/350 ×2, db 205/205, 49 casos nuevos con adversariales. **Pendiente externo:** credenciales de proveedor y `RESEND_WEBHOOK_SECRET` (sin ellas: CaptureProvider y webhook 503). **Siguiente:** auditoría #115, handler `mail_retry` en worker + E20 (#116), web 8b al cerrar #113, y pase único de ACEPTACION/TABLERO (incluye ML-07).
