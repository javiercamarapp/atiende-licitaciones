# Criterios de Aceptación — Atiende Licitaciones

Un criterio de aceptación por requisito (o grupo de requisitos estrechamente relacionados) de `docs/REQUISITOS.md`. Tipo de prueba: unit / integración / E2E / adversarial / render.

**Snapshot de gobierno**: la sección de la Ampliación 2 (REQ-172..210, S1–S12) y las filas de REQ-051..055 y REQ-169 fueron reverificadas contra HEAD `9a12173` el 2026-09-06 ~17:3x CST, abriendo el test/log/commit que cubre cada una. El resto de la tabla (REQ-001..171, A1–A15) refleja el estado verificado contra `git log` (HEAD `071263a`, tras el cierre de la ronda 5), `docs/auditoria-1/*.md`, `docs/auditoria-2/*.md` (incl. `api-ronda5.md`, `api-ronda5-reverificacion.md`, `api-r5-09-10-reverificacion.md`, `ronda5-final.md`, `ronda5-final-reverificacion.md`), `docs/PROGRESO.md` completo (rondas 0–5), `apps/api/docs/e11-cobertura.md` y los tests presentes en el repo al 2026-09-06 ~11:2x CST. **Reemplaza el snapshot anterior** (commit `e61156e`, HEAD `b362e62`). Cierre formal: todos los paquetes con código (`packages/agents`, `packages/expediente`, `apps/worker`, `packages/sources`, `packages/db`+`apps/api`, `apps/web`) tienen al menos una reverificación adversarial independiente con veredicto explícito citado más abajo; la ronda 5 (2FA/step-up, E11 estructurado + calendario oficial, procedencia vinculante, correlation-id extremo a extremo, aviso de privacidad, expediente y back office completos desde `apps/web`) suma su propia cadena auditoría→corrección→reverificación independiente, cerrada en `docs/auditoria-2/ronda5-final-reverificacion.md`. API-15/WI-06 (ronda #70, previa a la ronda 5) quedaron REPARADOS sin reverificación adversarial independiente adicional (BAJA, no explotables) — ver nota al pie.

**Escala estricta de Estado** (una de las seis, sin intermedios):
- **PENDIENTE**: sin código que lo implemente.
- **EN_EVIDENCIA**: código + test en verde, auditado o no, pero sin reverificación cerrada, o con hallazgo abierto que lo afecta, o con nota "mock"/"fixture" si la integración real no está verificada.
- **CUMPLIDO**: código + test + auditoría con hallazgos corregidos **y** reverificación independiente con veredicto CERRADO explícito para los hallazgos que lo afectan, sin PARCIAL/NO CERRADO relacionado.
- **BLOQUEADO_EXTERNO**: depende de credenciales/acceso externo no disponible (ComprasMX reCAPTCHA B-02, OpenAI real, Postgres productivo/CI B-03, validación por abogado externo).
- **NO_APLICA_A_PAQUETE**: el requisito no aplica al paquete listado como "dueño natural"; se anota el paquete correcto o la naturaleza (decisión reservada, revisión legal humana, decisión de producto).
- **LÍMITE_ACEPTADO**: hallazgo residual de severidad ≤MEDIA, documentado con causa, tras la "regla de 3 vueltas" de corrección (Likida) — no se abre una cuarta vuelta; se cita el informe donde se aceptó el límite.

Ningún criterio se marca CUMPLIDO por un mock, fixture, o integración simulada; las integraciones reales no verificadas son BLOQUEADO_EXTERNO o EN_EVIDENCIA con nota "mock".

**Conteo total (237 filas = 210 REQ + 15 A + 12 S)**, actualizado 2026-09-10/11 tras el workflow de cierre de ~20 items (ver commits `closure/*` fusionados en `closure/consolidacion-cierre-20-items`). Incluye, entre otros: los 4 motores estadísticos/deterministas de REQ-002/REQ-009/REQ-020/REQ-030/REQ-038 en `packages/agents/src/analytics/` (clasificador CUCoP, P(ganar)/P(desierta), simulador de puntaje, banda de precio; REQ-020 y REQ-038 comparten el mismo motor), `packages/ocr` (`OcrPort` + `TesseractOcrAdapter` real + `FakeOcrAdapter`, ver REQ-014/REQ-018/REQ-129 abajo), el WhatsApp interactivo de doble vía (REQ-074/080/090, `packages/whatsapp` + `apps/api/src/modules/whatsapp`, esqueleto honesto con `verificado_contra_real=false` — ver esas tres filas), REQ-041 (generador determinista de preguntas de junta de aclaraciones con `sources` verificables), el módulo comprador/OIC de REQ-060 (`packages/db` + `apps/api/src/modules/oic/`) y REQ-198 (guardia activa de 0 PII en analítica -- `apps/web/src/lib/analytics/piiGuard.ts` + pruebas adversariales; sigue sin auditoría/reverificación independiente cerrada, por eso EN_EVIDENCIA y no CUMPLIDO). El conteo agregado final de esta fusión se recalcula al cierre de la consolidación (ver nota al pie de esta sección tras el último merge).

Antes de este pase (2026-09-08, tras cerrar REQ-143): 58 · 101 · 64 · 5 · 3 · 6. Las 3 filas que suben de PENDIENTE a EN_EVIDENCIA por el motor OCR (REQ-014, REQ-018, REQ-129) reflejan trabajo nuevo real: el puerto `OcrPort` (contrato agnóstico de motor) + el adaptador real `TesseractOcrAdapter` (`tesseract.js`, 100% local, sin credencial externa, probado de verdad con 17 pruebas en `packages/ocr/test/` incluyendo reconocimiento genuino de texto+bbox contra imágenes generadas en la propia suite) + `FakeOcrAdapter` para pruebas del consumidor, cableados en `apps/api/src/lib/expediente/text-extraction.ts` (imagen suelta → OCR real cuando se pasa un `OcrPort`). Ninguna de las tres queda CUMPLIDO: el criterio LITERAL de cada una exige un gold set de licitaciones reales (REQ-014), un benchmark contra OmniDocBench con adaptadores Mistral OCR/Azure Layout que requieren credencial de proveedor no disponible (REQ-018), o un conjunto formal de "páginas de prueba" que tampoco existe (REQ-129) — ver el detalle de cada fila.

Antes de este pase (2026-09-08, tras cerrar REQ-143): 58 · 101 · 64 · 5 · 3 · 6. Antes de ese pase (2026-09-06 ~17:3x CST, HEAD `9a12173`): 57 · 101 · 65 · 5 · 3 · 6. Antes de ese pase (commit `a644803`, cierre de ronda 5, con la Ampliación 2 recién añadida y todavía sin verificar contra el código construido en las rondas 6–8a): 54 · 54 · 117 · 5 · 3 · 4. El salto de PENDIENTE→EN_EVIDENCIA (−52) **no refleja trabajo nuevo de ese pase**: refleja que el snapshot anterior estaba desactualizado — era el hallazgo **ML-07** (MEDIA, trazabilidad), abierto desde `docs/auditoria-2/mail.md` y reconfirmado sin corregir en `mail-reverificacion.md`, **que ese pase cierra**. Solo 3 filas subieron a CUMPLIDO en ese pase, todas de `packages/mail`, el único paquete de la Ampliación 2 con la cadena auditoría→corrección→reverificación completa. Ver `docs/TABLERO.md` para el detalle de gobierno, defectos abiertos y conteos por épica/paquete.

| REQ | Criterio de aceptación | Prueba | Paquete(s) responsable(s) | Evidencia actual | Estado |
|---|---|---|---|---|---|
| REQ-001 | El Radar ingiere ComprasMX/DOF/OCDS/portales estatales cada hora y deduplica por número de procedimiento normalizado | integración | packages/sources + apps/worker | `discovery-pipeline.ts` + `dedupe/*` (204 tests, `docs/auditoria-1/sources-cierre-definitivo.md`, commit `0bf72d3`); cadencia horaria real en `apps/worker` Scheduler (298 tests, `worker-cierre.md`) | EN_EVIDENCIA |
| REQ-002 | Clasificador CUCoP alcanza precision@5 ≥0.6 sobre gold set de 300-500 procedimientos | unit | packages/agents | Motor determinista construido (TF-IDF léxico sobre `CucopCatalogPort`, sin LLM): `packages/agents/src/analytics/cucop-classifier.ts` (`classifyCucop`/`runCucopBenchmark`); 13 tests en verde (`npm run -w packages/agents test -- test/analytics/cucop-classifier.test.ts`) contra catálogo y gold set **SINTÉTICOS explícitamente marcados** (`test/analytics/fixtures/synthetic-cucop-{catalog,goldset}.ts`), alcanzando precision@5 = 1.0 sobre esos 12 casos sintéticos — demuestra que el pipeline funciona, no certifica el umbral real. Catálogo oficial CUCoP/COG vigente + gold set de 300-500 procedimientos reales anotados a mano siguen PENDIENTES (no existen en el repo, no se fabricaron) — ver `packages/agents/README.md` §Pendientes. | EN_EVIDENCIA (motor completo; calibración real pendiente de datos humanos) |
| REQ-003 | Pipeline de señales tempranas detecta diffs de PAAASOP, vencimientos y desiertas en un set de prueba | integración | packages/sources / apps/worker | Sin evidencia localizada | PENDIENTE |
| REQ-004 | Test estático falla si aparece un `if provider === X` fuera del registro de conectores | unit | packages/sources | `src/connectors/registry.ts` + test estático real, confirmado en cierre definitivo (204/204 tests) | EN_EVIDENCIA |
| REQ-005 | Cada `SourceSnapshot` persiste sha256/URL/cabeceras/`fetched_at` y nunca se reescribe | integración | packages/sources (modelo) + packages/db (persistencia) | `src/types/tender-record.ts` (`SourceSnapshotSchema`); persistencia real en `packages/db` con RLS auditada (156 tests, `db-api-seguridad-reverificacion.md`) | EN_EVIDENCIA |
| REQ-006 | `match_procedures` devuelve resultados con RLS activa (pgTAP) y precision@k/nDCG medibles contra gold set | integración | apps/api + packages/db | **Motor semántico/pgvector construido esta ronda** (esqueleto honesto, `verificado_contra_real=false` hasta tener `OPENAI_API_KEY` real y un gold set real, ninguno de los dos existe en este repo): migración `packages/db/migrations/0099_req006_semantic_matching_pgvector.sql` intenta `create extension vector` y, si no está disponible (confirmado: PGlite -- el motor de test real de todo el repo -- no la empaqueta), cae a fallback determinista sin fallar la migración; tablas `tender_embeddings`/`company_profile_embeddings` con RLS real (`app.apply_org_rls`); RPC SQL `match_procedures(org_id, embedding, limit)` creado condicionalmente cuando pgvector SÍ está disponible, con fallback determinista en TypeScript (`apps/api/src/modules/matching/semantic.ts#matchProcedures`, coseno calculado sobre `embedding_fallback`) cuando no lo está. `EmbeddingProvider` real (`OpenAIEmbeddingProvider`, OpenAI Embeddings API, `packages/agents/src/embeddings/`) + adaptador fake determinista (`FakeEmbeddingProvider`, hashing trick bag-of-words, nunca red) seleccionado automáticamente sin `OPENAI_API_KEY` (mismo patrón que el LLM de agentes). Relevancia combinada real (`blendRelevance` en `engine.ts`): criterio léxico/reglas existente (sin tocar) + nuevo criterio `semantic_similarity` explicable (REQ-168), invariante `sum(criteria.score) === relevance.score` verificado. Embedding de convocatoria usa título + entidad + clasificadores + **texto real de anexos técnicos** (`tender_documents.extracted_text`), no solo metadatos/título. Tests reales (35 nuevos, todos en verde): `packages/agents/test/embeddings/*.test.ts` (26, unit, incl. caso negativo de vectores disjuntos/dimensión inválida/credenciales ausentes), `packages/db/test/req006-pgvector-migration.test.ts` (3, migración en PGlite real), `apps/api/test/matching-engine-blend.test.ts` (6, invariantes del blend), `apps/api/test/req006-semantic-matching.test.ts` (7, integración HTTP+DB real: criterio presente/ausente según texto disponible, uso real de anexos técnicos, caché de embeddings verificada forzando un vector opuesto, aislamiento cross-tenant con RLS real sin filtro explícito, RPC `match_procedures` con fallback determinista y `limit`), más 4 tests nuevos de `tender_embeddings`/`company_profile_embeddings` en el arnés genérico de RLS (`packages/db/test/rls-isolation.test.ts`, ahora 74/74). Suite completa NO regresionada: `packages/db` 228/228, `packages/agents` 289/289, `apps/api` (matching + provenance) 13/13. Comando: `cd packages/agents && npx vitest run test/embeddings` -> `26 passed`; `cd packages/db && npx vitest run` -> `228 passed`; `cd apps/api && npx vitest run test/req006-semantic-matching.test.ts test/matching-engine-blend.test.ts test/matching-and-go-no-go.test.ts test/security-req142-provenance-binding.test.ts` -> `20 passed`. Pendiente honesto: precision@k/nDCG contra gold set real de convocatorias (no existe ese gold set en este repo -- REGLA DE ORO "nunca inventar datos"); la calidad semántica de `FakeEmbeddingProvider` no certifica la de un modelo entrenado real; el índice ANN (`ivfflat`) no se ha verificado contra un Postgres con pgvector real (solo su creación está protegida por manejo de excepción, nunca ejercitada en este entorno). | EN_EVIDENCIA (mecanismo + RLS + fallback determinista; gold set/credenciales reales pendientes) |
| REQ-007 | Resolución de entidades por RFC/Clave UC con alias pasa el set de prueba de deduplicación | unit | packages/sources | No implementado — solo heurística de título/entidad normalizados, no RFC/Clave UC | PENDIENTE |
| REQ-008 | El esquema de BD valida contra OCDS 1.1/1.2+MxCnet; CI falla si los esquemas zod/Pydantic divergen | unit | packages/sources | `src/connectors/ocds/ocds-types.ts` (zod, subconjunto OCDS 1.1) | EN_EVIDENCIA (parcial — sin 1.2/MxCnet completo; sin contraparte Pydantic en monorepo JS) |
| REQ-009 | AUC de P(ganar) ≥0.70-0.78 y de P(desierta) ≥0.80 sobre gold set histórico | unit | packages/agents | Motor determinista construido (regresión logística por descenso de gradiente, sin `Math.random()`, sin LLM): `packages/agents/src/analytics/win-probability.ts` (`LogisticRegressionModel`, `WinDesertionEngine`, `computeAUC`); 16 tests en verde (`npm run -w packages/agents test -- test/analytics/win-probability.test.ts`) contra un gold set **SINTÉTICO explícitamente marcado** (`test/analytics/fixtures/synthetic-win-goldset.ts`, 400 casos generados por fórmula conocida + PRNG con semilla fija), alcanzando AUC(ganar)≈0.780 y AUC(desierta)≈0.819 sobre ese fixture — demuestra que el pipeline funciona, no certifica el umbral real. `verificadoContraReal` es siempre `false` salvo bandera explícita del llamador. Gold set histórico REAL de licitaciones con resultado conocido por tenant sigue PENDIENTE: se verificó que el único dataset histórico real del repo (`compras-mx-historico`) no sirve (contratos ya adjudicados a nivel de todo el gobierno, sin la perspectiva de un proveedor específico ni casos desiertos) — ver `packages/agents/README.md` §Pendientes. | EN_EVIDENCIA (motor completo; calibración real pendiente del gold set humano) |
| REQ-010 | Prueba automática rechaza cualquier cifra en el texto del LLM ausente del JSON de entrada | adversarial | packages/agents | Sin test específico para texto libre de LLM redactor; `no-fabrication.ts` cubre outputs de herramientas en general | PENDIENTE |
| REQ-011 | Recalcular Go/No-Go con umbrales configurados reproduce "habrías visto N" sobre las últimas 30 convocatorias | integración | apps/api | Rutas `matching/go-no-go.routes.ts` existen (ronda 2, `cb2bc3f`) y están conectadas en `apps/web` (ronda 3, commit del implementador #42); sin gold set de 30 convocatorias reales para reproducir la métrica | PENDIENTE (gold set) |
| REQ-012 | Cada sub-score del Go/No-Go es auditable hasta su evidencia origen en la UI | E2E | apps/web + apps/api | Ronda 3 (#42) conecta la página Go/No-Go de `apps/web` a `apps/api` real (`docs/logs/web-ronda3.log`, flujo login→org→…→convocatorias); reverificado sin regresión en `reverificacion-final-integrada.md` (98/98 E2E ×2) | EN_EVIDENCIA |
| REQ-013 | El índice de direccionamiento (0-100) se calcula con desglose por señal explicable | unit | packages/agents / packages/sources | Sin evidencia localizada en ningún paquete | PENDIENTE |
| REQ-014 | ≥98% de páginas del gold set quedan con texto y bbox extraídos correctamente | integración | packages/ocr + apps/api | `OcrPort`/`TesseractOcrAdapter` construidos (`packages/ocr`, motor `tesseract.js` real, 100% local); cableado en `apps/api/src/lib/expediente/text-extraction.ts` (imagen suelta con `ocr` real → `extracted` con `words[].bbox` reales por palabra). Probado de verdad contra imágenes generadas en la suite (`test/tesseract-adapter.test.ts`), NUNCA contra el gold set de licitaciones reales que pide el criterio (no existe en el repo). `TesseractOcrAdapter.liveVerification.verified=false` explícito por esto. PDF escaneado sigue en `requires_ocr` (rasterizar PDF a imagen queda fuera de esta ronda, ver `packages/ocr/README.md`) | EN_EVIDENCIA (mecanismo real) / BLOQUEADO_EXTERNO (métrica: sin gold set) |
| REQ-015 | Recall de requisitos atómicos ≥0.85(H)/0.93(M)/0.97(G); ≥0.99 en causas de desechamiento | integración | packages/expediente | `src/requirement-matrix.ts` (esquema atómico determinista), sin gold set anotado para medir recall real | EN_EVIDENCIA (parcial) / BLOQUEADO_EXTERNO (métrica) |
| REQ-016 | 0 citas alucinadas llegan en verde al checklist final sobre el gold set de verificación | adversarial | NO_APLICA_A_PAQUETE → packages/agents | Sin gold set de verificación | PENDIENTE |
| REQ-017 | Diff entre dos versiones reales de convocatoria detecta el 100% de cambios del gold set | integración | packages/expediente + packages/sources | `apps/api/test/tenders-and-ingest.test.ts` (caso A3, HTTP real, invalidación real de `proposals.invalidated_at`); `src/dedupe/version.ts` — sin gold set formal de convocatorias reales | EN_EVIDENCIA (parcial, fuerte) |
| REQ-018 | Benchmark interno de OCR supera el umbral definido sin depender solo de Tesseract | unit | packages/ocr | `OcrPort` (`packages/ocr/src/types.ts`) es intencionalmente agnóstico de motor -- agregar Mistral OCR/Azure Layout el día que exista credencial NO requiere tocar ningún consumidor, y `createOcrPortFromEnv()` NUNCA activa `TesseractOcrAdapter` por defecto (`OCR_ENGINE=tesseract` explícito), precisamente para no volverlo el ÚNICO OCR de producción. Lo que falta y sigue bloqueado: los adaptadores reales Mistral OCR/Azure Layout (credencial de proveedor no disponible, nunca fabricada) y el benchmark contra OmniDocBench en sí (no corrido) | EN_EVIDENCIA (diseño agnóstico + adaptador local real) / BLOQUEADO_EXTERNO (Mistral OCR/Azure Layout + benchmark OmniDocBench) |
| REQ-019 | Parseo de XLSX vía pandas/openpyxl no introduce alucinación numérica en el set de prueba | unit | (sin paquete — parseo XLSX no construido) | Sin evidencia | PENDIENTE |
| REQ-020 | El simulador de puntaje determinista valida que la suma de rubros sea 50 o 60 en 100% de los casos de prueba | unit | packages/agents | Motor determinista construido: `packages/agents/src/analytics/score-simulator.ts` (`validateRubric`/`simulateTechnicalScore`); 19 tests en verde (`npm run -w packages/agents test -- test/analytics/score-simulator.test.ts`) verifican en el 100% de los casos de prueba (rúbricas de 50 y 60 puntos válidas, sumas de rubro incorrectas, total inválido ≠50/60, técnica+económica≠100, rubro con maxPoints≤0, keys duplicadas) que `validateRubric` detecta cada violación sin excepción. | EN_EVIDENCIA |
| REQ-021 | Suite de evals reporta precisión de requisitos ≥0.90, citas 1.00 y alucinación 0 sobre gold set de 100-300 convocatorias | integración | (transversal — sin gold set) | Requiere gold set de 100-300 convocatorias anotadas, no existe | BLOQUEADO_EXTERNO |
| REQ-022 | El semáforo de 4 estados de la bóveda documental clasifica correctamente casos de prueba con fechas límite variadas | unit | packages/expediente (concepto distinto) | `integrity-checklist.ts` usa 3 estados (verde/ámbar/rojo) para el paquete completo, no un semáforo de 4 estados por documento en bóveda persistente | NO_APLICA_A_PAQUETE (parcial — bóveda documental viva no implementada) |
| REQ-023 | Alertas de vencimiento (15/7/2 días) se disparan comparando contra la fecha del acto, no contra "hoy" | unit | packages/expediente | `src/company-data.ts` (`isPast` vs `asOfIso`); EX-EXP-04/13 (offset numéricamente imposible → `NaN`/fail-open) **CERRADO**: offsets restringidos a -12:00..+14:00, `isPast` fail-closed (corrector #31, `5dfae17`/`9845ecf`); reverificado independiente en `expediente-cierre.md` (commit `92e577f`, 391/391) y `verificacion-puntual-final.md` (commit `1270ea6`) sin residuo. Solo veredicto binario vencido/vigente; sin alertas anticipadas 15/7/2 días como feature de UI | CUMPLIDO |
| REQ-024 | Subida cruzada entre tenants falla por RLS; `.key`/`.cer` rechazados por magic bytes | adversarial | packages/db + apps/api / apps/web | RLS: 198 ataques acumulados (622+176+22), 0 fugas sin cerrar (`db-api-seguridad-reverificacion.md`, commit `8edacdb`); magic bytes full-scan + anti zip-bomb (AE-03/AE-05) CERRADO y reverificado sin huecos (`api-expediente-reverificacion.md`, commit `2b0b028`); rechazo cliente de `.key`/`.cer` (WI-02) CERRADO y reverificado (`reverificacion-final-integrada.md`, commit `1ff99fb`) | CUMPLIDO |
| REQ-025 | Onboarding completo en ≤15 minutos medido en prueba de usuario/E2E | E2E | apps/web | Sin flujo de onboarding ni medición de tiempo | PENDIENTE |
| REQ-026 | Un RFC presente en la lista 69-B definitiva suspende automáticamente el alta del tenant | integración | (sin paquete — KYC no construido) | Sin código de cruce contra lista 69-B en ningún paquete | PENDIENTE |
| REQ-027 | 0 afirmaciones sin `sources[]` pasan el guardrail `no_unsourced_claims` en el set adversarial | adversarial | packages/agents | `no-fabrication.ts` (AG-10); todos los bypasses conocidos (AG-19/20/21/22/23: Map/Set/Buffer/base64/ZodPipeline/Branded/claves normalizadas) **CERRADO** y reconfirmado independientemente en `agents-cierre.md` (commit `f3e6a3b`) y `verificacion-puntual-final.md` (commit `1270ea6`, 263/263, 14 ataques sin bypass) | CUMPLIDO |
| REQ-028 | Ningún manifiesto se emite sin `doc_id` de respaldo; hueco de dato bloquea en rojo | unit | packages/expediente | `src/manifest-template.ts` (`ManifestTemplateRegistry`): motor real de `manifest.json` versionado — `registerVersion` exige `legalBasis` no vacío, `variables` tipadas (string/number/boolean/date/enum) y semver estrictamente creciente por `id` (migración de versión rechaza duplicar/retroceder), calcula el `sha256` real de `(legalBasis, variables, body)` (nunca aceptado como input), y rechaza al registrar cualquier placeholder de `body` sin variable declarada o variable opcional sin `defaultValue` que sí aparece en el texto. Toda versión nace `reviewStatus: "needs_human"` (gate `legal-doc` del criterio) — `render()` es fail-closed: bloquea (`ok:false`, nunca lanza ni sustituye por texto inventado) si la plantilla no existe, no está `approved`, o falta/no tipa correctamente cualquier variable (acumula TODOS los huecos, no solo el primero); solo `approveVersion(id, version, approvedBy, approvedAt)` — llamado explícitamente por un humano — cruza el gate, y una versión nueva de la misma plantilla NO reemplaza a la aprobada vigente hasta que también se apruebe explícitamente. Éxito de `render()` siempre trae `docId: {templateId, templateVersion, templateSha256}` (el "doc_id de respaldo" del criterio). Test: `test/manifest-template.test.ts` (21 casos: camino feliz con doc_id, hueco de dato bloquea, acumulación de huecos múltiples, enum inválido, plantilla no aprobada/no existente bloquean, migración de versión sube/baja/duplica, aprobación no re-aprobable, placeholder no declarado se rechaza al registrar, variable opcional sin default en body se rechaza, legalBasis vacío se rechaza, semver inválido se rechaza, enum mal declarado se rechaza, sha256 determinista e independiente del reviewStatus, formateo de fecha con offset explícito) — 444/444 verde en el paquete (`npx vitest run` en `packages/expediente`, incluida la suite completa preexistente sin regresión), `npx tsc --noEmit` y `npx eslint src test` limpios. **Pendiente honesto (no bloquea el avance, es la naturaleza real del gate)**: ninguna plantilla de PRODUCCIÓN (texto/base legal reales de un manifiesto que de verdad se firme) ha sido registrada+aprobada todavía — eso exige que un humano con autoridad legal redacte y confirme el texto real (la cita exacta de artículo vigente sigue sin verificar, mismo bloqueo documentado en REQ-117/BLOQUEOS.md sobre la renumeración LAASSP post DOF 16-04-2025); el `reviewStatus: "needs_human"` de este motor es precisamente el mecanismo que impide que ese hueco se traduzca en texto inventado mientras tanto. Integración de `economic-proposal.ts`/`technical-proposal.ts` (que hoy siguen con texto boilerplate hardcodeado) a este motor queda para una ronda posterior — cambiarlos no formaba parte del criterio de REQ-028 y se evitó para no arriesgar regresión en su cobertura ya cerrada (REQ-029/REQ-157/REQ-158/REQ-164). | EN_EVIDENCIA (motor real completo con tests en verde; sin auditoría/reverificación independiente todavía, y sin ninguna plantilla de producción aprobada por un humano — ver pendiente honesto) |
| REQ-029 | Casos borde de la propuesta económica (redondeo, letra=número) pasan en Decimal sin excepción | unit | packages/expediente | `src/money.ts`, `test/money.test.ts`/`economic-proposal.test.ts`; EX-EXP-07/09 reverificados **CERRADO**; expediente cerrado en su totalidad (413/413 tests, `expediente-cierre.md` + micro-corrección AE, commit `50fb7ea`) | CUMPLIDO (nota: usa `bigint` centavos, equivalente funcional a Decimal) |
| REQ-030 | La banda de precio `[prom×0.60, mediana×1.10]` se calcula igual en 100% de casos reales de prueba | unit | packages/agents | Motor determinista construido (aritmética half-up en centavos `bigint`, mismo patrón que `packages/expediente/src/money.ts`, nunca `number` de punto flotante): `packages/agents/src/analytics/price-band.ts` (`computePriceBand`/`computeMarketResearchStats`); 19 tests en verde (`npm run -w packages/agents test -- test/analytics/price-band.test.ts`) cubren la fórmula, redondeo half-up, banda degenerada (min>max, nunca intercambiada en silencio), rechazo de negativos/montos malformados, montos de escala de contrato gubernamental y el auxiliar de estadística descriptiva sobre observaciones reales. No depende de ningún gold set — es aritmética pura sobre las cifras de investigación de mercado que el llamador entregue (esas cifras, por convocatoria específica, siguen siendo responsabilidad de la extracción de bases/anexos, fuera de este motor). | EN_EVIDENCIA |
| REQ-031 | 25 casos borde de "cantidad con letra" (incl. "veintiuno") pasan en verde | unit | packages/expediente | `src/number-to-words.ts`, `test/number-to-words.test.ts`; EX-EXP-05/12 (fusión de "veintiuno" en centenas/millares/millones) **CERRADO** (corrector #31, `284db62`) y reverificado sin residuo en `expediente-reverificacion-2.md` y `expediente-cierre.md` | CUMPLIDO |
| REQ-032 | Dos tenants con plantilla idéntica generan huella MinHash sobre el umbral y disparan regeneración | adversarial | packages/agents + apps/worker + packages/db | MinHash/LSH real construido esta sesión (no aproximación): shingling por palabras + familia de hash universal + firma de 64 valores + bandas LSH (`packages/agents/src/similarity/minhash.ts`), umbral configurable `DEFAULT_SIMILARITY_THRESHOLD=0.75` (`cross-tenant-detector.ts`, ingeniería razonable, `calibrado_contra_datos_reales=false` documentado en código -- no existe gold-set de colusión confirmada). Esquema real `packages/db/migrations/0099_req032_proposal_section_fingerprints.sql`: `proposal_section_fingerprints` (solo huellas/hashes, nunca texto -- `worker_role` compara ENTRE tenants sin filtro de organización, RLS estándar por tenant para el resto) y `proposal_similarity_flags` (evento de cumplimiento, visible EXCLUSIVAMENTE a superadmin, append-only, ningún tenant puede leer ni forjar uno). Integrado en `proponer_seccion_propuesta` (`apps/worker/src/agents/business-tools.ts`): sobre el umbral regenera una vez con estilo propio y avisa (`collusionRisk` en el output + evento persistido), nunca decide colusión ni bloquea por sí solo. Tests reales en verde contra Postgres real (PGlite): `packages/agents/test/similarity/*.test.ts` (24, incl. caso negativo adversarial y regresión de rango de 32 bits), `packages/db/test/req032-similarity-fingerprints.test.ts` (7, incl. ataque: ningún tenant lee ni inserta un evento de otro), `apps/worker/test/business-tools.test.ts` (+4: positivo cross-tenant con regeneración y evento de cumplimiento, negativo, sección bloqueada nunca se evalúa, mismo tenant nunca se marca contra sí mismo). Comando: `npx vitest run` en cada paquete — verde. PENDIENTE honesto: el umbral es un parámetro de ingeniería sin calibrar contra casos reales de colusión confirmada (no fabricado, documentado explícitamente). | EN_EVIDENCIA |
| REQ-033 | Constraint `UNIQUE(procedure_id, lot_id) WHERE status='active'` rechaza el segundo tenant activo en el mismo procedimiento | integración | packages/db | `packages/db/migrations/0006_proposals_pipeline.sql`; packages/db cerrado (156/156 tests) y reverificado sin regresión (`db-api-seguridad-reverificacion.md`) | CUMPLIDO |
| REQ-034 | Revisión de código confirma que ninguna plantilla/precio se comparte entre tenants competidores | unit | packages/db + apps/api + apps/worker | Cubierto tangencialmente por el aparato RLS general (198 ataques, 0 fugas abiertas) MÁS, desde esta sesión, un mecanismo dedicado a la mitad "plantilla": el detector MinHash cross-tenant de REQ-032 (`packages/db/migrations/0099`, `packages/agents/src/similarity/*`) compara activamente el texto generado entre organizaciones y avisa/registra un evento de cumplimiento cuando coincide por encima del umbral -- ver evidencia de REQ-032 arriba. La mitad "precio" (compartir tarifas/montos entre tenants) sigue sin revisión ni detector dedicado como artefacto propio. | EN_EVIDENCIA (parcial) |
| REQ-035 | La tabla `proposal_facts` está poblada para el 100% de datos renderizados en un paquete de prueba | integración | packages/db | Tabla real `packages/db/migrations/0099_req035_proposal_facts.sql` (RLS multi-tenant vía `app.apply_org_rls`; CHECK `doc_id` no vacío/no-blanco = "campo sin sources = bug" reforzado en el esquema, no solo en aplicación; CHECK ata `page` a `source_kind` -- una cita de bases `clause` siempre trae página, un dato interno `company_data` nunca declara una que no tiene). Wiring real (de dónde salen los hechos): `apps/api/src/lib/expediente/proposal-facts.ts` (`recordProposalFacts`), invocado desde ambos handlers de generación (`proposal.routes.ts`) a partir de los `SourceRef` REALES que ya produce `TechnicalProposalBuilder`/`EconomicProposalBuilder` (@atiende/expediente) -- nunca un `doc_id` fabricado en esta capa; reemplaza por completo los hechos de una sección al regenerarla (sin residuos si un requisito deja de resolver). Cobertura verde: `packages/db/test/proposal-facts.test.ts` (6/6: constraints de esquema, upsert por `fact_key`, rol viewer bloqueado); `proposal_facts` añadida a `DOMAIN_TABLES` -- `packages/db/test/rls-isolation.test.ts` en verde (72/72); `apps/api/test/expediente-proposal.test.ts` en verde (7/7, incluye 2 pruebas REQ-035 de punta a punta vía HTTP real: técnica puebla 1 hecho con el `doc_id` real de la capacidad aprobada y 0 hechos en la sección bloqueada/PENDIENTE; económica puebla 1 hecho con el `doc_id` real de la tarifa aprobada tras aprobarla y 0 hechos mientras A8 bloquea sin total parcial). Suite completa de `packages/db` (233/233 vía `npx vitest run`) sin regresión. Sin auditoría/reverificación independiente todavía. | EN_EVIDENCIA |
| REQ-036 | Dashboard mide tasa de correcciones humanas por paquete contra los umbrales (<5%/<15%) | integración | apps/web | `PanelPage.tsx` sigue siendo `EmptyState`; no está en la lista de módulos conectados en ronda 3 (`apps/web/README.md`) | PENDIENTE |
| REQ-037 | `AuditReport{blocking[]}` vacío es condición necesaria para crear `approval_request`, verificado en casos de prueba con y sin bloqueos | integración | packages/agents/src/audit/{gates,judge,audit-report}.ts | Implementado: `runAuditGates` ejecuta SIEMPRE los 5 gates deterministas de la fila anterior en orden fijo, sin llamar a ningún LLM; `buildAuditReport` solo invoca al juez LLM (`AuditJudge`, temperatura 0, esquema zod `{resumenEjecutivo, advertenciasAdicionales}`) cuando `blocking=[]`, y el juez solo puede aportar `warnings` (nunca `blocking`) — `assertApprovalRequestAllowed`/`AuditBlockedError` son la única vía de decisión. Casos de prueba con bloqueos (`test/audit/audit-report.test.ts`: "el juez LLM NUNCA se invoca", "acumula bloqueos de varios gates a la vez") y sin bloqueos ("blocking=[] y el juez SÍ se invoca") en verde: `npx vitest run` → 298/298 (packages/agents), `npx tsc --noEmit` limpio, `npx eslint src test` limpio, cobertura de `src/audit` 99.65% líneas/100% funciones. **Nota "mock" (regla de oro, nunca fabricar cumplimiento)**: el juez se ejercita contra `FakeProvider` (adaptador fake explícito, nunca mockea la lógica de negocio del gate/parseo, solo el borde externo del LLM); la integración real contra `OpenAIResponsesProvider` sigue sin credenciales (mismo límite que REQ-124/REQ-039), y la calibración contra un gold-set anotado por humanos (REQ-038/REQ-127) sigue sin existir — `AUDIT_JUDGE_CALIBRATED_AGAINST_REAL_ANNOTATORS = false` declarado explícito en `judge.ts`, no se marca CUMPLIDO por esto (regla de la cabecera de este archivo) | EN_EVIDENCIA (nota "mock": gates deterministas 100% reales y cerrados; juez LLM real en estructura/determinismo/esquema, sin calibración ni integración real ejercitada) |
| REQ-038 | Exactitud del simulador de evaluador ≥0.85 y 0 falsos "cumple" sobre gold matrix | unit | packages/agents | Motor determinista construido (misma pieza que REQ-020): `runScoreSimulatorBenchmark()` en `packages/agents/src/analytics/score-simulator.ts` rechaza a 0 cualquier rubro con puntos>0 sin evidencia citada (`rejectedUnsourcedClaim`, "0 falsos cumple") y exige exactitud ≥0.85 Y 0 rechazos para pasar `meetsAcceptanceThreshold`. Probado contra un gold matrix **SINTÉTICO explícitamente marcado** (`test/analytics/fixtures/synthetic-score-goldmatrix.ts`, 10 casos inventados a mano, incluidos 2 que plantan un falso "cumple" a propósito): sobre el matrix completo exactitud=100% pero `meetsAcceptanceThreshold=false` porque detecta los 3 falsos "cumple" plantados (demuestra que la puerta combina ambas condiciones de verdad); sobre el subconjunto curado (sin los intentos de falso cumple) exactitud=100% y `meetsAcceptanceThreshold=true`. El gold matrix REAL de una convocatoria evaluada por un comité real (REQ-021) sigue PENDIENTE — no se fabricó. | EN_EVIDENCIA (motor completo; calibración real pendiente del gold matrix humano) |
| REQ-039 | Configuración confirma que el modelo juez es distinto del modelo redactor | unit | packages/agents | `src/llm/router.ts` separa roles (`auditor_juez`≠`redactor_legal`) por diseño; ahora además `AuditJudge` (`src/audit/judge.ts`) SIEMPRE enruta el componente `auditor_juez` a través de `ProviderRouter` (test dedicado nuevo: `test/audit/judge.test.ts` "REQ-125: enruta por el componente de tolerancia cero auditor_juez" — un proveedor sin sede en EE.UU. es rechazado en vez de usarse), lo que hace la invariante de enrutamiento estructuralmente imposible de saltarse desde el juez. Sigue pendiente lo mismo que antes: test contra la CONFIGURACIÓN REAL de producción con credenciales (qué `model` string concreto usa cada rol en `apps/worker` desplegado) — `apps/worker` todavía no conecta `ProviderRouter`/`AuditJudge` a sus tool_calls reales | EN_EVIDENCIA (parcial) |
| REQ-040 | Checklist de "sala de guerra" se ejecuta completo antes de cada apertura simulada | E2E | packages/expediente + apps/api + apps/web | **2026-09-10**: `WarRoomChecklist` real (`packages/expediente/src/war-room-checklist.ts`) con las 4 dimensiones literales del REQ (`checklist_anti_desechamiento` reutiliza `IntegrityChecklist`/`PackageAssembler` reales, nunca infiere "verde"; `hash_zip` reutiliza `verifyManifest` sobre el ZIP REAL en disco, AE-06; `cuenta_regresiva`/`holgura_24h` sobre `tenders.submission_deadline` y el reloj real del servidor, fail-closed ante fecha sin offset explícito). Persistido append-only en `war_room_checklist_runs` (migración `packages/db/migrations/0099_req040_war_room_checklist.sql`, RLS con políticas de solo SELECT/INSERT — sin UPDATE/DELETE). Expuesto en `apps/api` (`GET`/`POST /expediente/tenders/:id/war-room(/run)`, `war-room.routes.ts`) y en `apps/web` (`/entrega/sala-de-guerra`, `SalaDeGuerraPage.tsx`, enlazada también desde la vista general del expediente — "quién lo ve": cualquier miembro con acceso a la convocatoria, incluido `viewer` de solo lectura; "quién lo dispara": un rol de escritura; "cuándo se dispara": manual, las veces que haga falta, cada corrida queda en el historial inmutable). Pruebas reales en verde, incluido el caso negativo: `packages/expediente/test/war-room-checklist.test.ts` (18/18 unit, incluye holgura insuficiente con expediente completo, deadline vencida, fail-closed por fecha sin offset), `apps/api/test/expediente-war-room.test.ts` (7/7 integración: expediente incompleto, expediente completo con 72h de holgura, expediente completo con solo 10h → `holgura_24h` rojo aunque el resto esté verde, deadline vencida, sin deadline fijada, control de rol viewer/writer), `packages/db/test/rls-req040-war-room.test.ts` (5/5: aislamiento multi-tenant, append-only, rol). Comandos y resultado real: `npx vitest run test/war-room-checklist.test.ts` (packages/expediente) → 18 passed; `npx vitest run test/expediente-war-room.test.ts` (apps/api) → 7 passed; `npx vitest run test/rls-req040-war-room.test.ts` (packages/db) → 5 passed; `npx tsc --noEmit` limpio en los 3 paquetes tocados; `npx eslint` limpio en todos los archivos nuevos/editados; regresión dirigida sobre las rutas modificadas (`expediente-package-and-submission.test.ts`, `expediente-checklist-and-approval.test.ts`, `security-ae14-package-status-re-derived.test.ts`) → 7/7 sin cambios. Sin auditoría/reverificación adversarial independiente todavía (el criterio CUMPLIDO de este documento exige esa segunda vuelta) — por eso EN_EVIDENCIA, no CUMPLIDO. Ningún dato externo faltante: `submission_deadline` es un campo ya existente del dominio (REQ-023/AE-01), no un gold-set ni una credencial por conectar. | EN_EVIDENCIA |
| REQ-041 | Preguntas de junta generadas incluyen `sources` verificables en el 100% de casos de prueba | unit + integración | packages/expediente (`generateJuntaQuestions`) + apps/api (`POST/GET /expediente/tenders/:id/junta-questions*`) | **Construido 2026-09-10** (rama `closure/generador-de-preguntas-de-junta-con-sources`): generador determinista (sin LLM -- el criterio de REQ-041 es trazabilidad, no redacción) sobre `Conflict`/`RequirementItem` ya producidos por `RequirementMatrixBuilder`; cada `JuntaQuestion` cita documento+página+texto literal real (`buildVerifiedSource`), con guardrail final (`generateJuntaQuestions`) que descarta -- nunca fabrica -- cualquier candidato sin fuente verificable, dejando constancia en `rejected[]`. Ventana de 24h (`questionsDueAt = juntaAclaracionesAt - 24h`) calculada y probada. Persistencia append-only real en `junta_question_runs` (migración `0100_req041_junta_questions.sql`, RLS sin UPDATE/DELETE, mismo patrón que `war_room_checklist_runs`). Tests: `packages/expediente/test/junta-questions.test.ts` (10/10, incluye 4 casos adversariales -- fuente inválida/vacía en un ítem de un conflicto, item de baja confianza sin texto, propiedad "100% de las preguntas devueltas tiene ≥1 fuente" sobre un lote mixto válido+adversarial) y `apps/api/test/expediente-junta-questions.test.ts` (5/5 integración real contra Postgres: conflicto de plazos real vía 2 documentos subidos, ausencia de preguntas sin conflicto/ambigüedad, 422 en fecha sin offset, RBAC viewer 403, aislamiento cross-org 404). Verificado: `packages/expediente` 433/433, `packages/db` 225/225 (incluida migración 0100 en la cadena completa), `apps/api` typecheck/lint limpios y 460/464 en la suite completa (los 4 fallos son `expediente-renewal-radar.test.ts`, archivo NO tocado por este cambio, ya documentado como flaky por carga en `docs/BLOQUEOS.md` B-10 -- reproducido en aislamiento: 28/28 en verde sin la carga concurrente de la corrida completa). Pendiente para CUMPLIDO: reverificación adversarial independiente (segunda sesión) del guardrail, mismo criterio que el resto de la tabla. | EN_EVIDENCIA |
| REQ-042 | Cada transición de la máquina de estados del paquete emite un evento canónico verificable | integración | packages/expediente (parcial) + packages/db | `package-assembler.ts` (`PackageStatus`: `"draft"`/`"ready"`); ronda 3 de `packages/db` añade `proposal_approval_events` (log reproducible) y esquema de `post_award_followups`, pero la máquina de estados completa del ciclo (≥10 estados incl. descargado/firmado/enviado/cobranza) sigue sin construirse | EN_EVIDENCIA (muy parcial) |
| REQ-043 | Pausa y reanudación de `needs_approval` no ejecuta la acción dos veces ante doble aprobación | integración | packages/agents | `test/agent-runner.test.ts` (resume ejecuta el paso pendiente exactamente una vez); packages/agents cerrado con reverificación independiente (`agents-cierre.md`, `verificacion-puntual-final.md`, 263/263) | CUMPLIDO |
| REQ-044 | Flujo E2E de doble confirmación (1/2 + 2/2 con re-autenticación) completa correctamente y rechaza `always_approve` en precio | E2E | apps/web + apps/api | Guard de doble clic + 409 en aprobación de tarifas (WI-04, CERRADO); ronda 5 añade 2FA TOTP real con step-up de un solo uso atado a `(usuario, org, propósito)` (R5-09, migraciones 0061-0063), límite de tasa + bloqueo progresivo + auditoría de fallos con IP (R5-02/R5-03/R5-10), extendido a la aprobación de `tool_calls` org-scoped y cross-org de superadmin (R5-11), y conectado en `apps/web` con `StepUpDialog` real (RF-01) — verificado en código, HTTP directo y UI real con navegador (`docs/auditoria-2/api-ronda5.md`, `api-ronda5-reverificacion.md`, `api-r5-09-10-reverificacion.md`, `ronda5-final.md`, `ronda5-final-reverificacion.md`, todos con veredicto CERRADO); tests: `security-req044-064-step-up-2fa.test.ts`, `security-r502-r503-twofa-brute-force.test.ts`, `security-r505-stepup-scope.test.ts`, `security-r511-tool-call-stepup.test.ts`. Matiz: solo TOTP/códigos de respaldo, sin passkey/WebAuthn (no bloquea el criterio, que exige re-autenticación, no un factor específico) | CUMPLIDO |
| REQ-045 | CI: grep de patrones DER/PKCS#8 vacío en repo/Storage/logs/traces | unit | transversal (CI) | **Corrección 2026-09-08**: B-03 (bloqueo de CI real) está CERRADO desde el 2026-09-07 (repo público, GitHub Actions gratis e ilimitado) — el job `check-secrets` de `.github/workflows/quality.yml` SÍ corre en cada push real, confirmado en decenas de corridas de esta sesión. No está totalmente "vacío" hoy: falla de forma consistente por un único hallazgo aceptado como falso positivo (una clave de prueba obviamente falsa documentada en prosa en `docs/auditoria-2/worker-agentes.md:476`, ver INC-15 en `docs/BLOQUEOS.md`) — ningún patrón DER/PKCS#8 real detectado. | EN_EVIDENCIA (corre en CI real; 1 falso positivo aceptado sin resolver, no un patrón real) |
| REQ-046 | No existe función/endpoint de envío automático a ComprasMX en el código | unit | packages/agents + packages/expediente + packages/sources | `packages/expediente/test/api-surface.test.ts` (grep propio "cero coincidencias" de envío/firma), reverificado sin regresión en `expediente-cierre.md`/`verificacion-puntual-final.md`; módulo `submissions` de `apps/api` ronda 3 declara la sumisión del usuario sin cliente HTTP saliente ("verificado sin cliente HTTP saliente", `docs/logs/api-ronda3.log`) | CUMPLIDO |
| REQ-047 | Revisión de payloads de WhatsApp confirma ausencia de `.key`/`.cer`/contraseñas/datos bancarios/ZIP final | adversarial | (sin paquete — WhatsApp no construido) | No existe integración de WhatsApp en el repo | NO_APLICA_A_PAQUETE (funcionalidad aún no construida) |
| REQ-048 | El ZIP final solo se genera con checklist en verde/ámbar aceptado y ambas aprobaciones registradas | integración | packages/expediente + apps/api | `package-assembler.ts`; EX-EXP-01/11/17 (hash de insumos como string plano, no vinculante) **CERRADO** vía `InputsHash` branded + `HashedInputs` sellado (corrector #45, `14a35bb`); AE-14 (`/latest`/`/download` no re-derivaban estado) **CERRADO** (`1b73639`); reverificado sin residuo en `expediente-cierre.md` y `reverificacion-final-integrada.md` (A14 CUMPLE) | CUMPLIDO |
| REQ-049 | Suite Playwright cubre las 10 pantallas del portal; acceso cruzado de tenant devuelve 404; axe sin critical/serious | E2E | apps/web | `apps/web/e2e/recorrido.spec.ts` (24 rutas + axe, excede las 10 pantallas); W-01..W-20 CERRADO y reverificado dos veces (`web-reverificacion-2.md`, commit `eeeeaac`: 20/20, axe 0 violaciones en 48 combinaciones); reconfirmado en `reverificacion-final-integrada.md` (98/98 E2E ×2 determinista). Falta guard de ruta que devuelva **404 literal** en acceso cruzado de tenant (W-12, documentado como ausente por diseño: el aislamiento real ocurre por RLS/403 a nivel API, 0 fugas de datos) | EN_EVIDENCIA (fuerte — invariante de "0 fuga" cumplida; falta el mecanismo 404 específico del criterio) |
| REQ-050 | El motor de plazos aplica la ley correcta según la fecha de la convocatoria en casos de prueba de ambos regímenes | unit | packages/expediente (motor legal) + apps/api | Norma verificada (`docs/legal/verificacion-legal.md`); AE-09 (CERRADO, ronda 3) expone `calendarNote`/`legalRegime` con fecha DOF; ronda 5 corrige R5-01 (CRÍTICO: `calendar_holidays` cargado no afectaba el cómputo real ni el bisiesto/TZ, y `calendarNote` mentía afirmando inclusión) — reparado y reverificado dos veces sin bypass (feriado en ventana/fuera de ventana/fin de semana/bisiesto/TZ UTC vs. México, propiedad N=0..3 feriados), `security-r501-calendar-holidays-effect.test.ts`, veredicto CERRADO en `docs/auditoria-2/api-ronda5.md` y `api-ronda5-reverificacion.md` | CUMPLIDO |
| REQ-051 | Máquina de estados del contrato transiciona correctamente en el set de prueba; cifras de dinero en Decimal | integración | apps/api (`expediente/contract.routes.ts`) + packages/db | **Construido en la ronda 6** (despacho #85, migraciones 0065–0070): máquina de estados real con `UPDATE` condicionado por `fromStatus` (409 al re-decidir), historial inmutable (`UPDATE`/`DELETE` directos rechazados) y step-up exigido para `rescindido`. La auditoría `docs/auditoria-2/api-ronda6.md` comprobó en vivo las **78** combinaciones de transición inválidas → 409 y el aislamiento cross-org; la reverificación independiente `api-ronda6-reverificacion.md` confirmó 8 de 9 correcciones por mutación inversa. Abierto que lo afecta: **R6-04** (el test del `UPDATE` condicionado pasa igual sin la corrección — mutación VERDE → R6-10, pendiente) | EN_EVIDENCIA |
| REQ-052 | Extracción estructurada del contrato firmado valida contra un documento de prueba anotado | unit | apps/api (`lib/expediente/text-extraction.ts`) | **Construido en la ronda 6**. La auditoría levantó **R6-01/R6-02 (ALTA)**: `pdf-parse` 1.1.1 no leía PDFs con *xref-stream*, así que la extracción del contrato firmado nunca se probaba con éxito en ese formato — REPARADO migrando a `pdfjs-dist` (`83b806d`). La reverificación lo confirmó por mutación inversa. **Nuevo hallazgo ALTA R6-11** (límites anti «PDF bomb» evaluados DESPUÉS del trabajo: 263 KB = 42 s de CPU) REPARADO en `260a4df` con reproducción real del ataque (20.000 páginas, viejo 52,8 s → nuevo <2 s, `test/expediente-text-extraction.test.ts` caso «R6-11») **pero sin reverificación independiente todavía**. Sin OCR: un PDF sin capa de texto sigue en `requires_ocr` explícito | EN_EVIDENCIA |
| REQ-053 | Matriz de agravios clasifica viabilidad y bloquea "no presentar" en casos de baja viabilidad | unit | apps/api (`expediente/inconformidad.routes.ts`) | **Construido en la ronda 6** (borrador de inconformidad con fundamentos legales verificados). Auditado en `docs/auditoria-2/api-ronda6.md` (rubro «Inconformidad», hallazgos R6-05/R6-06 BAJA) con las citas legales comprobadas una a una contra la fuente oficial; reverificado en `api-ronda6-reverificacion.md`. `test/expediente-inconformidad.test.ts` (cobertura R6-07/R6-08 añadida en `aa16d15`) | EN_EVIDENCIA |
| REQ-054 | Clasificación del motivo de pérdida produce una acción derivada accionable en el set de prueba | unit | apps/api (`expediente/fallo-autopsy.routes.ts`) | **Construido en la ronda 6** (autopsia del fallo). Auditado en `docs/auditoria-2/api-ronda6.md` y reverificado en `api-ronda6-reverificacion.md` (18/18 endpoints post-adjudicación sin fuga cross-org). Nada fabrica texto: cifrado, truncado, vacío y bombas terminan siempre en un estado explícito | EN_EVIDENCIA |
| REQ-055 | Alertas de renovación se disparan en 90/60/30 días antes del vencimiento simulado | integración | apps/api (`expediente/renewal-radar.routes.ts`) | **Radar específico construido en la ronda 6**. La auditoría levantó **R6-03 (ALTA)**: no escalaba a 5.000 contratos (~17–28 s en una sola transacción bloqueante, N+1) — REPARADO por lotes en `b50e45d`; la reverificación lo confirmó (22 consultas para 5.000 contratos frente a ~60.000). **Nuevo hallazgo R6-12 (MEDIA)**: `GET /renewals/alerts` no paginaba (60.000 alertas / 32,4 MB en una respuesta) — REPARADO con keyset y `limit` por defecto en `527eba7`, **sin reverificación independiente todavía**. Abierto: R6-09 (criterio de rendimiento satisfacible con una consulta sin límite) | EN_EVIDENCIA |
| REQ-056 | El motor de calendario legal alerta cuando la convocatoria fija menos plazo del mínimo legal | unit | packages/expediente (motor legal) + apps/api | Ronda 5 construye la tabla real `calendar_holidays` (jurisdicción/año/fecha/fuente+fecha de consulta) + `GET/POST /admin/calendar-holidays` (RLS: lectura abierta, escritura solo superadmin) y alertas de vencimiento (`GET /expediente/post-award-alerts`, `alertLevel`); el bug de cómputo (R5-01) ya está cerrado y reverificado. **Pero la tabla se despliega y permanece VACÍA**: `apps/api/docs/e11-cobertura.md` documenta que la verificación legal de esta ronda no pudo confirmar en línea el calendario oficial de días inhábiles de la SABG — cargarla requiere un administrador humano o una ronda con acceso a la fuente oficial; sin feriados cargados el cómputo real solo excluye sábado/domingo (declarado honestamente en `calendarNote`). Además, "alerta si la convocatoria fija menos plazo del mínimo legal" y los recordatorios T-72/24/6h del texto del requisito no están construidos (alerta binaria vencido/próximo, granularidad día) | BLOQUEADO_EXTERNO (parcial — mecanismo EN_EVIDENCIA con R5-01 cerrado; calendario oficial vacío sin fuente SABG verificable en línea) |
| REQ-057 | pgTAP negativo confirma RLS en el 100% de tablas nuevas | unit | packages/db | `test/rls-isolation.test.ts` (68+ tests generados sobre `DOMAIN_TABLES`); 198 ataques acumulados con 0 fugas sin cerrar (`db-api-seguridad-reverificacion.md`, commit `8edacdb`, reverificación independiente) | CUMPLIDO (Vitest+PGlite adversarial, no pgTAP literal) |
| REQ-058 | pgTAP negativo distingue correctamente tablas públicas (sin tenant_id) de privadas (con RLS) | unit | packages/db | `migrations/0013_source_runs.sql` (tabla pública sin `org_id`); `test/backoffice-extension.test.ts` confirma que no es visible ni para un owner; incluida en el aparato RLS cerrado y reverificado | CUMPLIDO |
| REQ-059 | Eval de aislamiento con tenant sintético "competidor" no produce fuga de datos | adversarial | packages/db + apps/api | 198 ataques acumulados incl. "org fantasma", 0 fugas sin cerrar; reverificado tres veces de forma independiente (`db-api-reverificacion.md` → `db-api-seguridad-reverificacion.md` → `api-expediente-reverificacion.md`) | CUMPLIDO |
| REQ-060 | Test de aislamiento confirma que el producto de lado comprador no expone datos de proveedores propios | integración/adversarial | packages/db (`0099_req060_oic_module.sql`) + apps/api (`modules/oic/`) | **Construido en esta ronda** (closure/req-060): `organizations.kind` ('proveedor'/'comprador') + catálogo de roles y tabla de membresías PROPIOS del lado comprador (`oic_role`/`oic_memberships`, separados de `org_role`/`memberships`) + trigger de exclusión mutua entre ambos catálogos (una organización 'comprador' no puede tener fila en `memberships` ni una 'proveedor' en `oic_memberships`) + tabla de dominio propia `oic_watch_items` (sin FK hacia ninguna tabla privada de proveedor) + capa HTTP paralela (`requireOicOrg`/`X-Oic-Org-Id`, nunca `requireOrg`/`X-Org-Id`). Prueba adversarial real: `packages/db/test/req060-oic-isolation.test.ts` (14 tests, incluye el caso central del criterio -- un actor puramente OIC, iterado contra las ~30 tablas de `DOMAIN_TABLES`, y el caso de un usuario "de doble vida" con membresía en ambos lados que no filtra datos del proveedor operando en contexto OIC) + `apps/api/test/req060-oic-module.test.ts` (8 tests HTTP, incluida la cabecera cruzada `X-Oic-Org-Id`/`X-Org-Id`). Verificado: `packages/db` 239/239 (suite completa, sin regresión) y `apps/api` suite completa en verde (ver comando/resultado real en docs/REQUISITOS.md). HONESTO/PENDIENTE: REQ-058 (hechos públicos compartidos) no existe todavía, así que `oic_watch_items` captura el folio manualmente, sin enganche a un feed público real; tampoco hay flujo de invitación por correo para altas OIC (solo alta directa por `director_oic`) ni UI en `apps/web` -- ninguno de los dos es parte del criterio de aceptación literal (aislamiento), quedan fuera de alcance de esta ronda. Sin auditoría adversarial INDEPENDIENTE todavía (mismo agente que construyó y probó, sin una segunda ronda de revisión por separado) -- por la escala de esta tabla eso es EN_EVIDENCIA, no CUMPLIDO, hasta que exista esa reverificación independiente. | EN_EVIDENCIA |
| REQ-061 | pgTAP y revisión de configuración de vector store confirman ausencia de datos de otros tenants en el contexto del agente | integración | packages/db + packages/agents | **Actualizado esta ronda (ver REQ-006)**: pgvector ya está condicionalmente instalado (migración 0099) y el ÚNICO vector store real del repo hoy (`tender_embeddings`/`company_profile_embeddings`, motor de matching) queda scoped por (tenant, procedimiento) -- `org_id` + `tender_id`, unique constraint -- con RLS real verificada con canary cross-tenant y una query SIN filtro por org_id (`apps/api/test/req006-semantic-matching.test.ts`, caso "AISLAMIENTO REAL"; 4 tests adicionales en `packages/db/test/rls-isolation.test.ts`). Esto resuelve la parte de la garantía que SÍ tiene material que aislar. Sigue PENDIENTE la parte más amplia del requisito literal: ningún agente de `packages/agents` usa vector store para contexto/RAG (few-shot con propuestas de otro tenant) -- no hay ese material que aislar todavía, así que esa mitad del requisito no tiene código que verificar. | EN_EVIDENCIA (aislamiento del vector store de matching, único que existe hoy) / PENDIENTE (vector store de contexto de agente, aún no construido) |
| REQ-062 | Ningún rol "administrador" puede saltar la doble confirmación ni subir e.firma en pruebas de autorización | adversarial | packages/agents | AG-17 (`roleCeiling` solo puede bajar el techo, nunca subirlo) CERRADO en `agents-reverificacion-2.md` y confirmado en `agents-cierre.md` (commit `f3e6a3b`); nota explícita del reverificador: "REQ-062 → CUMPLIDO" | CUMPLIDO |
| REQ-063 | Cambiar de tenant reemite el JWT y no permite acceso a datos del tenant anterior | integración | apps/api | `apps/api/src/plugins/auth.plugin.ts` — diseño alternativo (rol resuelto por request contra DB, no claim embebido), propiedad de seguridad equivalente documentada en `db-api.md` §8; 0 fugas cross-org confirmadas en 198 ataques acumulados, reverificado independientemente 3 veces | CUMPLIDO (nota: diseño alternativo, no reemisión literal de JWT por cambio de tenant) |
| REQ-064 | La aprobación económica exige passkey/OTP en un flujo E2E de prueba | E2E | apps/api + apps/web | Mismo mecanismo que REQ-044 (2FA TOTP con step-up de un solo uso, exigido por `POST /company/rates/:id/approve` y `POST /tenders/:id/approval/approve`); flujo E2E real de extremo a extremo con dos actores (`writer`/`admin`) verificado en código, red real y UI en `docs/auditoria-2/ronda5-final.md` (rubro 3) y reconfirmado sin regresión en `ronda5-final-reverificacion.md`; tests citados en REQ-044. Matiz: OTP sí, passkey no implementado (fuera de alcance de ronda 5) | CUMPLIDO |
| REQ-065 | Suite Playwright completa en verde; axe sin hallazgos critical/serious | E2E | apps/web | Igual que REQ-049/089: W-01..W-20 CERRADO y reverificado dos veces de forma independiente (`web-reverificacion-2.md`), y una tercera vez integrado (`reverificacion-final-integrada.md`, 98/98 E2E en 2 corridas deterministas) | CUMPLIDO |
| REQ-066 | El archivo de auditoría periódica se genera cada 8 tareas cerradas y un veredicto 🔴 detiene el pipeline de CI/CD | integración | transversal (gobierno/CI) | Sin CI real; el patrón de auditoría cada N tareas existe como proceso manual en `docs/PROGRESO.md`/`docs/AGENTES.md`, no automatizado ni en archivo `docs/audits/YYYY-MM-DD-periodic.md` | PENDIENTE |
| REQ-067 | El widget de ChatGPT App solo permite `callTool` de lectura; no expone documentos firmables | adversarial | packages/expediente (`chatgpt-app.ts`) + apps/api (`modules/chatgpt-app/`) | **Construido esta ronda**: allowlist fijo de 4 herramientas de solo lectura (`list_tenders`, `get_tender`, `get_approval_status`, `get_compliance_matrix`) declarado como regla de negocio en `packages/expediente/src/chatgpt-app.ts` (`assertAllowedChatGptAppTool`, comparación exacta de string — cualquier otro nombre, incluidos `aprobar_propuesta`/`generar_paquete`/`fijar_precio_final`/`sign_contract`/`export_package`, se rechaza ANTES de abrir transacción) más una guarda de última línea `assertNoFirmableDocumentInToolOutput` (escaneo recursivo de la salida contra nombres de campo y patrones de ruta de archivo firmable) — ambas probadas de forma aislada en `packages/expediente/test/chatgpt-app.test.ts` (9/9, incl. 2 adversariales). Cableado real contra Postgres en `apps/api/src/modules/chatgpt-app/` (`tools.ts`: mismas tablas/aislamiento de tenant que `GET /tenders`, `GET /expediente/:id/approval` y `GET /expediente/:id/checklist`; `mcp.routes.ts`: servidor MCP JSON-RPC 2.0 real — `initialize`/`tools/list`/`tools/call`/`resources/list`/`resources/read` — y `GET /chatgpt-app/manifest.json`) con 16/16 tests en verde en `apps/api/test/chatgpt-app-widget.test.ts` (`npx vitest run --root apps/api test/chatgpt-app-widget.test.ts`), incluyendo el caso negativo exigido por el criterio: nombre de herramienta de escritura fuera del allowlist rechazado sin tocar la base de datos, cross-tenant (otra organización) sin fuga de datos ni de `evidenceRef`/enlaces de descarga, argumentos inválidos con `isError:true` (nunca 500 ni dato fabricado), URI de recurso arbitraria/path-traversal rechazada, y los 4 widgets `text/html+skybridge` verificados como autocontenidos (solo leen `window.openai.toolOutput`, nunca `callTool`/`fetch`/recursos externos). `verificado_contra_real=false` explícito (ver cabecera de `mcp.routes.ts` y `chatgpt-app.ts`): NO implementa el flujo OAuth 2.1 con registro dinámico de cliente de la especificación de autorización de MCP que un conector real de ChatGPT completaría — depende de publicar la App en el directorio de desarrolladores de OpenAI (credencial/aprobación que este entorno no tiene); la autenticación real usada es el mismo JWT bearer + `X-Org-Id` del resto de `apps/api`. Esto NO es B-02 ni B-04 (verificado contra `docs/BLOQUEOS.md`): es un tercer bloqueo externo distinto, propio de este requisito, que no impide verificar el criterio literal (el allowlist de lectura y la ausencia de documentos firmables son 100% comprobables sin ese registro). Sin auditoría/reverificación independiente todavía | EN_EVIDENCIA (BLOQUEADO_EXTERNO parcial: registro OAuth/directorio de OpenAI para un conector real, no implementado ni implementable sin esa credencial) |
| REQ-068 | Toda herramienta con efecto externo lleva `needs_approval=True` | unit | packages/agents | `ToolDefinition.riskLevel`/`declaredEffects` + `AuthorizationPolicy`, `test/tool-registry.test.ts`; AG-05 (handler que miente en `riskLevel`+`actionKind`+`declaredEffects` simultáneamente evade el gate) permanece como **límite arquitectónico reconocido** tras 3 vueltas de corrección — documentado en `agents-cierre.md`/`verificacion-puntual-final.md`, mitigación delegada a `apps/api` (checklist humano/sandbox), no implementada | LÍMITE_ACEPTADO (AG-05, causa: mentira simultánea en 3 metadatos no detectable dentro de una librería pura) |
| REQ-069 | Validación de esquema (zod/Pydantic strict) rechaza llamadas de herramienta que intenten fijar precio/tarifa | unit | packages/agents | `src/tool-registry.ts` (zod strict); mismo límite AG-05 que REQ-068 | LÍMITE_ACEPTADO (AG-05) |
| REQ-070 | Revisión de código confirma que el grafo de orquestación está definido por código, no por decisión del LLM | unit + integración | apps/worker (sobre `packages/agents`) | Grafo completo Radar→Analista→Redactor→Auditor→Mensajero construido y encadenado por CÓDIGO (`ORCHESTRATION_NEXT_STAGE`/`advanceOrchestrationGraph`, `apps/worker/src/handlers/run-agent.ts`): Radar→Analista ya estaba (`enqueueAgentEventsForIngestResults`, `discover-tenders.ts`); Analista→Redactor queda humano-gated a propósito (REQ-071: `redactor_borrador` necesita `sectionKeys`/decisión "go" que ningún dato decide solo); Redactor→Auditor y Auditor→Mensajero se autoencaden aquí, este último condicionado a `computeAuditReport(...).blocking.length === 0` (recalculado en código sobre datos reales, nunca leído de un tool_call). Auditor = `auditar_expediente`/`computeAuditReport` (`apps/worker/src/agents/audit-report.ts`, determinista sobre `requirement_items`/`proposals`/`proposal_sections` reales, nunca fabricado). Mensajero = `notificar_expediente_listo` → job `send_expediente_notification` (correo real, plantilla `pending-approval` ya existente pero huérfana). Radar probado de punta a punta con un conector SINTÉTICO/offline explícito (`createFixtureOfflineConnector`, `packages/sources/src/connectors/fixture/`, `liveVerification.verified=false` + `synthetic=true` PERMANENTE — nunca se finge verificación real; B-02 sigue ABIERTO para las 5 fuentes reales de producción, que `buildDefaultConnectorRegistry()` nunca incluye este conector). Migración `packages/db/migrations/0099_req070_orchestrator_grants.sql` (grant+RLS de `proposal_sections` para `worker_role`, mismo patrón que 0098; actualiza el test exhaustivo de grants de `worker_role` en `packages/db/test/worker-role-and-job-proposals.test.ts` y su copia en `apps/worker/db-proposals/PROPOSAL-03-worker-role.test.ts`). Evidencia real: `packages/sources` → `npx vitest run` (210/210, incl. `test/connectors/fixture.test.ts` 6/6 y el escaneo estático SR-12 sin nuevos offenders); `apps/worker` → `npx vitest run test/audit-report.test.ts test/business-tools.test.ts test/named-agents.test.ts test/run-agent-handler.test.ts test/send-expediente-notification.test.ts test/discover-tenders-fixture-radar.test.ts db-proposals/PROPOSAL-03-worker-role.test.ts` (9+22+17+13+5+2+7 = 75/75, incluye el caso negativo: redactor sin evidencia real detiene la cadena antes de Auditor/Mensajero, y el caso auditor-bloqueado: `sin_expediente` real detiene la cadena antes de Mensajero); `packages/db` → `npx vitest run test/worker-role-and-job-proposals.test.ts` (14/14). Typecheck/lint limpios en `apps/worker`, `packages/sources` y `packages/db`. Nota honesta: bajo una corrida COMPLETA y simultánea de las 3 suites (>25 archivos PGlite en paralelo en un host compartido con carga externa) `send-agent-alert.test.ts` (archivo preexistente, no tocado por REQ-070) expiró por el mismo `testTimeout` de 5000ms documentado como flake conocido en `mail-retry-handler.test.ts`/WK6-03 — no reproduce en ninguna corrida aislada de este cambio. | EN_EVIDENCIA (falta reverificación adversarial independiente de otra sesión; Radar real sigue BLOQUEADO_EXTERNO por B-02, documentado explícitamente, no fingido) |
| REQ-071 | Revisión de código confirma que las transiciones de estado pasan por el reducer transaccional, nunca ejecución directa del agente | unit | packages/db / apps/api | `packages/agents` no toca base de datos directamente (solo interfaces `RunStore`/`ToolCallStore`, ahora con implementación Postgres real en `apps/api`); `apps/api` ronda 3 implementa derive-on-read (`deriveCurrentManifest`, AE-14 CERRADO) y triggers de invalidación (`enforce_approved_rate`, DB-02/03/05) como mecanismo equivalente de reducer transaccional, reverificado sin huecos | EN_EVIDENCIA (apps/api + packages/db) |
| REQ-072 | Suite de ≥200 prompts adversariales anticolusión: detección ≥99%, falsos positivos ≤2% | adversarial | packages/agents | `test/guardrails.test.ts` (AG-12); suite independiente de vocabulario (≥30 casos) mide detección real **~3% (1/31)**, muy por debajo del ≥99% exigido; tras 3 vueltas de corrección el guardrail regex se documenta como capa mínima (falta capa LLM/juez en `apps/api`, no implementada) — límite aceptado explícitamente en `agents-cierre.md` | LÍMITE_ACEPTADO (AG-12, causa: guardrail regex sin capa LLM; objetivo numérico del REQ no alcanzable con el diseño actual) |
| REQ-073 | pgTAP: doble INSERT con la misma `idempotency_key` produce una sola fila | unit | packages/db + packages/agents | `packages/db/test/idempotency-and-audit.test.ts`; `packages/agents/test/idempotency.test.ts` (AG-13 CERRADO, `b5c8a38`); ambos paquetes cerrados con reverificación independiente | CUMPLIDO (Vitest+PGlite, no pgTAP literal) |
| REQ-074 | Doble entrega del mismo `wamid` es tratada como no-op | integración | packages/whatsapp + apps/api | `WamidReplayGuard`/`InMemoryWamidReplayGuard` (`packages/whatsapp/src/webhook/replay-guard.ts`, idempotencia PERMANENTE por `wamid`, sin ventana de tiempo — a diferencia del guardia de Svix de `@atiende/mail`) + `PgWamidReplayGuard` sobre `whatsapp_webhook_events_seen` (migración `0099_req090_whatsapp_interactive_webhook.sql`) en `POST /webhooks/whatsapp`; E2E real `apps/api/test/whatsapp-webhook.test.ts` → "ADVERSARIAL (REQ-074): reenviar la MISMA entrega (mismo wamid) es un no-op" (reenvía el mismo body firmado dos veces: `processed:0` en la segunda, una sola fila en `go_no_go_decisions`); unit en `packages/whatsapp/test/webhook/replay-guard.test.ts`. `verificado_contra_real=false`: sin credenciales reales de Meta, nunca se ha recibido una doble entrega genuina del proveedor — mecanismo completo y probado, falta la confirmación contra Meta real (ver `packages/whatsapp/README.md`) | EN_EVIDENCIA |
| REQ-075 | Mensaje en cola permanece oculto durante el visibility timeout y reaparece si no se archiva | integración | apps/worker | `src/queue/job-queue.ts` (lease+heartbeat); WK-01/02/14 (fencing token, doble ejecución de zombie) **CERRADO** y reverificado con 4/4 ataques sin bypass en `worker-cierre.md` y `verificacion-puntual-final.md` (298/298) | CUMPLIDO |
| REQ-076 | Configuración del crawler limita la concurrencia a 1-2 conexiones por portal en prueba de carga | unit | packages/sources | `src/http/http-client.ts` (`concurrencyPerHost:2`); `test/host-throttle.test.ts`; packages/sources cerrado (204/204, residuo SR-25 no relacionado con este criterio) | CUMPLIDO |
| REQ-077 | Política de reintentos aplica backoff exponencial ante 429/5xx y pausa total ante 403 repetidos | integración | packages/sources | `src/http/http-client.ts` (retry/backoff); `test/http-client.test.ts`; paquete cerrado, sin residuo relacionado | CUMPLIDO |
| REQ-078 | Cascada de modelos (caro→medio→barato) se activa correctamente ante fallos simulados 429/5xx | integración | packages/agents | README de `packages/agents` declara esto como pendiente; `ProviderRouter` resuelve el proveedor pero no la cascada dinámica entre niveles | PENDIENTE |
| REQ-079 | Configuración del rate limiter respeta ≤1 req/s y robots.txt en prueba contra fixture de portal | unit | packages/sources | `src/http/host-throttle.ts` (`minIntervalMsPerHost:1000`); `termsNote` por conector; paquete cerrado, sin residuo relacionado | CUMPLIDO |
| REQ-080 | Mensajes de WhatsApp de prueba nunca exceden 3 botones ni 10 elementos de lista | unit | packages/whatsapp | `packages/whatsapp/src/provider/content-limits.ts` (`validateButtonPayloads`/`validateInteractiveListMessage`, límites reales de la Cloud API: ≤3 botones QUICK_REPLY, ≤10 filas de lista EN TOTAL sumando secciones, más límites de longitud de título/descripción) exigido por AMBOS proveedores (`CaptureProvider` y `MetaCloudProvider`) antes de tocar la red — "test de contrato de contenido de mensajes" en `packages/whatsapp/test/provider/content-limits.test.ts` (12 casos, incluye el límite exacto y +1 sobre el límite) y ejercido de nuevo en `meta-cloud-provider.test.ts`/`capture-provider.test.ts` (rechazo SIN llamar a `fetch`). El catálogo cerrado de motivos de No-Go (`apps/api/src/lib/whatsapp/reason-catalog.ts`, 8 filas) se autovalida contra este mismo límite al importarse. Sin auditoría/reverificación independiente todavía (esta ronda es implementación, no auditoría) | EN_EVIDENCIA |
| REQ-081 | Cargas nocturnas usan Batch API; tareas críticas usan `service_tier` prioritario, verificado en configuración | unit | packages/agents | `grep` de "Batch API"/"service_tier" sin resultados en `packages/agents/src` | PENDIENTE |
| REQ-082 | `cita_no_verificada` nunca se renderiza como verde en la UI de prueba | adversarial | apps/web + packages/expediente/agents | Sin componente de UI que muestre citas/claims (módulos "Redacción"/"Revisión" siguen sin conectar, `apps/web/README.md`) | PENDIENTE |
| REQ-083 | pgTAP: intento de UPDATE/DELETE sobre `audit_log` falla; verificación de cadena de hashes pasa en CI | unit | packages/db | DB-06 (`5c0080c`, cadena de hashes verificable) CERRADO y reverificado en PGlite (`db-api-seguridad-reverificacion.md`); "pasa en CI" no cumplido literalmente por falta de CI real (B-03) | EN_EVIDENCIA (fuerte — mecanismo cerrado y reverificado; falta el gate de CI real) |
| REQ-084 | Consulta de auditoría reconstruye completamente quién vio/aprobó/sustentó una decisión de prueba | integración | apps/api + packages/db | `GET /audit-log` y `GET /admin/audit-log` (ronda 4, commits `88b28bb`/`270bbd9`), probados en `ronda4-audit-log.test.ts`; reconstrucción completa (ver/aprobar/sustentar) parcial, no verificada de punta a punta con un caso E2E dedicado | EN_EVIDENCIA |
| REQ-085 | 0 afirmaciones sin fuente pasan el guardrail de salida en el artefacto final de prueba | adversarial | packages/agents | `src/no-fabrication.ts`, `test/no-fabrication.test.ts`; todos los bypasses conocidos (AG-10/19/20/21/22/23) **CERRADO** y reconfirmados en `agents-cierre.md` y `verificacion-puntual-final.md` sin residuo | CUMPLIDO |
| REQ-086 | Test de redacción de PII confirma que las trazas no contienen datos sensibles | unit | packages/agents | AG-07 (`f41d3ce`), `GuardrailEvent` solo persiste `inputHash` sha256 + extracto redactado; reverificado **CERRADO** de forma independiente en múltiples rondas | CUMPLIDO |
| REQ-087 | El pipeline de CI bloquea el merge si los evals bajan del umbral configurado | integración | transversal (CI) + `packages/evals` (nuevo) | **2026-09-11**: `packages/evals` implementa el runner real (`src/runner.ts` + `src/cli.ts`, `process.exit(1)` real) con graders DETERMINISTAS que reutilizan la lógica de negocio real de `@atiende/agents` (`AntiCorruptionGuardrail`, `scanForUnsourcedSensitiveData` AG-10, `AuthorizationPolicy.decide`) — nunca mockeada — más un juez LLM (puerto `LLMJudge`) con `FakeCalibratedJudge` explícito (`calibratedAgainstRealGoldSet: false`, REQ-021 sigue bloqueado) y `OpenAILLMJudge` real (sin credenciales, pendiente). Umbral: 100% con tolerancia cero en las 4 categorías de cumplimiento legal, 80% en la de juicio de calidad. `.github/workflows/quality.yml` job `evals-gate` corre `npm run evals --workspace=packages/evals` y falla el build si algún umbral no se cumple; `packages/evals` también se agregó a la matriz `workspace-checks` (typecheck/lint/test/coverage). Evidencia real corrida en esta sesión: `npm run evals --workspace=packages/evals` → exit 0, "PASS (38/38 casos)"; `npm run test:coverage --workspace=packages/evals` → 69/69 tests, cobertura 99.69%/94.52%/100%/99.11% (líneas/ramas/funciones/statements) sobre umbral 90/80/90/90; pruebas de mutación (`test/graders.test.ts`, `test/cli.test.ts`) confirman que una regresión real (guardrail debilitado, veredicto falseado) sí rompe `overallStatus`. **Pendiente honesto** (no fabricado como resuelto): (1) no se hizo push — CI no ha corrido realmente en GitHub Actions con este cambio (B-03 sigue "EN CURSO"); (2) branch protection de `main` NO está configurada (`gh api repos/javiercamarapp/atiende-licitaciones/branches/main/protection` → 404 "Branch not protected" verificado en esta sesión), así que un check en rojo no bloquea técnicamente el botón "Merge" en GitHub — requiere autorización explícita del usuario para configurar el required status check (ver `docs/BLOQUEOS.md` B-11); (3) el juez de calidad no está calibrado contra el gold set humano real de REQ-021 (no existe). | EN_EVIDENCIA (mecanismo real construido, probado en verde localmente con mutación; falta ejecución real en GitHub Actions tras push y branch protection para "bloquear" literalmente el merge) |
| REQ-088 | Monitoreo confirma cumplimiento de los SLO declarados | integración | transversal (operación) | Sin monitoreo productivo; no hay entorno productivo desplegado | PENDIENTE |
| REQ-089 | Auditoría automatizada (axe u homóloga) del portal no reporta hallazgos critical/serious | render | apps/web | Igual que REQ-049/065: axe 0 violaciones en 48 combinaciones ruta×tema (`web-reverificacion-2.md`), reconfirmado en `reverificacion-final-integrada.md` | CUMPLIDO |
| REQ-090 | Flujo E2E confirma que WhatsApp permite decisiones pero no edición de matriz/propuesta | E2E | packages/whatsapp + apps/api | Flujo interactivo REAL construido de punta a punta sobre la ÚNICA decisión de negocio con endpoint HTTP equivalente hoy (`go_no_go_decisions`, `POST /tenders/:id/go-no-go`, `modules/matching/go-no-go.routes.ts`): plantilla saliente con botones QUICK_REPLY "Go"/"No-Go" (`OutboundWhatsAppMessage.buttonPayloads`, `new-tender-match-notify.ts`/`triggers.ts`) → `POST /webhooks/whatsapp` (`modules/whatsapp/webhook.routes.ts`) verifica firma real de Meta (`X-Hub-Signature-256`), resuelve identidad `whatsapp_phone_e164`→usuario→membresía→rol (nunca solo la firma) y llama a `decideGoNoGo` — la MISMA función que usa la ruta HTTP, extraída para no duplicar lógica de negocio — "Go" decide directo, "No-Go" abre una LISTA interactiva de motivos cerrados (`reason-catalog.ts`, nunca texto libre) y elegir una fila decide con ese motivo; el chat NUNCA toca `requirement_items`/`proposals` (no existe ninguna ruta de escritura hacia esas tablas desde `modules/whatsapp/`). E2E real en `apps/api/test/whatsapp-webhook.test.ts` (16 casos: Go persiste, No-Go abre lista, elegir motivo persiste con el motivo completo, doble entrega no-op, rol insuficiente/número no vinculado/cross-org se ignoran con rastro en `audit_log`, firma forjada/alterada → 401, sin secreto → 503, payload deforme no lanza) + 83 unit en `packages/whatsapp/test/`. `verificado_contra_real=false`: no hay app de Meta, App Secret, ni plantilla con botones aprobada todavía (bloqueo externo, ver `docs/BLOQUEOS.md`/REQ-140) — el 90% construible sin esas credenciales (puerto, lógica de negocio, webhook, autorización, límites de contenido) está completo y probado; falta el ciclo E2E contra el proveedor real | EN_EVIDENCIA |
| REQ-091 | Mensajes de prueba fuera de horario (7:00-22:00) se retienen salvo plazo <24h | unit | (sin paquete — WhatsApp no construido) | Sin integración WhatsApp | PENDIENTE |
| REQ-092 | Guardrail de voz: la palabra "confirmo" nunca ejecuta la herramienta de fijar precio en prueba adversarial | adversarial | packages/agents + apps/api | **Cierre 2026-09-10 (rama `closure/voz-realtime`)**: `packages/agents/src/guardrails/voice.ts` (`classifyVoiceInteraction`) + webhook real `apps/api/src/modules/voice/routes.ts` (`POST /webhooks/voz/:orgId/:toolName`, catálogo cerrado a `listar-convocatorias`/`leer-bases`/`leer-perfil-empresa`/`resumir-cambios-convocatoria`/`programar-recordatorio` — ninguna tool de fijar precio/firma/portal existe en el catálogo). `packages/agents/test/voice-guardrail.test.ts` (23 tests, incl. adversariales "confirmo, fija el precio..."/"firma el documento..." sin ejecución) + `apps/api/test/voice-webhook.test.ts` (16 tests contra Postgres real, incl. 0 jobs encolados en cada intento adversarial) — todos verdes. Sin cuenta real de ElevenLabs conectada (no hace falta: este webhook es el receptor, ver `docs/agente-voz/README.md` §0) | CUMPLIDO |
| REQ-093 | Test de contrato confirma disclosure de IA en los primeros segundos de voz/WhatsApp y transferencia a humano <10s | integración | apps/web (portal) + packages/agents/apps/api (voz, parcial) | `AiDisclosureNote.tsx` cubre "portal" (`ai-disclosure.spec.ts`, 3 tests verdes). **Cierre 2026-09-10**: para voz, `DISCLOSURE_MESSAGE_VOZ`/`RESPUESTA_FIJA_ES_HUMANO` (`packages/agents/src/guardrails/voice.ts`) tienen fuente única y se exponen en `GET /voice/config` para pegarse en el agente de ElevenLabs (probado, `voice-guardrail.test.ts`) — pero NO hay "transferencia a humano <10s" (sin mecanismo construido) ni un test de contrato que confirme que ElevenLabs de verdad reproduce ese texto en una llamada real: ElevenLabs es dueño del loop de conversación completo, este webhook solo se invoca en tool-calls, no en cada turno (ver `docs/agente-voz/README.md` §4, límite explícito). WhatsApp sigue sin construir | EN_EVIDENCIA (portal cumplido; voz parcial — constantes listas y expuestas, sin transferencia a humano ni contrato de conversación real; WhatsApp pendiente) |
| REQ-094 | Confirmar con producto si se requiere app móvil nativa antes de cerrar el alcance de "móvil" como solo WhatsApp+web | — (decisión de producto) | (decisión de producto, no técnica) | `docs/REQUISITOS.md` marca explícitamente "VERIFICAR con el equipo de producto"; sin registro de decisión tomada | NO_APLICA_A_PAQUETE (decisión de producto pendiente) |
| REQ-095 | Escaneo de secretos (tipo gitleaks) limpio en cada corrida de CI | unit | transversal (CI) | **Corrección 2026-09-08**: mismo estado que REQ-045 — B-03 cerrado, `check-secrets` corre automáticamente en cada push real desde el 2026-09-07; no está "limpio" al 100% por el mismo falso positivo aceptado y documentado (INC-15), sin patrones de secreto real detectados. | EN_EVIDENCIA (corre en cada push real; 1 falso positivo aceptado sin resolver) |
| REQ-096 | Tests de verificación HMAC y deduplicación por `event_id` en webhooks entrantes | unit + integración + adversarial | packages/webhooks (nuevo) + packages/mail + apps/api | **Corrección 2026-09-10**: la fila anterior (`grep -rln webhook apps/api/src` → 0 resultados) estaba desactualizada — el webhook de correo (`POST /webhooks/mail/:provider`, REQ-181..195) ya tenía HMAC+anti-replay real, solo que acoplado a `@atiende/mail` (esquema Svix de Resend). Esta ronda lo GENERALIZA a un verificador/middleware reutilizable por cualquier webhook entrante NUEVO: (1) paquete nuevo `@atiende/webhooks` (`packages/webhooks/`) con el núcleo provider-agnóstico — `matchesAnyHmacSignature`/`isWithinTolerance` (comparación HMAC en tiempo constante + ventana de tiempo) y `WebhookReplayGuard`/`InMemoryWebhookReplayGuard` (dedup por `event_id`), `npm run test:coverage` en verde (27/27 tests, cobertura 95.16%/93.1%/88.88%/95.16% líneas/ramas/funciones/sentencias, umbral 85/80/85/85); (2) `@atiende/mail` (`packages/mail/src/webhooks/verify-signature.ts`) REFACTORIZADO para consumir ese núcleo en vez de reimplementarlo — comportamiento idéntico, verificado con su suite completa sin modificar ni un test: `npm run test` → 332/332 en verde, `npm run test:coverage` → 96.51%/85.05%/97.8%/96.51%, por encima del umbral 85/80/85/85; (3) middleware Fastify genérico `apps/api/src/lib/webhooks/hmac-webhook-guard.ts` (`createHmacWebhookGuard`) que aplica las 3 comprobaciones en orden (falla cerrado 503 sin secreto → firma HMAC 401 → replay 409) para CUALQUIER `HmacWebhookScheme`, probado con un esquema de juguete estilo GitHub/Stripe (10 tests, incluye adversariales: cuerpo alterado, secreto equivocado, replay, firma forjada nunca "gasta" el `event_id`, dos proveedores no colisionan) y con un adaptador real `createSvixScheme()` cuya EQUIVALENCIA contra `verifyResendWebhookSignature` de `@atiende/mail` se probó caso por caso (7 tests, `svix-scheme.test.ts`); (4) migración `0099_req096_generic_webhook_replay_guard.sql` (tabla `webhook_events_seen`, función `app.webhook_claim(provider, event_id, tolerance_seconds)`, en la lista blanca de `security-definer-audit.test.ts`) + `PgWebhookReplayGuard` genérico namespaced por `provider`, probado contra Postgres real (PGlite): a nivel SQL (`packages/db/test/req096-generic-webhook-replay.test.ts`, 4 tests: primera vez gana/replay rechazado, dos providers con el mismo event_id NO colisionan, expiración por TTL, 10 reclamos concurrentes → exactamente 1 gana) y a nivel de clase (`apps/api/test/lib/webhooks/pg-webhook-replay-guard.test.ts`, 3 tests). El webhook de correo REAL en producción (`POST /webhooks/mail/:provider`) NO se tocó a nivel de ruta/DB (sigue sobre `mail_webhook_events_seen`/`app.mail_webhook_claim`, 0081) — su suite de integración completa sigue en verde sin modificarse: `apps/api` `npx vitest run test/mail-webhook.test.ts` → 12/12 (incluye los 6 casos adversariales ya existentes: firma forjada, cuerpo alterado, cabeceras ausentes/timestamp fuera de rango, replay exacto, sin secreto configurado, AM-03 email_id incoherente). `tsc --noEmit` y `eslint` en verde en los 4 paquetes tocados (`packages/webhooks`, `packages/mail`, `packages/db`, `apps/api`). **Pendiente, fuera del alcance de esta ronda**: ningún webhook entrante NUEVO (WhatsApp/Meta, etc.) usa todavía este middleware en producción — no existe aún un segundo proveedor real que conectar; `createHmacWebhookGuard`/`PgWebhookReplayGuard`/`webhook_events_seen` quedan listos y probados para cuando exista. | EN_EVIDENCIA (generalizado, probado unit+integración+adversarial en los 4 paquetes; sin un segundo webhook real conectado en producción todavía — no hay ningún otro proveedor entrante construido más allá del de correo) |
| REQ-097 | Suite de red-teaming de inyección de prompt (PDF/WhatsApp maliciosos) integrada en CI y en verde | adversarial | packages/agents / packages/evals (nuevo) / apps/worker / transversal (CI) | **2026-09-10/11** (dos rondas independientes, la segunda amplía y corrige a la primera): (0) primera ronda — `apps/worker/test/prompt-injection.test.ts` (5/5 verde, PGlite real vía `createRunAgentHandler`) confirmó plan de tool_calls FIJO (`buildNamedAgentPlan`, nunca decidido por el LLM), texto malicioso propuesto VERBATIM sin obedecerse, 0 `tool_calls` ejecutados aunque el `LLMProvider` esté 100% comprometido (`completeText()` nunca lee `LLMCompletionResult.toolCalls`), AG-10/no-fabricación cierra en falso un precio "confirmado" sin fuente, y aislamiento entre tenants ante payload de exfiltración. (1) segunda ronda — `packages/evals/src/cases/prompt-injection.cases.ts` (9 casos): override de instrucciones, tags de sistema falsos, Unicode de ancho completo/ancho cero, aprobación humana fingida, cambio de idioma, dentro de documentos de bases/título de convocatoria — corridos contra el `AntiCorruptionGuardrail` REAL. (2) Al construir estos casos se encontró que el guardrail SÍ era evadible por ancho completo Unicode y caracteres de ancho cero insertados dentro de la palabra prohibida: se corrigió `packages/agents/src/guardrails/anticorruption.ts` (AG-24, normalización NFKC + strip de ancho cero antes de comparar) con 6 pruebas de regresión nuevas en `packages/agents/test/guardrails.test.ts` (25/25 verdes) — incluida una que documenta el límite conocido residual (homoglifos entre alfabetos, ver README). (3) `apps/worker/test/prompt-injection-red-team.test.ts` (5 pruebas, verdes) ejercita el pipeline COMPLETO (Postgres real vía PGlite + `createRunAgentHandler`) con título/dependencia/documento de bases maliciosos: confirma que el plan fijo de tool_calls (Ronda 6) nunca cambia por contenido externo adversarial (contención estructural), y documenta EXPLÍCITAMENTE (no oculta) que el guardrail solo revisa el input directo del tool_call, no datos resueltos de la BD como `tender.title` — ver detalle en `packages/evals/README.md` "Cobertura real vs pendiente". (4) Wired en CI: job `evals-gate` + `apps/worker`/`packages/evals` en la matriz `workspace-checks`. **Honestidad explícita**: NO existe hoy ninguna ruta donde un mensaje de WhatsApp entrante llegue a un prompt de LLM en este repo (`packages/whatsapp` solo envía notificaciones salientes) — no se fabricó un caso de "WhatsApp malicioso" contra una integración inexistente; queda documentado como pendiente real para cuando esa ruta exista. Corre automáticamente en CI vía `workspace-checks`/`evals-gate` (`.github/workflows/quality.yml`). | EN_EVIDENCIA (fuerte para documentos de bases/título externos, dos rondas independientes con un hallazgo real corregido en la segunda; WhatsApp fuera de alcance real hoy, documentado y no fabricado; sin reverificación adversarial independiente adicional ni integración LLM real) |
| REQ-098 | CI: grep de patrones DER/PKCS#8 y extensiones `.cer/.key/.pfx/.p12` vacío en todo el repo/Storage/logs | unit | transversal (CI) | Rechazo cliente+servidor de extensiones e.firma (WI-02, AE-03/05) CERRADO y reverificado sin huecos; no es un gate automatizado de CI (B-03, sin CI real) | EN_EVIDENCIA (parcial — mecanismo cerrado; grep automatizado de CI no existe) |
| REQ-099 | Revisión de almacenamiento cifrado y de flujo de decisión confirma minimización de RFC/CURP y ausencia de perfilado automático | integración | packages/db + transversal (política) | `migrations/0011_company_profile.sql` almacena RFC; sin revisión documentada de minimización ni control anti-perfilado | PENDIENTE |
| REQ-100 | Motor de calendario aplica `norma_version`/`articulo_map` correctamente — bloqueado hasta VERIFICAR fecha de vigencia contra el DOF | unit | docs/legal + packages/expediente/apps/api | `docs/legal/verificacion-legal.md`: VERIFICADO-CON-MATIZ (fecha correcta 16-abr-2025); `apps/api` ronda 3 expone `legalRegime`/`calendarNote` con esa fecha (AE-09, CERRADO y reverificado); motor determinista completo que versione ambos regímenes por fecha aún no construido | EN_EVIDENCIA (norma verificada + expuesta parcialmente en API; motor no construido) |
| REQ-101 | La matriz de cumplimiento marca correctamente el campo `causa_desechamiento` en el gold set | unit | packages/expediente | Campo no confirmado en `requirement-matrix.ts`; sin gold set | PENDIENTE |
| REQ-102 | Umbral de precio no aceptable se aplica en casos de prueba — bloqueado hasta VERIFICAR artículo contra el DOF | unit | docs/legal + packages/expediente (motor, no construido) | `docs/legal/verificacion-legal.md`: VERIFICADO-CON-MATIZ (artículo correcto 5-XIV-b, no 2-XI); sin motor | EN_EVIDENCIA (norma verificada con matiz; motor no construido) |
| REQ-103 | Lectura del acta de fallo el mismo día se dispara en prueba de integración — bloqueado hasta VERIFICAR cita legal contra el DOF | integración | docs/legal + packages/expediente (motor, no construido) | `docs/legal/verificacion-legal.md`: VERIFICADO sin reservas de artículo; sin motor ni test de disparo | EN_EVIDENCIA (norma verificada; falta validación por abogado; motor no construido) |
| REQ-104 | Plazo de inconformidad se calcula en el motor — bloqueado hasta VERIFICAR el plazo exacto contra el DOF | unit | docs/legal + packages/expediente (motor, no construido) | `docs/legal/verificacion-legal.md`: VERIFICADO (6/10 días hábiles confirmados); sin motor | EN_EVIDENCIA (norma verificada; motor no construido) |
| REQ-105 | Plazo de pago se calcula en el motor — bloqueado hasta VERIFICAR 17 días hábiles vs. 20 naturales contra el DOF | unit | docs/legal + packages/expediente/apps/api | `docs/legal/verificacion-legal.md`: VERIFICADO-CON-MATIZ (17 días hábiles, Art. 73 nueva ley); `apps/api` AE-09 lo expone en `calendarNote`/`legalRegime` (CERRADO y reverificado sin huecos); motor determinista independiente aún no construido | EN_EVIDENCIA (norma verificada y ya expuesta parcialmente en API) |
| REQ-106 | Porcentaje de garantía de cumplimiento se aplica en el motor — bloqueado hasta VERIFICAR artículo exacto | unit | docs/legal + packages/expediente (motor, no construido) | `docs/legal/verificacion-legal.md`: VERIFICADO-CON-MATIZ (la ley NO fija %, remite a lineamientos SABG no verificados); sin motor | EN_EVIDENCIA (parcial — norma remite a fuente aún no verificada) |
| REQ-107 | Tope de convenio modificatorio de obra (25%) se aplica en el motor — bloqueado hasta VERIFICAR contra el DOF | unit | docs/legal + packages/expediente (motor, no construido) | `docs/legal/verificacion-legal.md`: VERIFICADO-CON-MATIZ — **el tope de 25% fue eliminado por la reforma**, no debe usarse; sin motor | EN_EVIDENCIA (hallazgo relevante: la cifra original ya no aplica) |
| REQ-108 | Simulador de puntos y porcentajes valida contra la rúbrica específica de cada convocatoria de prueba | unit | packages/agents (E6, no construido) | Sin implementación real; norma solo NO-VERIFICABLE-EN-LÍNEA (parcial) | EN_EVIDENCIA (norma parcial) |
| REQ-109 | Plantilla de manifiesto MIPyME incluye los campos obligatorios definidos | unit | packages/expediente | Sin plantilla MIPyME; `resolveExperience`/`resolveCapability` no distinguen estratificación MIPyME | PENDIENTE |
| REQ-110 | Umbral de adjudicación directa por tramo se aplica en el motor — bloqueado hasta VERIFICAR contra el PEF vigente | unit | docs/legal + packages/expediente (motor, no construido) | `docs/legal/verificacion-legal.md`: VERIFICADO-CON-MATIZ el mecanismo (remite al PEF); cifras del PEF 2026 no verificadas; sin motor | EN_EVIDENCIA (parcial) |
| REQ-111 | Matching de fingerprint de entidad detecta interpósita persona en el set de prueba sintético | integración | (sin paquete — compliance/KYC no construido) | Sin código de fingerprint de interpósita persona | PENDIENTE |
| REQ-112 | Job nocturno de KYC negativo genera alerta ante coincidencia con lista 69-B/sancionados | integración | (sin paquete — KYC no construido) | Sin job KYC en el repo; norma verificada legalmente pero sin implementación | PENDIENTE |
| REQ-113 | Ningún manifiesto de prueba se emite sin evidencia y confirmación humana registrada | adversarial | packages/expediente | Patrón general "missing/blocked" de `CompanyDataService` aplica, pero sin motor de plantillas de manifiesto versionado ni cuestionario OTP específico | EN_EVIDENCIA (débil) |
| REQ-114 | Suite de 200 prompts adversariales anticolusión/soborno: detección ≥99%, FP ≤2% | adversarial | packages/agents | Mismo que REQ-072: detección real ~3%, muy por debajo de ≥99%, límite aceptado tras 3 vueltas | LÍMITE_ACEPTADO (AG-12) |
| REQ-115 | Leyenda de disclosure de IA presente en el 100% de artefactos generados en prueba | render | apps/web | `AiDisclosureNote.tsx`; `ai-disclosure.spec.ts` (3 tests verdes) | EN_EVIDENCIA (cubre solo el portal; falta WhatsApp/voz/PDF/escritos a autoridad, inexistentes) |
| REQ-116 | Texto de banderas de riesgo revisado por abogado antes de publicarse (checklist de revisión, no automatizable) | — (revisión humana) | docs/legal (revisión humana) | Checklist de revisión legal humana no existe como documento ejecutado | PENDIENTE |
| REQ-117 | Aviso legal de responsabilidad del licitante presente antes de cada envío de manifiesto en prueba E2E | E2E | apps/web + packages/expediente | Sin E2E, sin flujo de envío de manifiesto (correctamente, dado que el envío está prohibido) | PENDIENTE |
| REQ-118 | Diseño del producto documentado como "alto riesgo" en el registro de riesgos del proyecto | — (revisión de diseño) | docs/legal + transversal (gobierno) | Premisa normativa verificada (México sin Ley General de IA); sin registro de riesgos del proyecto creado | PENDIENTE |
| REQ-119 | Aviso de privacidad publicado y vigente; RLS por tenant sobre tablas de datos personales verificado por pgTAP | unit | docs/legal (aviso) + apps/api (endpoint) + packages/db (RLS) | Norma-base verificada; RLS cubierta por el aparato general de RLS cerrado. Ronda 5 publica `GET /legal/privacy-notice` (público, sin `Authorization`, versionado por front matter, contenido en `apps/api/docs/legal/privacy-notice.md` coherente con `docs/legal/verificacion-legal.md`: LFPDPPP DOF 20-mar-2025, SABG, plazo ARCO 20 días, multas 200-320,000 UMA), consumido real en `apps/web` (`/privacidad`); test `legal-privacy-notice.test.ts` verde, confirmado en `docs/auditoria-2/api-ronda5.md`/`ronda5-final.md`. El propio aviso se marca explícitamente `status: borrador_pendiente_validacion_juridica` — el entregable existe y es honesto sobre su estado, pero **requiere validación por un abogado mexicano antes de afirmarse "vigente" a un cliente real** | EN_EVIDENCIA (entregable real y honesto; pendiente validación jurídica humana para el estado "vigente") |
| REQ-120 | Test confirma redacción de PII antes de cualquier llamada a un proveedor de LLM externo | unit | packages/agents | Sin pipeline de redacción de PII general antes de llamadas a proveedores externos (solo `GuardrailEvent` redacta su propio log, AG-07) | PENDIENTE |
| REQ-121 | Revisión legal de cláusulas de autoría en contratos de cliente — bloqueado hasta VERIFICAR LFDA art. 12 contra el DOF | — (revisión legal) | docs/legal | `docs/legal/verificacion-legal.md`: NO-VERIFICABLE-EN-LÍNEA | PENDIENTE |
| REQ-122 | Revisión legal de cada nueva fuente de scraping antes de activarla en el registro de conectores | — (revisión legal) | docs/legal (norma) + transversal (proceso) | `docs/legal/verificacion-legal.md`: VERIFICADO-CON-MATIZ (distinguir límite penal CPF 211bis de fundamento LGTAIP); sin proceso documentado de revisión por conector | EN_EVIDENCIA (norma) / PENDIENTE (proceso) |
| REQ-123 | Decisión de forma jurídica (S.A.P.I.) documentada y aprobada por el fundador antes del primer cobro | — (decisión reservada) | fundador (decisión reservada) | `docs/legal/verificacion-legal.md` da el insumo (tope S.A.S. ≈$7,678,849.94 MXN, VERIFICADO-CON-MATIZ); decisión en sí no tomada | PENDIENTE |
| REQ-124 | Configuración de modelos por rol (barato/medio/caro) documentada y usada consistentemente en runs de prueba | unit | packages/agents | `src/types.ts` (`ModelTier`), usado en `llm/router.ts`; paquete cerrado con reverificación independiente, sin residuo sobre esta invariante | CUMPLIDO |
| REQ-125 | Auditoría de configuración confirma que los 5 componentes de tolerancia cero siempre enrutan a proveedor EE.UU. | unit | packages/agents | AG-06 (CRÍTICA, `f8ef82f`), `test/llm/router.test.ts`; **CERRADO** (variantes de mayúsculas/espacios probadas, falla cerrado), reconfirmado en las 3 rondas de reverificación posteriores | CUMPLIDO |
| REQ-126 | Evidencia de los 5 gates de cambio de modelo documentada en `tasks/evidence/` antes de cualquier cambio de enrutamiento | integración | packages/agents (lógica) + transversal (gobierno) | `test/llm/router.test.ts` (`evaluateModelGates`, 5 gates probados y reverificados); **el patrón literal `tasks/evidence/<id>/` NO existe en el repo** — la evidencia real vive en `docs/logs/`+`docs/auditoria-1/`+`docs/auditoria-2/` | EN_EVIDENCIA (lógica cerrada; entregable documental literal del REQ no existe) |
| REQ-127 | Verificador de entailment y juez de evals usan un modelo distinto del evaluado, con calibración documentada | unit | packages/agents | `src/no-fabrication.ts` con tests; sin calibración documentada contra anotadores humanos; el hook de juez LLM real se delega a `apps/api`, no construido | EN_EVIDENCIA |
| REQ-128 | `budget_cap` por corrida dispara alerta ante corridas simuladas que lo excedan | integración | packages/agents | AG-08 (`4731139`, CERRADO), `src/budget-ledger.ts`; Batch API/prompt caching (REQ-081) siguen PENDIENTE; sin entorno productivo para verificar en producción | EN_EVIDENCIA |
| REQ-129 | ≥70% de páginas de prueba se procesan con el motor local sin costo marginal | unit | packages/ocr | `TesseractOcrAdapter` es el motor local que pide el criterio: corre 100% en proceso (WASM), sin API key ni llamada a servicio externo -- costo marginal cero por diseño, no solo por configuración. Probado de verdad en `packages/ocr/test/tesseract-adapter.test.ts`. No existe en el repo un conjunto formal de "páginas de prueba" de licitaciones reales sobre el que medir el ≥70% -- el criterio literal (motor Docling/PyMuPDF para PDF nativo) tampoco se construyó en esta ronda, solo el motor local de OCR (imagen suelta), no el de extracción nativa (que ya cubre `pdfjs-dist` en `text-extraction.ts` por otra vía) | EN_EVIDENCIA (mecanismo real, costo marginal cero verificado) / PENDIENTE (métrica formal sobre páginas de prueba) |
| REQ-130 | Decisión de alcance de cuenta del proveedor de LLM documentada como aprobada por el fundador | — (decisión reservada) | fundador (decisión reservada) | Sin registro de decisión ni comunicación al equipo | PENDIENTE |
| REQ-131 | Aviso de privacidad y reporte de transparencia actualizados antes de activar cualquier enrutamiento alternativo | — (revisión legal) | docs/legal + apps/api | El aviso de privacidad de REQ-119 ya existe y es versionado, y menciona la promesa de declarar el enrutamiento alternativo en un reporte de transparencia futuro; pero **no existe ningún reporte de transparencia real ni ningún enrutamiento a un proveedor distinto del principal en el código hoy** — no es una regresión, es funcionalidad aún no activada (`docs/auditoria-2/api-ronda5-reverificacion.md` §6) | EN_EVIDENCIA (aviso versionado existe; reporte de transparencia y el propio enrutamiento alternativo no aplican todavía porque no se han activado) |
| REQ-132 | Conector a ComprasMX pasa pruebas de contrato diarias contra el sitio real (o fixture actualizado) | integración | packages/sources | `compras-mx-historical-csv-connector.ts` + `test/connectors/compras-mx-historical.test.ts` (CSV histórico real conectado, streaming, RFC 4180, encoding); cierre definitivo (`sources-cierre-definitivo.md`); API en vivo devuelve 401/403 protegido por reCAPTCHA (B-02) | EN_EVIDENCIA (mock/fixture para histórico) / BLOQUEADO_EXTERNO (API diaria real) |
| REQ-133 | Conector OCDS/PDN-S6 valida el esquema de respuesta contra OCDS | integración | packages/sources | `src/connectors/ocds/ocds-types.ts`, `test/connectors/ocds.test.ts` sobre fixtures; OCDS-SHCP inalcanzable, PDN-S6 con bot-detection en vivo | EN_EVIDENCIA (fixture) / BLOQUEADO_EXTERNO (real) |
| REQ-134 | Job de verificación cruzada contra el DOF opera sobre un set de prueba de convocatorias | integración | packages/sources | `src/connectors/dof/dof-connector.ts` alimenta el pipeline de fingerprint cruzado; sin job de verificación cruzada explícito y separado | EN_EVIDENCIA (parcial) |
| REQ-135 | Un conector genérico `/edca/...` cubre correctamente los portales estatales con ese patrón en prueba | integración | packages/sources | `src/connectors/state-portal/state-portal-connector.ts` (3 portales configurados); los 3 en `verified:false` → BLOQUEADO_EXTERNO en la práctica | EN_EVIDENCIA (mecanismo) |
| REQ-136 | Decisión de producto documentada sobre si se compromete un conector a PLACSP/TED en el roadmap | — (decisión de producto) | (decisión de producto) | Sin decisión registrada | PENDIENTE |
| REQ-137 | Evidencias de cada tarea completa están presentes en `tasks/evidence/<id>/` | integración | transversal (gobierno) | El patrón real de evidencia usado es `docs/logs/*.log` + `docs/auditoria-1/*.md` + `docs/auditoria-2/*.md` + mensajes de commit, **no** el literal `tasks/evidence/<id>/` | PENDIENTE (patrón literal no seguido; existe patrón funcional equivalente y exhaustivo) |
| REQ-138 | CI de evals de gate se ejecuta y bloquea el merge ante cambios de prompt/esquema/modelo sin evidencia | integración | transversal (CI) | **2026-09-11**: mismo mecanismo que REQ-087 (`evals-gate` en `.github/workflows/quality.yml`, ver esa fila para el detalle completo). `on.push`/`on.pull_request` solo excluyen `paths-ignore: docs/**` (ya existente, sin tocar) — cualquier cambio de prompt (p. ej. `apps/worker/src/agents/business-tools.ts`), esquema (zod en `packages/agents/src/tool-registry.ts`/handlers) o modelo (`packages/agents/src/llm/*`) es código fuera de `docs/**`, así que dispara el workflow completo incluido `evals-gate` automáticamente — sin necesidad de un `paths:` adicional dedicado a "prompt/esquema/modelo". Mismo pendiente honesto que REQ-087: falta ejecución real en GitHub Actions (no se hizo push) y branch protection para que un rojo bloquee el botón de merge en la UI (ver B-11). | EN_EVIDENCIA (mecanismo real: cualquier cambio de código dispara `evals-gate`; falta ejecución real en GitHub Actions y branch protection) |
| REQ-139 | Ninguna tarea marcada `needs-human` se cierra sin aprobación explícita registrada del fundador | integración | transversal (gobierno) | Sin mecanismo formal de aprobación registrada de tareas `needs-human`; las decisiones reservadas (REQ-123/130) simplemente no se han cerrado | PENDIENTE |
| REQ-140 | Estado actualizado de los bloqueos conocidos revisado antes de iniciar el módulo dependiente | — (revisión de gobierno) | transversal (gobierno) | `docs/BLOQUEOS.md` (B-01, B-02, B-03, INC-01..09); proceso seguido consistentemente en cada ronda (ver `docs/PROGRESO.md`) | CUMPLIDO |
| REQ-141 | El esquema de perfil de empresa persiste las 8 categorías por tenant, con CRUD probado por categoría | integración | packages/db + apps/api + apps/web | `migrations/0011_company_profile.sql` (8+ categorías); `apps/api/src/modules/company/routes.ts`; AE-08 (hash de perfil completo, incl. capabilities/experience/signatories) **CERRADO** y reverificado (`api-expediente-reverificacion.md`, `reverificacion-final-integrada.md`); `apps/web` ronda 3 conecta el módulo "Empresa" a datos reales (ya no `EmptyState`, `docs/logs/web-ronda3.log`) | EN_EVIDENCIA (fuerte) |
| REQ-142 | Un dato de perfil sin `owner`/`source`/`updated_at` es rechazado o excluido de matching/expediente en prueba de contrato de datos | unit | apps/api | Ronda 5: `apps/api/src/lib/provenance.ts` sigue registrando `owner_user_id`/`source`/`updated_at` (el servidor fija `source:'manual'` como literal, no falsificable por el cliente), y **ahora sí se aplica**: una capacidad insertada sin pasar por la API (sin `field_provenance`) bloquea el requisito correspondiente en el expediente, y un documento de empresa sin procedencia genera un criterio de elegibilidad `no_evaluable` explícito en matching — `security-req142-provenance-binding.test.ts`, reconfirmado sin regresión en `docs/auditoria-2/api-ronda5.md`, `api-ronda5-reverificacion.md` y `ronda5-final.md` | CUMPLIDO |
| REQ-143 | Una referencia de experiencia sin `doc_id` de respaldo queda excluida del cálculo de elegibilidad en el set de prueba | unit | packages/expediente + apps/api | `company-data.ts` (`resolveExperience`) verifica en RUNTIME (no solo por el tipo) que `evidenceDocId` sea no vacío y corresponda a un documento real y existente vía `getDocuments()`, devolviendo `blocked`/`evidencia_no_verificable` si no (`test/company-data.test.ts`, incl. payloads `as unknown as` que burlan el tipo); `apps/api` (`POST`/`PATCH /company/experience`) rechaza con 422 cualquier `evidenceRef` que no sea el uuid de un `company_documents` real de la organización (`apps/api/test/company-profile.test.ts`) | CUMPLIDO |
| REQ-144 | Un rol sin permiso de edición de una sección del perfil recibe rechazo de escritura en prueba de autorización | integración | apps/api + apps/web | `company/routes.ts` usa `requireOrgRole(...)`; test dedicado ya existía antes de esta revisión (2026-09-08) en `apps/api/test/company-profile.test.ts` (`PUT /company/profile` viewer→403 con verificación de que la fila NO cambió; `POST /company/capabilities` viewer→403; `POST /company/rates` viewer→403) — el hallazgo de `docs/BACKLOG.md`/esta fila estaba desactualizado respecto al código real, no había ningún trabajo pendiente | CUMPLIDO |
| REQ-145 | Asociar una firma a una persona no listada como firmante autorizado es rechazado en prueba E2E del flujo de firma | integración (HTTP, apps/api) | packages/expediente (unidad) + apps/api (integración real) | `company-data.ts` (`resolveAuthorizedSigner`) ya rechazaba correctamente a nivel de librería; ahora también hay integración real de punta a punta vía HTTP: `apps/api/test/expediente-proposal.test.ts` (`REQ-145: resolveAuthorizedSigner de punta a punta vía HTTP...`) ejercita `POST /expediente/tenders/:id/proposal/technical/generate` con mapeos `kind:'signer'` — un rol sin ningún firmante registrado y un firmante registrado pero fuera de vigencia a la fecha del acto quedan bloqueados (`PENDIENTE`, sin inventar firmante), y un firmante autorizado resuelve en texto trazable con su nombre y rol reales | CUMPLIDO |
| REQ-146 | La cadencia de ingesta configurada por fuente se respeta en ejecución real/simulada, distinta entre al menos dos fuentes de prueba | integración | apps/worker | `src/scheduler/scheduler.ts`, `WORKER_SCHEDULE_JSON`; apps/worker cerrado (298/298, `worker-cierre.md`/`verificacion-puntual-final.md`) | EN_EVIDENCIA (sin recarga en caliente) |
| REQ-147 | `source_runs` queda poblado tras cada corrida con fuente, última consulta exitosa, intentos, estado, evidencia y cobertura | integración | apps/worker + packages/db | `src/handlers/discover-tenders.ts` (`recordSourceRun`); WK-22/23 (estados finos colapsados, `updateAgentRunRow` sin `current_user_id`) **CERRADO** en corrección vuelta 3 y reverificado independientemente (`worker-cierre.md`, `verificacion-puntual-final.md`) | EN_EVIDENCIA (fuerte) |
| REQ-148 | Fixtures que simulan fuente caída/CAPTCHA/cambio de interfaz/permisos faltantes producen el estado explícito correspondiente en back office, nunca una lista vacía silenciosa | adversarial | packages/sources + apps/worker | `not_configured`/`captcha_detected`/`interface_changed` explícitos (`ResponseClassifier`); SR-19..24 CERRADO en `sources-cierre-definitivo.md`; **límite aceptado SR-25** (falso positivo por longitud sobre notas DOF genuinas breves, BAJA-MEDIA, causa documentada, regla de 3 vueltas) | EN_EVIDENCIA (fuerte, con límite aceptado SR-25) |
| REQ-149 | Una convocatoria con dato más viejo que el umbral de frescura configurado muestra el badge de obsolescencia calculado contra `source_runs` | integración | apps/worker (dato) + apps/web (badge) | `discover-tenders.ts`/`source-health.ts` (`staleForMs`); `apps/web` ronda 3 conecta `FuentesFrescuraPage.tsx` a `app.source_freshness()` real — confirmado "A4 UI real" en `docs/auditoria-2/web-integrado.md` (ya no demo estática) | EN_EVIDENCIA (fuerte, upgradeado de "demo estática" a conexión real) |
| REQ-150 | El conector del portal oficial vigente solo se marca "activo" con evidencia de verificación puntual documentada | integración | packages/sources | `src/connectors/types.ts` (`liveVerification.verified`), 4/5 conectores en `false` con evidencia documentada; paquete cerrado | EN_EVIDENCIA |
| REQ-151 | Timestamps de convocatoria/versión/plazo se almacenan y muestran correctamente convertidos a America/Mexico_City | unit | packages/sources | `src/util/timezone.ts` (`fromMexicoCityNaive`); `test/timezone-independence.test.ts` (subproceso real, 3 TZ); invariante confirmada en todos los conectores tras cierre definitivo | CUMPLIDO |
| REQ-152 | pgTAP: reingesta de la misma versión produce 0 filas nuevas; una nueva versión del mismo procedimiento produce 1 fila enlazada, sin duplicar el procedimiento | unit | packages/db + packages/sources | `test/backoffice-extension.test.ts` (constraint `unique(org_id, tender_id, source_version)`); `apps/api/test/tenders-and-ingest.test.ts` (caso A2, HTTP real); ambos paquetes cerrados con reverificación independiente | CUMPLIDO |
| REQ-153 | Consulta de historial reconstruye, en orden, todas las versiones/aclaraciones/anexos de una convocatoria de prueba | integración | packages/sources | `src/dedupe/version.ts`, `InMemoryTenderVersionStore.history()`; paquete cerrado | CUMPLIDO |
| REQ-154 | Reprocesar el mismo snapshot de detección de cambios dos veces produce el mismo estado final (idempotencia) | integración | packages/sources | `src/util/hash.ts`/`src/dedupe/version.ts` (canonicalización de arrays); paquete cerrado, sin residuo relacionado | CUMPLIDO |
| REQ-155 | Modificar el plazo/bases de una convocatoria de prueba invalida la matriz/expediente/aprobación dependiente y dispara notificación a los roles responsables | E2E | packages/sources (emisión) + packages/expediente + apps/api | Cascada real cerrada extremo a extremo: `ChangeDetectedEvent` → invalidación de aprobación (EX-EXP-01/11/17 CERRADO) → re-derivación de estado en API (AE-02/AE-14 CERRADO y reverificado); notificación explícita a roles responsables sigue sin implementar | EN_EVIDENCIA (fuerte; falta el mecanismo de notificación) |
| REQ-156 | Cada fila de la matriz de requisitos de bases expone fuente/página/cláusula/obligatoriedad/responsable/fecha/estado; una fila incompleta bloquea el estado "listo" del expediente | integración | packages/expediente + apps/api + apps/web (UI) | `src/requirement-matrix.ts` (7 campos); `apps/api` ronda 3 expone matriz+conflictos vía HTTP real (`expediente-documents-and-matrix.test.ts`, caso A6 CUMPLE); módulo "Análisis de bases" de `apps/web` conectado en ronda 5 contra `/expediente/tenders/:tenderId/...` (`apps/web/README.md`: "no queda ningún módulo de dominio sin conectar"), ejercitado en `expediente-flujo-completo.spec.ts` ("Análisis de bases: sube el documento..."); el bloqueo real de "listo" con expediente incompleto se confirma a nivel de paquete (A14, CUMPLE) más que fila por fila en la UI de la matriz | EN_EVIDENCIA (fuerte, backend+API+UI conectados) |
| REQ-157 | Un dato/tarifa en estado "pendiente de aprobación" es rechazado por el pipeline de redacción de la propuesta técnica/económica en prueba adversarial | adversarial | packages/expediente + packages/db + apps/api | `test/company-data.test.ts`/`economic-proposal.test.ts`; trigger `app.enforce_approved_rate` (DB-02); AE-01 (vigencia derivada en servidor, no por el cliente) **CERRADO** y reverificado sin huecos (caso A8 CUMPLE en `reverificacion-final-integrada.md`) | CUMPLIDO |
| REQ-158 | Un expediente de prueba con un dato/documento/tarifa faltante muestra el bloqueo con el campo exacto pendiente, sin sustituir ni inferir el valor | integración | packages/expediente + apps/api | `test/technical-proposal.test.ts`; EX-EXP-03/12 (requisito "condicional" que aplica sin evidencia desaparecía en silencio) **CERRADO** (corrector #31, `1dc9026`, test de propiedad con 200 casos) y reverificado sin residuo (`expediente-cierre.md`, 0 críticos/altos abiertos); `apps/web` (módulo Expediente, `ExpedientePage.tsx`) conectado desde ronda 5, con panel general por convocatoria y enlaces a cada módulo (`apps/web/README.md`) | EN_EVIDENCIA (fuerte, backend+API+UI conectados) |
| REQ-159 | Un expediente de prueba con al menos un ítem de checklist en rojo/ámbar nunca transiciona a estado "listo" en prueba E2E | E2E | packages/expediente (unit) + apps/api + apps/web | `package-assembler.ts`; EX-EXP-01/03/11/17 CERRADO; A14 = CUMPLE en `reverificacion-final-integrada.md` (`expediente-package-and-submission.test.ts`, HTTP real, más `apps/web` W-16 + `recorrido.spec.ts`) | CUMPLIDO |
| REQ-160 | Cada una de las 7 dimensiones del checklist de integridad tiene un chequeo automatizado independiente con resultado propio | integración | packages/expediente | `test/integrity-checklist.test.ts` (11 tests); EX-EXP-04/13 (`isPast` fail-open ante offset imposible) **CERRADO**; paquete cerrado (413/413, 0 críticos/altos abiertos) | CUMPLIDO |
| REQ-161 | Consultar una versión del expediente reconstruye los insumos exactos (por hash) usados en esa versión | integración | packages/expediente | `src/proposal-version.ts` (`ProposalVersionRegistry`) solo guarda hashes; integridad del hash reforzada por EX-EXP-17 (`InputsHash` branded), pero la reconstrucción de insumos por hash no tiene test dedicado — cobertura de funciones baja, señalada como código subutilizado | EN_EVIDENCIA (débil) |
| REQ-162 | Editar una tarifa/dato/documento ya aprobado tras la aprobación del expediente revoca esa aprobación en el alcance afectado y exige nueva revisión, verificado en prueba E2E | E2E | packages/expediente + packages/db + apps/api | `src/approval-workflow.ts`; DB-03/05 CERRADO; AE-02/AE-08/AE-11 CERRADO y reverificado; A11 = CUMPLE en `reverificacion-final-integrada.md` (`expediente-checklist-and-approval.test.ts`, HTTP real) | CUMPLIDO |
| REQ-163 | Exportar un expediente incompleto de prueba produce un paquete con marca "BORRADOR" visible en portada y manifiesto; solo un expediente completo y validado produce el paquete "listo" | integración | packages/expediente + apps/api + apps/web | `package-assembler.ts` (marca BORRADOR); AE-14 (`/latest`/`/download` re-derivan estado, 409 si no listo) CERRADO; `package-status-badge.tsx` (W-16) probado en `recorrido.spec.ts`; A14 = CUMPLE | CUMPLIDO |
| REQ-164 | Suite adversarial confirma que ninguna salida del sistema contiene un valor no trazable a `Claim.sources[]` o al perfil aprobado | adversarial | packages/expediente + packages/agents | `no-fabrication.ts` (AG-10, CERRADO); gap de `evidenceDocId` en `resolveExperience` (REQ-143) CERRADO — ver REQ-143 | EN_EVIDENCIA (fuerte) |
| REQ-165 | Revisión de código y prueba adversarial confirman que no existe ruta de ejecución que envíe, firme o actúe en un portal sin un `approval_request` explícito y específico previo | adversarial | packages/expediente + packages/agents | `packages/expediente/test/api-surface.test.ts` (CERRADO, sin regresión); A15 = CUMPLE en `reverificacion-final-integrada.md`; `packages/agents` — AG-17..23 CERRADO, pero AG-05 (handler que miente en 3 metadatos a la vez evade el gate) permanece **límite arquitectónico aceptado** | LÍMITE_ACEPTADO (packages/agents, AG-05) — CUMPLIDO para el alcance de packages/expediente |
| REQ-166 | Un campo de elegibilidad sin dato de respaldo nunca produce `elegible=true` en el set de prueba adversarial | adversarial | packages/sources | SR-11 `src/matching/types.ts`/`matching-engine.ts`, `test/matching.test.ts` (elegibilidad y score independientes); paquete cerrado, sin residuo relacionado | CUMPLIDO |
| REQ-167 | pgTAP y eval de aislamiento con tenant sintético "competidor" sobre las tablas nuevas de perfil/expediente no producen fuga de datos | adversarial | packages/db | Mismo aparato RLS de REQ-057/059 extendido a tablas de perfil/expediente, incluido en el aparato cerrado (198 ataques, 0 fugas abiertas) | CUMPLIDO |
| REQ-168 | La API/UI de matching expone relevancia y elegibilidad como dos valores independientes, cada uno con su propio desglose de evidencia, en caso de prueba | integración | packages/sources (motor) + apps/api/apps/web (exposición) | SR-11 separa ambos valores de forma determinista; `apps/api` (`cb2bc3f`) y `apps/web` (ronda 3, página Go/No-Go conectada) exponen ambos scores; sin test dedicado que confirme el desglose de evidencia por score en la UI | EN_EVIDENCIA (fuerte, exposición real desde ronda 3) |
| REQ-169 | Revisión de dashboards confirma que toda métrica derivada de una proyección/estimación lleva una etiqueta visible distinta de una métrica medida | render | apps/web | **Panel real construido en la ronda 7** (commit `0a656da`): `PanelPage.tsx` ya no es un `EmptyState` — muestra 6 KPIs (`KpiCard`), actividad y alertas leídos de endpoints reales, más el checklist de activación de REQ-192. Tests: `PanelPage.test.tsx`, `e2e/dashboard.spec.ts` (KPIs de la organización sembrada + axe). **Honestidad sobre el criterio**: ninguna de las métricas mostradas es una proyección o estimación (son conteos medidos), así que la regla de etiquetado no llega a ejercitarse; no hay test que la fuerce. Sin auditoría de la ronda 7 | EN_EVIDENCIA (el criterio no se ejercita: no hay métricas proyectadas) |
| REQ-170 | Suite E2E navega los 6 módulos de back office y confirma que muestran datos reales, no mocks | E2E | apps/web + apps/api | Ronda 5 cierra los módulos restantes: **Usuarios y roles** (`UsuariosRolesPage.tsx` sobre `GET /organizations/:orgId/memberships` real), **Auditoría** (`AuditoriaPage.tsx` sobre `GET /audit-log` real con filtro y "Ver traza" por `correlationId`), **Aprobaciones cross-org** (`AprobacionesBackofficePage.tsx` sobre `POST /admin/tool-calls/:id/approve\|deny`, con step-up tras la corrección RF-01) — sumados a Fuentes/frescura, Conectores/Jobs/Costos/Incidentes ya conectados desde ronda 3/4; `apps/web/README.md` confirma "no queda ningún módulo de dominio sin conectar". Verificado en código, 238 tests de `apps/api` y en vivo con navegador real (`docs/auditoria-2/ronda5-final.md`, rubro 4). Superadmin sin 2FA → 403 confirmado por test (`ronda4-admin-tool-calls.test.ts`), no reproducido en vivo por límite metodológico (sin ruta HTTP para crear un superadmin) | CUMPLIDO |
| REQ-171 | Consulta de auditoría con un solo `correlation_id` reconstruye la cadena completa convocatoria→matriz→propuesta→paquete→archivo en un caso de prueba de extremo a extremo | integración | apps/api + packages/db + apps/web | Ronda 5: `correlation_id` nace en `POST /internal/tenders/ingest` y se hereda en `tenders`/`tender_versions`/`audit_log` (R5-04, migración 0060); `GET /audit-log?correlationId=` reconstruye perfil→tarifa→propuesta→checklist→aprobación→paquete con la entidad `tenders` presente, sin fuga cross-org; `AuditoriaPage.tsx` conecta el filtro + botón "Ver traza" real en `apps/web`. `correlation-id-e2e.test.ts`, `security-r504-correlation-id-ingest.test.ts`; CERRADO y reconfirmado en `docs/auditoria-2/api-ronda5-reverificacion.md` y `ronda5-final-reverificacion.md`. **Residual documentado, fuera del ámbito de este cierre**: `apps/worker/src/source-runs/source-runs-repository.ts` nunca incluye `correlation_id` en su INSERT a `source_runs` — el primer eslabón (descubrimiento automático, antes de que exista un `tender`) sigue sin id de correlación | CUMPLIDO (para la cadena observable desde la ingesta vía `apps/api`; residual en `apps/worker`/`source_runs` documentado, no bloqueante) |

## Pruebas mínimas obligatorias de la ampliación back office

Exigidas explícitamente por `docs/AMPLIACION-BACKOFFICE.md`. Cada una debe probar el **flujo integrado real** (ingesta/matching/expediente/aprobación end-to-end). Tabla actualizada con la tabla A1–A15 de `docs/auditoria-2/reverificacion-final-integrada.md` (commit `1ff99fb`, reverificación independiente con 2 descendientes, 21 ataques dinámicos adicionales sin bypass), que reemplaza la evidencia mock/unitaria citada en el snapshot anterior (commit `d8e6d29`).

| # | Prueba mínima | REQ relacionados | Tipo de prueba | Evidencia actual (test integrado real) | Estado |
|---|---|---|---|---|---|
| A1 | Nueva publicación de convocatoria | REQ-001, REQ-151, REQ-152 | integración | `apps/api/test/tenders-and-ingest.test.ts` → `it('A1: nueva publicación crea la convocatoria y una versión inicial')`, HTTP real, más fuerte que el mock de `apps/worker` | CUMPLIDO |
| A2 | Duplicado / replay del mismo evento de ingesta | REQ-073, REQ-074, REQ-152, REQ-154 | integración | `tenders-and-ingest.test.ts` → `it('A2: reingestar exactamente la misma versión es un no-op idempotente (0 filas nuevas)')` | CUMPLIDO |
| A3 | Modificación/aclaración con plazo adelantado | REQ-017, REQ-041, REQ-153, REQ-155 | E2E | `tenders-and-ingest.test.ts` → `it('A3: ...adelanta el plazo, crea evento e invalida dependientes')`; detección/invalidación real cubiertas, sin notificación al rol responsable ni UI E2E dedicada | EN_EVIDENCIA (fuerte) |
| A4 | Fuente inaccesible y obsolescencia visible | REQ-148, REQ-149, REQ-150 | adversarial | Backend: `apps/worker/test/discover-tenders-handler.test.ts` (mock); UI real conectada en ronda 3 (`FuentesFrescuraPage.tsx` → `app.source_freshness()`), confirmada en `web-integrado.md`; sin test E2E que asertúe valores reales de `ageSeconds` de punta a punta | EN_EVIDENCIA (fuerte) |
| A5 | Dos clientes con capacidades distintas y cero fuga | REQ-059, REQ-061, REQ-167 | adversarial | `apps/web/e2e/ronda3-flujo-real.spec.ts` (cambio de organización real); cross-org: `apps/api/test/ronda4-memberships.test.ts`, `ronda4-audit-log.test.ts`; 198 ataques acumulados, 0 fugas | CUMPLIDO |
| A6 | Dato ausente o contradictorio | REQ-142, REQ-158, REQ-166 | integración | `apps/api/test/expediente-documents-and-matrix.test.ts` → `it('A6: dos versiones de bases con plazos distintos generan un conflicto escalado')`, HTTP real | CUMPLIDO |
| A7 | Documento o certificado vencido | REQ-022, REQ-023, REQ-143, REQ-160 | integración | `apps/api/test/company-profile.test.ts`, `matching-and-go-no-go.test.ts` (citan "REQ-023, A7"), HTTP real | CUMPLIDO |
| A8 | Precio no aprobado | REQ-029, REQ-157, REQ-164 | adversarial | `apps/api/test/expediente-proposal.test.ts` → `it('A8: propuesta económica con tarifa NO aprobada se rechaza de punta a punta')`, endurecido por `security-ae01-asofiso-server-derived.test.ts` | CUMPLIDO |
| A9 | Anexo obligatorio faltante | REQ-048, REQ-156, REQ-158 | integración | Mecanismo probado vía HTTP real en `expediente-e2e-flow.test.ts` (comentario `// A9`, sin `it()` nombrado a nivel `apps/api`); nombrado explícitamente solo en `packages/expediente/test/integrity-checklist.test.ts` | EN_EVIDENCIA (matiz de nomenclatura, no de cobertura) |
| A10 | Cálculo económico | REQ-029, REQ-030, REQ-160 | unit+integración | Cálculo determinista probado de punta a punta en `expediente-e2e-flow.test.ts` (comentario `// A10`); banda de precio (REQ-030) ahora tiene motor determinista en `packages/agents/src/analytics/price-band.ts` (19 tests en verde, ver fila REQ-030) — todavía no está cableado dentro del flujo E2E de `expediente-e2e-flow.test.ts` (integración entre paquetes fuera del alcance de esta ronda) | EN_EVIDENCIA |
| A11 | Edición que invalida aprobación | REQ-155, REQ-162 | E2E | `apps/api/test/expediente-checklist-and-approval.test.ts` → `it('A11: cambiar un insumo real invalida la aprobación automáticamente')`, reforzado por `security-ae02/ae08/ae11`; reconfirmado desde UI real con dos actores en ronda 5 (`'A11: editar una sección tras aprobar invalida...'`, `expediente-flujo-completo.spec.ts`, `docs/auditoria-2/ronda5-final.md` rubro 2) | CUMPLIDO |
| A12 | Autorización desde rol indebido | REQ-062, REQ-064, REQ-144, REQ-165 | adversarial | `expediente-checklist-and-approval.test.ts` → `it('A12: writer no puede aprobar')`; `ronda3-flujo-real.spec.ts`; reconfirmado desde UI real en ronda 5 (`'A12: el rol writer no puede aprobar el expediente...'`, `expediente-flujo-completo.spec.ts`); passkey/OTP (REQ-064) sigue sin implementar (gap ya declarado, no nuevo — el mecanismo OTP en sí ya CUMPLE, ver REQ-064) | CUMPLIDO |
| A13 | Expediente completo descargable con manifiesto, evidencia y revisión | REQ-048, REQ-159, REQ-161, REQ-163 | E2E | `apps/api/test/expediente-package-and-submission.test.ts` → `it('A13: expediente completo ensambla "ready"')`, HTTP real; ronda 5 conecta `PaqueteDescargablePage`/`EntregasPage` y agrega `expediente-flujo-completo.spec.ts` → `"descarga autenticada del paquete listo"`, que descarga el `.zip` real desde la UI autenticada contra `apps/api` real — reconfirmado en `docs/auditoria-2/ronda5-final.md` (rubros 2 y 8) y `ronda5-final-reverificacion.md` (2/2 corridas `test:e2e:full` limpias) | CUMPLIDO |
| A14 | Expediente incompleto nunca aparece "listo" | REQ-159, REQ-163 | adversarial | `expediente-package-and-submission.test.ts` → `it('A14: expediente incompleto nunca ensambla "ready"')`; `security-ae14-package-status-re-derived.test.ts`; `apps/web` W-16 + `recorrido.spec.ts`; reconfirmado desde UI real en ronda 5 (`'A14: el paquete nunca aparece "Listo" con el expediente todavía incompleto'`, `expediente-flujo-completo.spec.ts`) | CUMPLIDO |
| A15 | Firma/envío siempre a cargo del usuario | REQ-045, REQ-046, REQ-165 | adversarial | `expediente-package-and-submission.test.ts` → `it('A15: submissions solo registra la declaración del usuario, nunca envía nada')`; `packages/expediente/test/api-surface.test.ts`; reconfirmado desde UI real en ronda 5 (texto "El sistema nunca envía ni firma nada" visible antes de declarar la presentación, `expediente-flujo-completo.spec.ts`) | CUMPLIDO |

---

**Nota**: los criterios marcados con "bloqueado hasta VERIFICAR" (REQ-100, 102-107, 110, 121) tienen verificación puntual documentada en `docs/legal/verificacion-legal.md`, con la reserva explícita de que **no constituye asesoría jurídica** y requiere validación por abogado antes de afirmarse a clientes. REQ-121 permanece NO-VERIFICABLE-EN-LÍNEA.

**Hallazgos de severidad BAJA reparados sin reverificación adversarial independiente adicional** (no explotables, sin marcar ningún REQ como no-CUMPLIDO por sí solos): **API-15** (`apps/api/src/modules/company/routes.ts` — `approve`/`reject` de tarifas ahora toleran un cuerpo vacío con `Content-Type: application/json` vía `withOptionalEmptyJsonBody`, commit `fbf5771`) y **WI-06** (guard síncrono en `apps/web` cierra la ventana de doble clic físico en Aprobar/Rechazar, commit `9bd8b55`) — corrector #70, ver `docs/PROGRESO.md` ("Micro-corrección API-15/WI-06") y `docs/logs/fix-api15-wi06.log`. Ambos originalmente citados en `docs/auditoria-2/reverificacion-final-integrada.md` (commit `1ff99fb`).

**Hallazgo de severidad BAJA genuinamente abierto hoy (ronda 5, sin corregir — fuera del alcance de la corrección de RF-01..04)**: **R5-12** — `apps/web/src/lib/api/twofa.ts:33-35` describe incorrectamente el comportamiento de `apps/api`: afirma que `/2fa/verify-enrollment` "no fue actualizada para EXIGIR orgId/purpose a nivel de aplicación como sí lo está `/2fa/step-up`", cuando ambas rutas los exigen por igual (`modules/twofa/routes.ts`). Es un comentario sin código ejecutable ni impacto funcional (el cliente real ya declara ambos campos siempre). Citado en `docs/auditoria-2/api-r5-09-10-reverificacion.md`, reconfirmado sin corregir en `ronda5-final.md` y `ronda5-final-reverificacion.md`.

**Límites aceptados (regla de 3 vueltas, causa documentada, sin cuarta vuelta)**: AG-05 y AG-12 (`packages/agents`, ver `docs/auditoria-1/agents-cierre.md`); SR-25 y el carácter cooperativo de `reportDropped` (`packages/sources`, ver `docs/auditoria-1/sources-cierre-definitivo.md`); EX-EXP-08 y EX-EXP-10 (`packages/expediente`, ver `docs/auditoria-1/expediente-cierre.md`); DB-07 y DB-09 (`packages/db`, alcance documentado, ver `docs/auditoria-1/db-api-seguridad-reverificacion.md`); concurrencia real de `jobs` (`apps/worker`, WK-04/WK-08, ligada a B-03).

---

## Criterios de aceptación — Ampliación 2: Salida a promoción (REQ-172 a REQ-210)

Fuente: `docs/AMPLIACION-2-SALIDA.md` (D-08, `docs/DECISIONES.md`); requisitos nuevos, sección 34 de `docs/REQUISITOS.md`. **Este pase (2026-09-06 ~17:3x CST, HEAD `9a12173`) reemplaza por completo el snapshot anterior de esta sección, que declaraba «Sin código / PENDIENTE» para casi todas las filas** — era falso desde las rondas 6–8a y estaba registrado como hallazgo abierto **ML-07** (MEDIA, trazabilidad) en `docs/auditoria-2/mail.md` y reconfirmado sin corregir en `mail-reverificacion.md`. **ML-07 se cierra con este pase.**

Misma escala estricta de seis valores definida al inicio del documento. Recordatorio aplicado aquí sin excepción: **CUMPLIDO exige auditoría adversarial + reverificación independiente con veredicto CERRADO** (REQ-210). Hoy solo `packages/mail` tiene esa cadena completa (`docs/auditoria-2/mail.md` → correcciones ML-01..ML-06 → `docs/auditoria-2/mail-reverificacion.md`). **No existe auditoría de `apps/web` rondas 7/8a, ni de `infra/`; la auditoría del login con Google (`api-google.md`) no tiene reverificación independiente de sus correcciones GO-03/GO-07/GO-10; la auditoría de la integración de correos en `apps/api` (despacho #115) está en curso.** Por eso la enorme mayoría de esta tabla queda en **EN_EVIDENCIA**: hay código real y pruebas reales en verde, pero falta el eslabón de reverificación que este proyecto exige para cerrar.

Ninguna fila se marca CUMPLIDO por un mock o fixture. El proveedor OIDC falso y la bandeja de captura de correo son **dobles fieles al contrato** (tokens RS256 reales verificados contra un JWKS real; correo renderizado y almacenado, nunca enviado): habilitan EN_EVIDENCIA, no sustituyen a la integración real contra Google / proveedor de correo / Docker, que permanece **BLOQUEADO_EXTERNO** hasta que el usuario aporte credenciales (Google Cloud; Resend/Postmark/SMTP con dominio SPF/DKIM) o el entorno tenga Docker (verificado hoy: `which docker` → *not found*).

**Gates reales de esta ampliación, reproducibles** (corridas citadas fila a fila):

| Comando | Resultado | Log |
|---|---|---|
| `npm run -w apps/api test` | **74 archivos / 350 tests**, dos pasadas idénticas, `EXIT=0` | `docs/logs/api-mail.log` |
| `npm run -w packages/db test` | **27 archivos / 205 tests**, `EXIT=0` | `docs/logs/api-mail.log` |
| `npm run -w packages/mail test` | **26 archivos / 252 tests**, dos pasadas, `EXIT=0` | `docs/logs/reverify-mail.log` |
| `npm run -w packages/mail preview` | 16 plantillas HTML + índice | `docs/logs/reverify-mail.log` |
| `npm run -w apps/web test` | **39 archivos / 162 tests**, cuatro pasadas idénticas | `docs/logs/web-ronda8.log` |
| `npm run -w apps/web test:e2e:full` | **137/137** (navegador real + `apps/api` real + OIDC falso) | `docs/logs/web-ronda8.log` |
| `npm run -w apps/worker test` | **22 archivos / 383 tests**, dos pasadas | `docs/logs/reverify-r6-wk6.log` |

| REQ | Criterio de aceptación | Prueba | Paquete(s) responsable(s) | Evidencia actual | Estado |
|---|---|---|---|---|---|
| REQ-172 | Botón "Continuar con Google" visible y funcional junto a email+contraseña en login/registro | render | apps/web | Construido (commits `11d48e0`, `fbe9614`): `src/components/auth/GoogleAuthButton.tsx`, `src/pages/LoginPage.tsx`, `src/pages/RegistroPage.tsx`, `src/pages/auth/GoogleCallbackPage.tsx`. Tests: `LoginPage.test.tsx` («REQ-172: ambos métodos visibles y funcionales»), `RegistroPage.test.tsx`, `GoogleAuthButton.test.tsx` (3), `e2e/login-parity.spec.ts`. `npm run -w apps/web test` → 162/162 ×4; `test:e2e:full` → 137/137. **Sin auditoría adversarial de `apps/web` rondas 7/8a** (REQ-210) | EN_EVIDENCIA |
| REQ-173 | Login con Google de un email verificado que coincide con una cuenta existente vincula sin duplicar | integración | apps/api | `apps/api/test/google-oidc-login.test.ts` caso **S2** (`npm run -w apps/api test -- google-oidc-login -t "S2"`); 16/16 en el archivo, api 350/350. Auditado en `docs/auditoria-2/api-google.md` (rubro 3 «comprobado correcto»). Matiz del propio auditor: el test S2 no asevera explícitamente que `password_hash` sobreviva a la vinculación (verificado solo por lectura de código). Sin reverificación independiente de las correcciones GO-03/07/10 | EN_EVIDENCIA |
| REQ-174 | Login con Google de un email nuevo crea usuario sin contraseña; con invitación pendiente entra a esa organización; sin ella `sin_acceso` (D-09) y onboarding de creación explícita | integración | apps/api (+ apps/web onboarding) | API: `google-oidc-login.test.ts` casos **S1** (invitación pendiente aceptada) y «usuario nuevo sin invitación pendiente: compuerta sin_acceso». Web: `src/pages/SinAccesoPage.tsx` + `SinAccesoPage.test.tsx` (5) + E2E real de navegador `e2e/google-login.spec.ts` (cuenta Google nueva → `/sin-acceso` → «Crear mi organización» → `POST /orgs` real → `/panel`). Reformulado por **D-09**. Residual de gobernanza **GO-01**: el criterio verificable de `docs/REQUISITOS.md:361` todavía dice «usuario y organización creados en una sola operación» — corregirlo exige tocar el texto del requisito, fuera del ámbito de este pase | EN_EVIDENCIA |
| REQ-175 | Tokens de sesión/refresh de Google comparten esquema y expiración con los de email+contraseña | integración | apps/api | Construido: `issueTokenPair` es literalmente el mismo emisor (`src/modules/auth/google/routes.ts`). **Brecha real de verificación**: no existe prueba de contrato de paridad de esquema/expiración ni ninguna que **rote** un refresh token emitido por Google (`test/audit-api01-refresh-rotation.test.ts` no menciona Google). Lo único aseverado hoy es `typeof refreshToken === 'string'` y que el `accessToken` sirve en `GET /organizations`. El veredicto del auditor para este REQ fue explícitamente «confirmado por lectura de código», no por ejecución | EN_EVIDENCIA (parcial — sin prueba de contrato) |
| REQ-176 | Cuenta con 2FA activo exige el mismo segundo factor tras login con Google | integración | apps/api | Tres capas reales: `google-oidc-login.test.ts` («usuario con 2FA activo… exige el mismo segundo factor») y los dos casos de regresión **GO-10**, verificados en rojo antes del arreglo (`sin_acceso` en vez de `ok`/`requires_2fa`); `GoogleCallbackPage.test.tsx` (`requires_2fa` + código incorrecto); y E2E de navegador con TOTP vivo en `e2e/google-login.spec.ts`. **GO-10 era severidad ALTA (bypass de 2FA)**: REPARADO en `0e130ae`, **sin reverificación independiente** | EN_EVIDENCIA |
| REQ-177 | Cada evento de login/vinculación/creación vía Google aparece en `audit_log` | integración | apps/api + packages/db | Aseverado contra la base real en `google-oidc-login.test.ts`: `auth.google_login`, `auth.google_linked`, `auth.google_rejected` (incl. `reason: identity_race_unresolved`); migración `0072_req177_google_auth_audit.sql`; `packages/db/test/req172-google-oidc.test.ts` (17 tests). **Brecha honesta**: `recordAuthAudit` (`apps/api/src/lib/audit.ts:97-104`) nunca pasa `correlationId` a `app.record_auth_event`, así que `audit_log.correlation_id` queda NULL en todo evento de autenticación — con paridad exacta respecto a email+contraseña, que tampoco lo llena | EN_EVIDENCIA |
| REQ-178 | Sin `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` reales, el flujo contra Google real queda bloqueado y declarado como tal; el flujo se prueba con un proveedor OIDC falso | unit + integración | apps/api (config) + transversal (CI/secretos) | **El código ya existe** (contra lo que decía el snapshot anterior): `src/modules/auth/google/env.ts` es el único lector de `GOOGLE_CLIENT_SECRET`; el 503 explícito sin credenciales está probado en `GoogleAuthButton.test.tsx`, `GoogleCallbackPage.test.tsx`, `RegistroPage.test.tsx` y `security-go03-oidc-issuer-https.test.ts` (8/8). Dos dobles OIDC falsos reales: `apps/api/test/helpers/fake-oidc.ts` y `apps/web/scripts/fake-oidc-server.mjs` (302 real + PKCE S256 verificado). **Sigue BLOQUEADO_EXTERNO**: el usuario no ha aportado credenciales de Google Cloud. Brecha secundaria: `scripts/check-secrets.sh` no tiene ningún patrón de client secret de Google (`GOCSPX-`/`client_secret`), que es lo que pide la letra del criterio | BLOQUEADO_EXTERNO |
| REQ-179 | Token de Google con `email_verified=false` rechazado sin crear ni vincular cuenta | adversarial | apps/api | `google-oidc-login.test.ts` caso **S3**: 0 cuentas creadas, 0 vinculaciones y fila `auth.google_rejected` en `audit_log`. `npm run -w apps/api test -- google-oidc-login -t "S3"` | EN_EVIDENCIA |
| REQ-180 | Vinculación bloqueada ante conflicto de organización/rol para el mismo email | adversarial | apps/api | `google-oidc-login.test.ts` («un segundo subject de Google para el mismo email ya vinculado se rechaza como conflicto»: 403 + auditado) y `GoogleCallbackPage.test.tsx` («REQ-180 adversarial: 403 de conflicto de identidad») | EN_EVIDENCIA |
| REQ-181 | Catálogo de 10 plantillas con asunto/disparador/variables documentados | unit | packages/mail (nuevo) | `packages/mail/src/templates/catalog/` con **16 plantillas** (sobrecumple las 10) registradas en `registry.ts`; `test/templates/catalog.test.ts` (84 tests) valida asunto, variables tipadas, wordmark y pie con motivo de envío en cada una. `npm run -w packages/mail test` → 26/252 ×2. Cadena cerrada: `docs/auditoria-2/mail.md` → `mail-reverificacion.md` (ML-01..ML-06 CERRADOS). Nota: el test asevera `>= 15`, no el número 10 del criterio, y `packages/mail/README.md:14` todavía dice «10» | CUMPLIDO |
| REQ-182 | Adaptador único de proveedor de correo por env, sin `if provider === X` fuera del adaptador | unit | packages/mail (nuevo) | Arquitectura correcta: un solo `src/provider/factory.ts` decide; adaptadores `resend`/`postmark`/`smtp`/`capture` detrás de `provider/types.ts`; `test/provider/factory.test.ts` (6). **La reverificación señala explícitamente la brecha**: *«sin el test estático explícito que el propio REQ-182 pide… no se encontró tal test en `packages/mail/test/`»*, reconfirmado en este pase | EN_EVIDENCIA (falta el test estático que exige el criterio) |
| REQ-183 | Sin proveedor configurado, correos se capturan localmente y 0 llamadas HTTP salientes | integración | packages/mail (nuevo) | `src/provider/capture-provider.ts` + degradación segura de `createMailProviderFromEnv` (sin `MAIL_PROVIDER` o con valor desconocido → `CaptureProvider`), `test/provider/factory.test.ts` y `capture-provider.test.ts`. La aseveración decisiva de «0 llamadas» vive en `apps/api/test/mail-provider-and-retry.test.ts`, que sabotea `globalThis.fetch` y exige `llamadasDeRed === 0`. Esa mitad depende de `apps/api`, cuya auditoría de correos (#115) está en curso | EN_EVIDENCIA |
| REQ-184 | Previsualización renderizada de cada plantilla con datos de ejemplo | render | packages/mail (nuevo) + apps/web | `packages/mail/scripts/preview.ts` (`npm run -w packages/mail preview`) genera las 16 plantillas + índice; reproducido en la reverificación (`docs/logs/reverify-mail.log`) y en `docs/logs/audit-mail.log`. Aserciones de render en `test/templates/catalog.test.ts` con `html-validate`. Nota: no hay previsualización servida desde `apps/web` (el criterio la nombra como responsable secundario) | CUMPLIDO |
| REQ-185 | Plantillas en español de México, tono/estructura Likida, marca Atiende, 0 menciones a Likida | render | packages/mail (nuevo) | 0 menciones en la **salida renderizada**: `grep -il likida packages/mail/preview/*.html` → vacío, registrado en `docs/logs/audit-mail.log` y confirmado por `docs/auditoria-2/mail.md`; `catalog.test.ts` exige el wordmark `Atiende · Licitaciones` en las 16. **Brecha honesta**: ningún test committeado guarda el criterio (la evidencia es un grep manual registrado en log) y quedan **11 menciones a "Likida" vivas** en comentarios de `packages/mail/src/**` (7) y en `packages/mail/README.md` (4) — procedencia del patrón, nunca contenido de correo | EN_EVIDENCIA (sin test que guarde el criterio) |
| REQ-186 | Enlaces firmados con expiración; enlace expirado o alterado es rechazado en servidor | integración | apps/api + packages/mail | Doble candado real: firma HMAC (`packages/mail/src/security/signed-link.ts`, `test/security/signed-link.test.ts`) **más** consumo atómico de token hasheado en base (`app.consume_email_verification_token`, `app.reset_password_with_token`, migración 0084). Tests: `apps/api/test/mail-email-verification.test.ts` (9, incl. enlace vencido, reutilizado, alterado y de otra cuenta), `mail-organization-invite.test.ts` (6), `mail-password-reset.test.ts` (8). `npm run -w apps/api test -- test/mail-email-verification.test.ts`. Auditoría de la integración en `apps/api` en curso (#115) | EN_EVIDENCIA |
| REQ-187 | Preferencias de notificación y baja respetadas salvo transaccionales de seguridad | integración | apps/api | `apps/api/src/lib/mail/preferences.ts` + `packages/mail/src/preferences/filter.ts`; `apps/api/test/mail-unsubscribe-preferences.test.ts` (8) incluye «la preferencia SE APLICA al enviar: un correo opcional apagado no se manda, uno obligatorio sí», baja de un clic RFC 8058, GET que no aplica, token alterado/vencido/categoría desconocida → 400, aislamiento entre usuarios. Supresión (rebote/queja) es más fuerte: no recibe nada, ni obligatorio. Commit `c84f736`. Sin auditoría cerrada de la integración (#115) | EN_EVIDENCIA |
| REQ-188 | Registro de envíos con estado/intentos; reintento vía job ante fallo simulado | integración | packages/mail (nuevo) + apps/worker | **Mitad productora, verde y probada**: `mail_outbox` (migración 0080) con historial de intentos, `app.enqueue_mail_retry` (migración 0086, `SECURITY DEFINER` acotada — nació de un bug real: la RLS `ins_jobs` bloqueaba el encolado y el error se tragaba en el `.catch` de fondo, commit `a0330b5`); `apps/api/test/mail-provider-and-retry.test.ts` (fallo transitorio → historial + job encolado; fallo 4xx → ni reintenta ni encola); `packages/db/test/req181-mail.test.ts` (20). **Mitad consumidora ausente de HEAD**: `apps/worker/src/handlers/mail-retry.ts` y su test existen solo como trabajo sin commitear (despacho #116 en curso; 0 commits en cualquier rama). `apps/api/README.md` lo declara literalmente: *«apps/worker está fuera del alcance de este cambio y todavía no lo consume»* | EN_EVIDENCIA (el reintento se encola y se registra; nadie lo ejecuta en HEAD) |
| REQ-189 | Ningún correo a destinatario no registrado ni no explícito de contacto | adversarial | apps/api + packages/mail | `packages/mail/src/recipients/` + `apps/api/src/lib/mail/recipients.ts`; `apps/api/test/mail-provider-and-retry.test.ts` («REQ-189: nunca se manda a un destinatario que el llamador no declaró registrado») y `packages/mail/test/recipients/types.test.ts` (6). Reverificado a nivel de librería sin cambios desde la auditoría original; la mitad de `apps/api` sin auditoría cerrada (#115) | EN_EVIDENCIA |
| REQ-190 | Sin credenciales de proveedor configuradas, 0 conexiones salientes de red para correo | integración | packages/mail (nuevo) | `apps/api/test/mail-provider-and-retry.test.ts`, dos casos que **sabotean `globalThis.fetch`** y exigen 0 llamadas: sin `MAIL_PROVIDER` (bandeja de captura) y con `MAIL_PROVIDER=resend` **sin** `RESEND_API_KEY` (→ `not_configured`, outbox en `pending`, 0 red). Límite declarado: el camino SMTP (nodemailer) no pasa por `fetch` y ese saboteo no lo cubre | EN_EVIDENCIA |
| REQ-191 | Onboarding guiado navegable de principio a fin (6 pasos) | E2E | apps/web | `src/pages/onboarding/OnboardingPage.tsx` (commit `6625266`), asistente real con endpoints reales. **Discrepancia real con el criterio**: son **5 pasos** (Organización → Perfil de empresa → Invitar equipo → Primer documento → Listo), no los 6 que enumera REQ-191 — faltan «verificación de correo» (ronda 8b, en curso) y «primera convocatoria». E2E: `e2e/onboarding.spec.ts` (requiere `test:e2e:full`), verde dentro de 137/137 | EN_EVIDENCIA (5 de los 6 pasos del criterio) |
| REQ-192 | Checklist de activación refleja estado real de cada paso | render | apps/web | `src/hooks/useActivationChecklist.ts` (5 ítems, cada uno leído de un endpoint real: perfil, documentos, tarifas, convocatorias, 2FA — ninguno en `localStorage` ni inventado) + `components/dashboard/ChecklistCard.tsx`, renderizado en `PanelPage.tsx`. Tests: `PanelPage.test.tsx` y `e2e/onboarding.spec.ts` («1 de 5 pasos completados.»). Desviación documentada en el propio código: no existe endpoint de «convocatoria vista», se usa «hay al menos una convocatoria» como proxy | EN_EVIDENCIA |
| REQ-193 | Estados vacíos principales incluyen guía de siguiente acción | render | apps/web | Esta fila estaba desactualizada — seguía diciendo "ninguna página lo pasa" cuando cuatro commits previos ya lo habían corregido (`f6b4ddf`, `0db4843`, `ad97273`, `f574466`, 2026-09-07). Reauditadas las ~53 invocaciones actuales de `EmptyState` en `apps/web/src`: cada una tiene ahora acción real (`actionLabel`/`onAction` con navegación o limpieza de filtro que de verdad dispara algo) o copia corregida para no prometer una acción inexistente — nunca un botón decorativo. Por módulo principal (REQ-193 nombra empresa/convocatorias/expediente/equipo): **empresa** — TarifasAprobadasPage/DocumentosVigenciasPage: su "Aún no hay X" tiene el formulario de alta (proponer tarifa / subir documento) ya visible arriba en la misma página cuando el rol puede escribir, un botón duplicado en el EmptyState sería ruido; FirmantesAutorizadosPage/PerfilCapacidadesPage no tienen un EmptyState de "sin registros" (usan texto plano fuera de este componente), solo el de "Selecciona una organización", que no necesita botón (la acción real es el selector del encabezado). **convocatorias** — DescubrimientoPage: honestamente vacío hasta que exista una fuente real (B-02); MatchingPage/GoNoGoPage/AprobacionesPage/TenderSelect: `actionLabel="Ir a Descubrimiento"` navegando a `/convocatorias/descubrimiento`; FuentesFrescuraPage: copia corregida, ya no promete "configurar fuente"; ConvocatoriaDetallePage: datos de fuente externa, sin acción manual que ofrecer. **expediente** — ExpedientePage: selector de convocatoria visible arriba; RedaccionPage: `actionLabel="Ir a Análisis de bases"` cuando faltan requisitos activos; su `SectionEditor` ("Aún no hay secciones") no tiene botón propio porque los formularios de generación técnica/económica ya están visibles arriba en la misma página cuando el rol puede escribir (y si no puede, tampoco podría usar ese botón). **equipo** — no existe una pantalla literalmente llamada "Equipo" en `apps/web/src/config/navigation.ts`; la única que gestiona miembros/roles es "Usuarios y roles" (UsuariosRolesPage, bajo Back office): `InviteForm` visible arriba cuando el rol puede invitar. Fuera del alcance nombrado por el REQ pero auditado igual (backoffice): JobsPage/AuditoriaPage ofrecen `actionLabel` ("Ver todos los estados"/"Limpiar filtros") solo cuando hay un filtro activo que limpiar (`f574466`, ya en HEAD antes de esta ronda). **Corregido en esta ronda**: `AgentesHerramientasPage` — "Aún no hay agentes configurados" prometía una acción de configuración que no existe en ninguna pantalla (no hay endpoint ni botón para iniciar una corrida de agente manualmente; `apps/api/src/lib/agent-triggers.ts` confirma que solo el backend las dispara); corregido a "Aún no hay corridas de agentes" sin botón, con test nuevo que verifica la copia y la ausencia de cualquier acción fabricada (`AgentesHerramientasPage.test.tsx`). Verificado en el worktree de cierre: `npm run typecheck` y `npm run lint` de `apps/web` limpios (repo completo, no solo el archivo tocado); `AgentesHerramientasPage.test.tsx` en verde (5/5, incluye el test nuevo). La suite completa de `apps/web` (`npx vitest run`) se intentó dos veces en este cierre y ambas quedaron inconclusas por contención real de máquina compartida (≥35 procesos `vitest` concurrentes de otras sesiones/worktrees corriendo en paralelo sobre el mismo repo en ese momento, confirmado con `ps aux`) — no por ningún fallo atribuible a este cambio, que solo toca `AgentesHerramientasPage.tsx`/`.test.tsx` y esta fila de esta tabla. No se afirma "suite completa en verde"; falta esa corrida en un entorno sin contención (ver `docs/logs/web-req-193.log` para el detalle de las tres corridas reales sí concluidas y el diagnóstico de las dos que no). Límite honesto: el criterio literal ("cada estado vacío principal muestra una acción sugerida accionable") no exige aquí un botón donde no existe ninguna acción real (p. ej. "Selecciona una organización"); se interpreta, consistente con el resto de esta tabla, que una guía honesta en texto cuenta como cumplimiento cuando fabricar un botón simularía una capacidad que no existe. Sin reverificación adversarial independiente | EN_EVIDENCIA |
| REQ-194 | Landing pública con las 4 secciones exigidas e identidad Atiende | render | apps/web (público) | `src/pages/LandingPage.tsx` (507 líneas, commit `4630723`) con secciones `hero`/`como-funciona`/`seguridad`/`casos`/`planes`/`faq`/`demo` — cubre propuesta de valor, cómo funciona, seguridad-sin-actuación-automática y solicitud de demo. Tests: `LandingPage.test.tsx` y `e2e/landing.spec.ts` (7 casos). Sin auditoría de la ronda 7 | EN_EVIDENCIA |
| REQ-195 | Aviso de privacidad y términos publicados como borrador marcado `borrador_pendiente_validacion_juridica` | render | apps/web + apps/api (aviso existente REQ-119) | Términos: `src/lib/legal/termsContent.ts:18` (`TERMS_STATUS = "borrador_pendiente_validacion_juridica"`) renderizado por `TermsPage.tsx` como tarjeta visible «Borrador pendiente de validación jurídica»; commit `e39f15b`. Aviso: `PrivacyNoticePage.tsx` + `schemas.ts:665` (`z.literal("borrador_pendiente_validacion_juridica")` desde `GET /legal/privacy-notice`). Tests: `TermsPage.test.tsx`, `PrivacyNoticePage.test.tsx`, `e2e/landing.spec.ts` (pie → ambas páginas, texto de borrador visible). **La validación por abogado mexicano sigue siendo bloqueo externo declarado** | EN_EVIDENCIA |
| REQ-196 | Formulario de contacto crea registro interno y dispara correo interno | integración | apps/web (público) + apps/api | **Mitad de API construida y probada**: `apps/api/src/modules/public/contact.routes.ts` (`POST /public/contact` → registro en `contact_requests` + correo interno con plantilla `contact-received`; honeypot `website` que descarta en silencio, longitudes acotadas, rate limit tier `auth`), `apps/api/test/mail-public-contact.test.ts` (4), commit `c5705d1`. **La web NO la consume**: el formulario de `LandingPage.tsx:412-490` tiene el botón `disabled` y un `onSubmit` que solo hace `preventDefault()`, con un comentario ya obsoleto que afirma que «no hay ningún endpoint de contacto en apps/api». Cablearlo es trabajo de la ronda 8b, en curso | EN_EVIDENCIA (API sí; formulario web sin cablear) |
| REQ-197 | Metaetiquetas por página, `sitemap.xml`, `robots.txt` accesibles | render | apps/web (público) | Metaetiquetas por página: `src/hooks/useDocumentMeta.ts`, usado por LandingPage, DemoPage, TermsPage, SinAccesoPage, RegistroPage, OnboardingPage y GoogleCallbackPage. `robots.txt` existe (`apps/web/public/robots.txt`) pero su contenido íntegro es `User-agent: *` / `Disallow: /`, y `index.html` fija `<meta name="robots" content="noindex, nofollow">` — correcto para un sitio aún no lanzado, contrario a la letra del criterio. **`sitemap.xml` no existe** en ninguna parte del repositorio. Ningún test verifica metaetiquetas, robots ni sitemap | EN_EVIDENCIA (parcial — sin `sitemap.xml`, sin test) |
| REQ-198 | Payloads de analítica sin campos con PII identificable | adversarial | apps/web (público) | Blindaje ACTIVO agregado (antes el criterio se cumplía solo de forma PASIVA -- "no hay payload que auditar porque no hay analítica" -- sin nada que fallara si algún día se agregaba analítica con PII). `apps/web/src/lib/analytics/piiGuard.ts`: puerto real `AnalyticsSink` + lógica de negocio real `assertNoPii`/`guardAnalyticsSink` que bloquea cualquier evento con PII -- por nombre de campo denylisted (email/nombre/teléfono/RFC/CURP/IP/dirección/datos financieros-credenciales, comparación por token normalizado, no substring crudo) o por patrón de valor embebido (correo, RFC, CURP, IPv4, secuencia de 10 dígitos) incluso bajo un nombre de campo inocuo, con recorrido recursivo de objetos/arreglos y profundidad máxima fail-closed -- ANTES de tocar cualquier sink real. `apps/web/src/lib/analytics/fakeAnalyticsSink.ts`: adaptador `FakeAnalyticsSink` (mismo patrón que `FakeProvider` en `packages/agents/src/llm/fake-provider.ts`), solo para pruebas, nunca sale a la red -- se mockea el borde externo, nunca la lógica de negocio real. `piiGuard.test.ts` (24 pruebas, incl. adversariales: PII bajo nombre de campo inocuo, PII anidada en objetos/arreglos, fail-closed por profundidad excedida, y la aserción de que el sink de destino queda VACÍO cuando la guardia bloquea) + `src/test/no-analytics-sdk.test.ts` (4 pruebas, fija como regresión automática el estado real actual -- `package.json`/`index.html`/`src/`/`e2e/` sin ningún SDK/script de analítica de terceros -- y que `connect-src 'self'` de la CSP sigue vigente como defensa de red complementaria). Comando y resultado reales: `cd apps/web && NODE_OPTIONS=--no-experimental-webstorage npx vitest run src/lib/analytics src/test/no-analytics-sdk.test.ts` -> `Test Files 2 passed (2)` / `Tests 28 passed (28)`; `npx tsc -b --noEmit` limpio; `npx eslint src/lib/analytics src/test/no-analytics-sdk.test.ts` limpio. `verificado_contra_real=false`: ningún proveedor de analítica real está conectado (ninguno existe en el repo) -- la guardia se verificó con `FakeAnalyticsSink`, nunca contra un proveedor real; el día que se integre uno, debe enrutarse por `guardAnalyticsSink(sinkReal)`. Sin auditoría/reverificación independiente cerrada todavía. | EN_EVIDENCIA |
| REQ-199 | Dockerfile de producción por servicio (api/worker/web) construye sin errores | integración | infra/ | **Archivos construidos** (commit `beddc41`): `apps/api/Dockerfile` (node:22-alpine, 2 etapas, `USER node`, `HEALTHCHECK` a `/healthz`), `apps/worker/Dockerfile`, `apps/web/Dockerfile` (vite build → nginx:1.27-alpine), `.dockerignore`. Validación **estática**: `npx dockerfile-utils lint` → exit 0 en los tres (`docs/logs/infra-ronda6.log`). Hallazgo real de esa ronda: `node dist/index.js` no arranca (paquetes TS sin compilar) → las imágenes usan `tsx`. **`docker build` nunca se ha ejecutado**: Docker no está instalado (verificado hoy: `which docker` → *not found*), así que la letra del criterio («construye sin errores») no puede verificarse en este entorno | BLOQUEADO_EXTERNO (Docker no disponible; archivos reales y lint estático en verde) |
| REQ-200 | `docker-compose` de producción levanta api+worker+web+postgres con healthcheck en verde | integración | infra/ | `infra/compose/docker-compose.prod.yml` (6 servicios: postgres, migrate, api, worker, web, caddy) + `infra/compose/docker-compose.dev.yml` + `infra/env/.env.prod.example` (34 variables) + `infra/docker/{Caddyfile,nginx.conf,nginx.web.conf}`. Validación **estática**: ambos ficheros parsean con `YAML.parse`; `docker compose config` **no se pudo correr** por ausencia de Docker. Ningún `up` se ha ejecutado jamás | BLOQUEADO_EXTERNO (Docker no disponible) |
| REQ-201 | Migraciones se ejecutan automáticamente e idempotentemente al arrancar | integración | packages/db + infra/ | Runner con checksum y tabla `schema_migrations`: `packages/db/src/migrate.ts`. Hooks de arranque reales: `apps/api/src/app.ts:102-104` y `apps/worker/src/index.ts:24-26` (`SKIP_MIGRATIONS`), más el servicio de un solo disparo `migrate` del compose (`scripts/db-postgres-migrate.mjs`), tras el cual api/worker corren con `SKIP_MIGRATIONS=true`. **Idempotencia probada y ejecutada**: `packages/db/test/migrate.test.ts` (4 tests; la segunda aplicación no repite nada), verde en `docs/logs/ci-local-5.log:812-814` y en `npm run -w packages/db test` → 205/205. Límite honesto: se prueba `applyMigrations` dos veces sobre PGlite, no «arrancar el servicio dos veces» en contenedor | EN_EVIDENCIA |
| REQ-202 | Cada servicio expone healthcheck consumido por el orquestador | integración | apps/api + apps/worker + apps/web + infra/ | `apps/api`: `GET /healthz` (200 sin tocar la base) y `GET /readyz` (`select 1`, 503 si la base cae) en `src/modules/health/routes.ts`, **ejecutados** en `apps/api/test/rate-limit-and-health.test.ts` incluido el camino 503. `apps/web`: `HEALTHCHECK` de imagen sobre `/index.html`. Sonda externa `infra/scripts/healthcheck.sh`. **Dos huecos reales**: `apps/worker` no expone ningún endpoint (declarado por diseño en su Dockerfile y en `docs/OPERACION.md:118`), y el compose de producción declara `healthcheck:` **solo para postgres** — ningún servicio de aplicación está condicionado a salud | EN_EVIDENCIA (parcial — worker sin healthcheck; compose no lo consume) |
| REQ-203 | Runbook de backup completo (frecuencia/retención/restauración) sin ejecución real | documentación | infra/ (docs) | `infra/README.md` §Backups/restauración + `infra/scripts/backup-postgres.sh` y `restore-postgres.sh` + `docs/OPERACION.md` §8: frecuencia (cron diario 03:00), retención (`BACKUP_RETENTION_DAYS`, 14 por defecto), restauración paso a paso, cifrado GPG opcional, advertencia de roles/RLS. `bash -n` + `shellcheck 0.11.0` → 0 hallazgos. El propio README dice: *«Nada de esto se ha probado contra un backup/restore real»* — que es exactamente lo que el criterio permite («sin ejecución real»). Falta la auditoría (no existe informe de infra en `docs/auditoria-*`) | EN_EVIDENCIA |
| REQ-204 | Runbook de salida a producción cubre variables/orden/verificación/rollback | documentación | infra/ (docs) | `infra/README.md`: §Despliegue paso a paso (8 pasos: DNS → clon → `.env` → build → `up -d` → verificación → primer superadmin → backups), §Rollback (re-`up -d` con `IMAGE_TAG` anterior) y §Checklist de go-live (12 ítems). Cubre las cuatro partes exigidas, con una advertencia honesta y correcta: *«Las migraciones NO tienen "down"»*, por lo que el único camino real de recuperación es `restore-postgres.sh` sobre un respaldo previo. Sin auditoría de infra | EN_EVIDENCIA |
| REQ-205 | Ninguna tarea de esta ampliación ejecuta despliegue real ni gasto sin autorización | revisión de proceso | transversal (gobierno) | Cumplido por disciplina de proceso y verificable por ausencia: no hay Docker en la máquina, ninguna imagen se construyó ni se publicó, no se compró crédito alguno. Los dos despliegues que sí existen fueron **autorizados explícitamente por el usuario** el 2026-09-06 12:4x (GitHub privado y Vercel, ver B-03 en `docs/BLOQUEOS.md`). **Sigue sin existir mecanismo de enforcement automatizado**: es disciplina, no un candado | EN_EVIDENCIA |
| REQ-206 | Suite E2E de Google (OIDC falso): S1+S2+S3 en verde | integración/E2E | apps/api + apps/web | `apps/api/test/google-oidc-login.test.ts` **16/16** (S1, S2, S3 nombrados literalmente + 4 adversariales + 2FA + 2 de GO-10 + `sin_acceso` + REQ-180 + 3 de GO-07 + `/auth/google/start`), dentro de api 350/350 ×2; `apps/web/e2e/google-login.spec.ts` 3/3 dentro de `test:e2e:full` 137/137. Además `apps/api/test/security-go03-oidc-issuer-https.test.ts` (8) y `packages/db/test/req172-google-oidc.test.ts` (17). Sin reverificación independiente (REQ-210) | EN_EVIDENCIA |
| REQ-207 | Suite de correos (bandeja de captura): S4+S5+S6+S7 en verde | integración/E2E | packages/mail (nuevo) + apps/api | S4, S5 y S6 en verde de punta a punta (`packages/mail` 252/252; `apps/api` 350/350 ×2 con `CaptureProvider`, sin red). **S7 solo verde en su mitad productora**: el job `mail_retry` se encola y se registra, pero el handler que lo consume no está en HEAD (#116 en curso). Declarar la suite completa en verde sería falso hoy | EN_EVIDENCIA |
| REQ-208 | Suite E2E de onboarding completo: S8 en verde | E2E | apps/web | `apps/web/e2e/onboarding.spec.ts` verde dentro de `test:e2e:full` 137/137 (registra un usuario limpio contra `apps/api` real, recorre los 5 pasos hasta `/panel`, con axe limpio). No cubre registro→verificación de correo→primera convocatoria, que es lo que S8/REQ-191 exigen | EN_EVIDENCIA (alcance menor al del criterio) |
| REQ-209 | Suite de render/accesibilidad de landing y páginas públicas: S9 en verde | render | apps/web (público) | `e2e/landing.spec.ts` (7 casos: hero, pie legal, axe a 1280×800, axe + sin scroll horizontal a 390×844, sin scroll a 320×568, formulario deshabilitado, sesión→/panel) y `e2e/demo.spec.ts` (6, con axe y 390×844); axe unitario con `vitest-axe` en `LandingPage.test.tsx`, `TermsPage.test.tsx`, `DemoPage.test.tsx`, `OnboardingPage.test.tsx`. Matiz honesto: el filtro de `e2e/utils/a11y.ts` es `serious`/`critical`, no «0 violaciones de cualquier impacto»; `/legal/terminos` y `/privacidad` solo tienen axe unitario, no E2E | EN_EVIDENCIA |
| REQ-210 | Auditoría adversarial y reverificación independiente citada antes de cerrar cualquier REQ de esta ampliación | gobierno | transversal (gobierno) | Cadena **completa** solo en `packages/mail`: `docs/auditoria-2/mail.md` → correcciones ML-01..ML-06 → `mail-reverificacion.md` (5 CORREGIDOS con ataques nuevos; ML-06/08/09 BAJA documentados). **Cadena incompleta o inexistente en el resto**: `api-google.md` no tiene reverificación de GO-03/GO-07/GO-10 (este último ALTA); la auditoría de la integración de correos en `apps/api` está en curso (#115); **no existe ningún informe de auditoría de `apps/web` rondas 7/8a ni de `infra/`** (`grep -rl "ronda 7\|ronda 8" docs/auditoria-*` → vacío). Por eso solo 3 filas de esta ampliación llegan a CUMPLIDO | EN_EVIDENCIA |

## Pruebas mínimas obligatorias de la Ampliación 2 — Salida a promoción

Exigidas explícitamente por la tarea de gobierno de requisitos de Ampliación 2 (2026-09-06), derivadas de `docs/AMPLIACION-2-SALIDA.md`. Cada una debe probar el flujo integrado real con dobles fieles al contrato (proveedor OIDC falso con tokens de forma real, bandeja de captura de correo real) — nunca contra la red externa sin credenciales del usuario. **Advertencia de trazabilidad registrada en `docs/logs/api-mail.log` §5**: los asuntos de varios commits de la integración de correos citan números de escenario `S` equivocados; la correspondencia autoritativa es la de esta tabla, no la de los mensajes de commit.

| # | Prueba mínima | REQ relacionados | Tipo de prueba | Evidencia actual | Estado |
|---|---|---|---|---|---|
| S1 | Login con Google (OIDC falso) crea usuario; entra por invitación o queda `sin_acceso` → onboarding crea organización (D-09) | REQ-172, REQ-174, REQ-177 | integración | `apps/api/test/google-oidc-login.test.ts` caso S1 + caso `sin_acceso`; `apps/web/e2e/google-login.spec.ts` recorre el camino completo en navegador real contra `apps/api` real y el proveedor OIDC falso. `npm run -w apps/api test -- google-oidc-login`; `npm run -w apps/web test:e2e:full`. Sin reverificación (REQ-210) | EN_EVIDENCIA |
| S2 | Login con Google vincula cuenta existente por email verificado | REQ-173, REQ-180 | integración | `google-oidc-login.test.ts` caso S2 (`-t "S2"`). Matiz del auditor: el test no asevera explícitamente que `password_hash` sobreviva; eso solo se verificó por lectura de código | EN_EVIDENCIA |
| S3 | Google con email no verificado → rechazado, 0 cuentas creadas | REQ-179 | adversarial | `google-oidc-login.test.ts` caso S3: 0 cuentas, 0 vinculaciones y `auth.google_rejected` en `audit_log` | EN_EVIDENCIA |
| S4 | Cada plantilla renderiza con variables reales y pasa validación HTML/accesibilidad básica | REQ-181, REQ-184, REQ-185 | render | `packages/mail/test/templates/catalog.test.ts` (84 tests sobre las 16 plantillas, con `html-validate` vía `test/support/html-validate-config.ts`) + `test/theme/contrast.test.ts` (5; ML-04 cerrado: `faint` a 4.92:1/4.67:1). `npm run -w packages/mail test` → 26/252 ×2. Cadena auditoría→corrección→reverificación cerrada. Límite declarado en `mail.md`: sin verificación en clientes reales (Litmus/Email on Acid) | CUMPLIDO |
| S5 | Correo de invitación con enlace firmado expira y es rechazado tras vencer | REQ-186 | integración | `apps/api/test/mail-organization-invite.test.ts` («el enlace del correo acepta la invitación tal cual (d/s), y VENCIDO o ALTERADO se rechaza en el servidor»; invitación vencida en base; `Idempotency-Key` sin doble envío; token de un solo uso atado al email) y `mail-password-reset.test.ts` (8). Auditoría de la integración en curso (#115) | EN_EVIDENCIA |
| S6 | Baja de notificaciones respetada (salvo transaccionales de seguridad) | REQ-187 | integración | `apps/api/test/mail-unsubscribe-preferences.test.ts` (8), incluido el caso decisivo «la preferencia SE APLICA al enviar: un correo opcional apagado no se manda, uno obligatorio sí» y la baja de un clic RFC 8058. Auditoría en curso (#115) | EN_EVIDENCIA |
| S7 | Envío fallido reintenta vía job y queda registrado con su historial de intentos | REQ-188 | integración | `apps/api/test/mail-provider-and-retry.test.ts`: el fallo transitorio agota reintentos, deja historial en `mail_outbox` y **encola** un job `mail_retry`; el fallo permanente 4xx ni reintenta ni encola. `packages/db/test/req181-mail.test.ts` prueba `app.enqueue_mail_retry` con RLS real. **El reintento no se ejecuta en HEAD**: el handler de `apps/worker` no está committeado (#116 en curso) | EN_EVIDENCIA (encolado y registrado; sin consumidor en HEAD) |
| S8 | Onboarding completo navegado de punta a punta en E2E | REQ-191, REQ-192, REQ-193, REQ-208 | E2E | `apps/web/e2e/onboarding.spec.ts` verde dentro de 137/137: registro real → `/sin-acceso` → 5 pasos → `/panel`, con axe limpio y «1 de 5 pasos completados.». Cubre 5 de los 6 pasos del criterio. No ejercita REQ-193 directamente (el flujo de onboarding no pasa por un EmptyState con acción); ver fila REQ-193 arriba para su evidencia propia, ya no desactualizada | EN_EVIDENCIA (parcial) |
| S9 | Landing renderiza con axe 0 violaciones, incluida vista móvil | REQ-194, REQ-197, REQ-198, REQ-209 | render | `e2e/landing.spec.ts` + `e2e/demo.spec.ts` con axe en 1280×800, 390×844 y 320×568, verdes dentro de 137/137. Las patas REQ-197 (falta `sitemap.xml`) y REQ-198 (no hay analítica) de este escenario no están cubiertas; el filtro de axe es `serious`/`critical` | EN_EVIDENCIA |
| S10 | Formulario de contacto crea registro y dispara correo interno | REQ-196 | integración | `apps/api/test/mail-public-contact.test.ts` (4): registro en `contact_requests` + correo interno, honeypot que descarta en silencio, 422 por longitud, 429 al sexto intento por minuto. **El formulario de la landing no lo llama**: botón `disabled`, `onSubmit` solo `preventDefault()` (`LandingPage.tsx:412-490`) | EN_EVIDENCIA (API sí; web sin cablear) |
| S11 | `docker-compose` levanta api+worker+web+postgres y pasa healthchecks | REQ-199, REQ-200, REQ-201, REQ-202 | integración | Los archivos existen y pasan validación estática (lint de Dockerfiles, `YAML.parse`, `shellcheck` 0 hallazgos). **Docker no está instalado en este entorno** — reverificado hoy: `which docker` / `which docker-compose` → *not found*. Ningún `docker build` ni `docker compose up` se ha ejecutado jamás; la verificación real de este escenario permanece bloqueada independientemente del código construido | BLOQUEADO_EXTERNO |
| S12 | Ningún envío de correo alcanza la red real sin proveedor configurado | REQ-190 | integración | `apps/api/test/mail-provider-and-retry.test.ts`, dos casos que sabotean `globalThis.fetch` y exigen `llamadasDeRed === 0`: sin `MAIL_PROVIDER` (bandeja de captura) y con `MAIL_PROVIDER=resend` sin `RESEND_API_KEY` (`not_configured`, outbox `pending`). Toda la suite de correo de `apps/api` corre así. Límite: el camino SMTP no pasa por `fetch`. Auditoría de la integración en curso (#115) | EN_EVIDENCIA |
