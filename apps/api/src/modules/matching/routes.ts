import type { FastifyInstance } from 'fastify';
/** Placeholder temporal: se reemplaza en esta misma ronda tras cerrar los hallazgos de auditoría (docs/auditoria-1/db-api.md). */
export async function matchingRoutes(_app: FastifyInstance): Promise<void> {}
