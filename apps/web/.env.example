# URL base de la API de Atiende Licitaciones (apps/api). Sin backend aún: el
# cliente en src/lib/api.ts usa este valor y muestra ErrorState honesto si la
# petición falla (no hay datos simulados como si fueran reales).
VITE_API_URL=http://localhost:3000
